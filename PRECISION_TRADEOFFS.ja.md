# 精度と互換性のトレードオフ

[English](PRECISION_TRADEOFFS.md)

[COMPATIBILITY_REPORT.ja.md](COMPATIBILITY_REPORT.ja.md) の冒頭で定義している通り、本プロジェクトが検証する「互換性」は CCSDS 122.0 の仕様への適合ではなく、この C 参照実装（`original/source/`）の実際の挙動とのバイト一致である。この方針が実際のコード上でどう現れたか——「数学的により正確・より良い」実装を意図的に退け、Cの挙動（時にCの側の精度・正しさが劣る場合を含む）に合わせた具体的箇所——を本ファイルに列挙する。目的は bpe-rs による置き換えであり、置き換え先が参照実装と異なる答えを返すことは「改善」ではなく「非互換」を意味するため、この優先順位は一貫している。

**更新**: 以下の項目1・3(Cのビルドフラグではなくbpe-rs自体の挙動である2箇所)は、もはや無条件ではない。bpe-rsに`strict_c_compat`切り替えを追加した——既定は有効で、以下に書いた通りCとバイト一致するが、CLIの`--fix-c-quirks`フラグで修正済み・より精度の高い代替挙動へオプトインできる。以下の記述は既定値(`strict_c_compat = true`)についてのもの——`verify/run_compat.sh`がこのフラグを渡さず検証しているのもこの既定挙動。

各項目の詳細な原因分析・修正内容は [INVESTIGATION_LOG.md](INVESTIGATION_LOG.md) の該当節を参照。

## 1. 逆DWT（float）のペア加算順序（INVESTIGATION_LOG.md §3.3）

- **Cの挙動**: `inversef97f` の `0.0406894 * (x[r_idx+1] + x[r_idx-1])` のような式は、Cの「usual arithmetic conversions」が式全体でなく演算子ごとに適用されるため、括弧内の加算（`float`同士）が**まず単精度で**行われ、無サフィックス（`double`）の係数と掛け合わされる段になって初めて倍精度に昇格する。
- **数学的に「より良い」代替案**: 各被加数を先に `f64` へ変換してから加算する（`f(x[r_idx+1]) + f(x[r_idx-1])`）方が、加算段の丸め誤差が生じない分、精度は高い。
- **選んだ実装**: bpe-rs は当初この「より精度の良い」順序で実装していたが、Cとの間に稀な1 ULP差（noise_256で28レート中最大5/28件）を生んでいたため、Cと同じ「f32のまま加算してから昇格」する順序に**わざと変更**した——これが今も既定挙動。`strict_c_compat`フラグ(CLIの`--fix-c-quirks`)により、より精度の高いf64累算順序を再び選べるようになった。Cとの相互運用が目的でない場合向け。
- **トレードオフ**: 既定では数学的な精度を下げてCとのバイト一致を取る。`--fix-c-quirks`はその一致を手放して精度を取り戻す。

## 2. FMA（融合積和）縮約の無効化 — `-ffp-contract=off`（INVESTIGATION_LOG.md §3.10）

- **Cの挙動**: `original/source/makefile` は元々 `-ffp-contract` を指定しておらず、コンパイラ既定のFP_CONTRACT設定に委ねられていた。FMA命令がベースISAにあるターゲット（ARM64/Apple Silicon等）では、`a*b+c` が1回の丸めで計算されるFMA命令に融合され、2回丸める通常の乗算＋加算より**精度が高くなる**。
- **Rust側の制約**: bpe-rs は `f32`/`f64` の `*`・`+` 演算子を常に分離したまま計算し、自動融合しない（Rustの基本算術演算子の仕様上の保証）。
- **選んだ対応**: Cのビルドフラグに `-ffp-contract=off` を追加し、C側のFMA融合を明示的に禁止。forward側（`forwardf97f`、エンコード）・inverse側（デコード）の両方に影響。
- **トレードオフ**: FMAが使えるハードウェアではCの方が本来精度が高い計算ができたが、Rustに合わせるためCの精度を意図的に下げた。x86-64ベースライン（FMA命令自体が存在しないターゲット）では実質no-opだが、ARM64等では実際に計算結果が変わる。

## 3. DPCM DCマッピングの整数折り返し再現（INVESTIGATION_LOG.md §3.2）

DC深度 N=16 のときにのみ発現する、2段階の整数オーバーフロー。

- **`-(short)(...)` の丸め込み**: Cは負値化前に中間値を16bitの `short` へ縮小する。中間値がちょうど32768になる場合、`short`キャストが32768→−32768に折り返り、符号反転後の値が本来の−32768ではなく+32768になる。
- **`theta` の符号なし折り返し**: 上記により`ShiftedDC`が範囲外に出ると、Cの符号なし`DWORD32`同士の引き算が折り返り、`min()`マクロが意図と異なる巨大値を比較してしまう。
- **選んだ実装**: bpe-rsは`wrapping_sub`を使い、Cのこの折り返し挙動を`neg_short`/`theta_from_prev`ヘルパーで明示的に再現。項目1と同じ`strict_c_compat`切り替え配下(既定有効、`--fix-c-quirks`で素直な符号あり演算に切り替え)。
- **トレードオフ**: 「符号あり16bit整数として正しい」演算ではなく、Cのオーバーフローバグをそのまま再現する演算を既定で選んでいる。厳密には浮動小数点精度の話ではないが、「数値的に正しい結果」より「Cとの一致」を優先した点は同じ構造。項目1と違いこちらはビットストリーム自体に影響する(mapped値がそのままRice符号化される)ため、`--fix-c-quirks`を使うとエンコーダ・デコーダはもうC側のツールが読めるフォーマットを生成しなくなる——画素値の丸め差ではなく、フォーマットそのものの非互換。

## 4. `TypeC<<(1<<(3-i))` のタイプミス（`BPEBlockCoding.c`、INVESTIGATION_LOG.md §4）

- **Cの挙動**: `TypeC`（4bitフィールド）に対して `&` と書くべきところを `<<` と誤記したと見られる比較式。数学的に常に真になり、意図されていたと思われる「全ヒット済み」判定分岐には構造的に到達不能。
- **選んだ方針**: 修正せず、Rust側もこの誤記由来の到達不能パターンをそのまま踏襲する対象として扱う。
- **注記**: 制御フロー解析の結果、この分岐は確定デッドコード（実データでは絶対に到達しない）と証明済みであり、**実際の出力バイト列には影響しない**。上記1〜3とは異なり、これは「精度を犠牲にして互換性を得た」実例ではなく、「Cの欠陥を修正しない、という一貫した方針」の一例として参考までに記載する。

## この一覧に含めなかったもの

- **float DWTの丸め誤差修正（INVESTIGATION_LOG.md §3.1）**: `(int)(v+0.5)` の加算をf64側に合わせた修正は、精度とCとの一致が**同時に改善した**ケース（Rust側が元々単精度で計算していたためCより精度が低く、かつ不一致でもあった）。精度を犠牲にした例ではないため除外。
- **`DCGaggleDecoding`/`ACGaggleDecoding`のループ変数オーバーフロー修正**（`short i`→`long int i`、COMPATIBILITY_REPORT.ja.md §6）: これはKiely氏によるC参照実装自体へのバグ修正であり、bpe-rs側は最初から正しい幅で実装している。精度と互換性が対立した箇所ではない。

## まとめ

上記1・2は浮動小数点の丸め・演算順序に関するトレードオフ、3は整数オーバーフローの再現、4はCの明白なタイプミスの黙認。いずれも共通するのは、「より正確な計算」が「Cとは異なる出力」を意味する場面で、本プロジェクトは既定として一貫して後者（Cとの一致）を選んでいるという点——[COMPATIBILITY_REPORT.ja.md](COMPATIBILITY_REPORT.ja.md) 冒頭のスコープ定義通り、目的が参照実装の置き換えである以上、この優先順位は意図的である。項目1・3はオプトアウト可能（`--fix-c-quirks`）——Cとの相互運用よりも精度を重視する呼び出し側向け。
