# C/Rust Compatibility Verification Report

[日本語版](COMPATIBILITY_REPORT.ja.md)

This report summarizes, for the compatibility between `original/` (the C reference implementation of CCSDS 122.0 Bit Plane Encoder, bug-fixed version by Aaron Kiely) and `bpe-rs/` (its Rust port), what perspectives were used, how it was tested, and what was found. The test harness lives in `verify/`; how to run it is documented in [README.md](README.md).

Commits as of writing: this repository ([bpe-c-comparison](https://github.com/isas-yamamoto/bpe-c-comparison)) from `ee40b71` onward / [bpe-rs](https://github.com/isas-yamamoto/bpe-rs) `8ae7bbf`.

## 0. Summary: how far is compatible, and where do differences start

Since this report is long, the conclusions are summarized here first; see each section for details and rationale. **The trend of ongoing verification results is recorded in [verify/results/history.md](verify/results/history.md).**

| Category | Scope | Status |
|---|---|---|
| **Confirmed fully compatible** | Full-pipeline 166/166 cases across both integer and float DWT (§2.1, PASS at every rate value, including the 5 cases that used to be the known 1-ULP residual) / Exhaustive function-level verification 9/9 for Rice, two's-complement conversion, DPCM (DC & AC), pattern mapping, `AdjustOutPut`, `CodingOptions`, `ACDepthEncoder`/`Decoder`, `DCEntropyEncoder`/`Decoder` (§2.2) / Randomized fuzz testing, 3500 cases across both DWT types (§2.3, continuously run in CI) / 8 externally reported bug items from `readme_kielymods` (§6) / Effective coverage 100.00% (both line and branch) across all 15 core files (§4) | **0** mismatches |
| **Known differences** | None. The earlier version of this report claimed a 1-ULP residual in float DWT decoding due to "a difference in compiler rounding implementations," but this turned out to be a real floating-point precision bug on the bpe-rs side, and it's been fixed (§3.3) | — |
| **Out of scope** | Error paths such as file I/O failures, configuration items structurally unreachable from the CLI (e.g. `CustomWtFlag`, `TransposeImg`, 356 lines of confirmed dead code, §4) | Intentionally out of scope |

### How to read this document

This report mixes "what the current state is" (a reference, overwritten as things change) with "how we investigated to reach this conclusion" (a chronological investigation log, which basically only grows). It's faster to narrow down where you read based on your purpose.

| What you want to know | Where to read | Notes |
|---|---|---|
| Just the conclusion | The summary table above, §5 Overall assessment | Usually enough by itself |
| What kind of testing is being done (methodology) | §1 Verification perspectives, §2 Test contents and results | Stable content that rarely changes |
| The list of bugs found and their current status | §3.8 Resolution summary (table) | §3.1–§3.7, §3.9 are the investigation history/detail behind each individual discovery — if the table isn't enough, go to the relevant subsection |
| How much of the C code is actually exercised (numbers) | The three tables in §4: "Final results," "Reachability breakdown," "Branch reachability breakdown" | The other subsections of §4 are investigation history (skippable) |
| What's left to do | §7 Future work | Strikethrough = resolved |

§0, §1, §2, §5, §6 describe "the current state," so reading them in full each time is low-cost. §3, §4, §7 are long, but as the table above says, **picking out just the conclusion (the tables) means you don't need to follow the history** — the history is kept as an audit trail, not something readers are expected to read through every time.

## 1. Verification perspectives

Since "compatible" can't be measured by a single metric, five perspectives of a different nature are combined.

| # | Perspective | What it verifies | Why it's needed |
|---|------|----------------|-----------|
| 1 | Full-pipeline byte compatibility | Encode/decode real images with both implementations and check whether the output byte streams match exactly | This is ultimately the only thing users care about. But to avoid conflating "happened to match on today's test images" with "always matches," images deliberately targeting known boundary values are mixed in |
| 2 | Coverage of known edge cases | Prepare inputs that reproduce the real bug fixes Aaron Kiely recorded in `original/readme_kielymods.rtf` (single-block segments, all-zero images, block-count boundaries, endianness, etc.) and check whether they match there too | Ordinary natural images rarely hit the boundary values that tend to breed bugs. Without deliberately constructing edge cases, "it matched" doesn't mean much |
| 3 | Exhaustive function-level verification | Cross-check Rice coding, two's-complement conversion, DPCM DC mapping, pattern mapping, and `CodingOptions` across the entire (or near-entire) representable input domain, using generators linked directly against the C reference implementation's object code | Perspectives 1 and 2 only exercise "the values that image happens to generate." They can't cover the full range of values an individual function can receive. In fact, the DPCM bug described later was never hit by any ordinary test image |
| 4 | Randomized fuzz testing | Randomly combine image size, bit depth, signedness, byte order, rate, segment size, and pixel content, and repeat the same byte-compatibility check as perspective 1 at scale (`verify/fuzz_compat.py`) | Perspectives 1 and 2 only exercise hand-picked combinations. "100% coverage" only means a branch was executed at least once — it can't rule out a mismatch on a different value that takes the same branch. This perspective compensates for that |
| 5 | Stage-boundary trace comparison | Dump and diff intermediate values from both implementations right after the DWT transform and right after block-string construction (`verify/compare_traces.py`) | A diagnostic tool for immediately pinpointing "at which stage of the encoder did the two diverge" when perspective 1 finds a mismatch. It is not itself a pass/fail criterion |

In addition to the above, **code coverage measurement** (§4 of this report) quantifies how much of the C code perspectives 1 and 2 actually exercise, making visible the "still-unverified areas" that these perspectives alone can't reveal.

## 2. Test contents and results

### 2.1 Full-pipeline byte compatibility (`verify/run_compat.sh`)

166 cases (one of which is a low-frequency regression case that only runs with `--include-slow`). All cases confirmed **encode output byte match, and cross-decode output byte match (C→Rust and Rust→C, both directions)**.

| Case | Contents |
|---|---|
| baseline_256 × (t=0,1) × (r=0, 1.0, 4.0) | An ordinary gradient image. integer/float DWT × lossless/lossy rates |
| all_zero_64 | An all-zero image (regression for the `leftmost==1` special-case bug) |
| minimal_17x17 | The minimum image size (`IMAGE_WIDTH_MIN`/`IMAGE_ROWS_MIN`) |
| single_trailing_block_48x24 | The readme's "trailing segment is a single block" repro (`-s 17`) |
| blocks_15 / 16 / 17→18 / 31→32 / 32 / 33 | Images whose total block count lands on the GAGGLE_SIZE(16) boundary (17 and 31 are geometrically impossible to construct, so adjacent values substitute — see the comment in `verify/gen_vectors.py` for the reason) |
| pixels16_f0 / f1 × (t=0,1) | 16-bit pixels × little/big endian × integer/float DWT (the float DWT side exercises `ImageWriteFloat`'s 16-bit branch, which had never been hit before at all) |
| pixels12_f0 × (t=0,1) | 12-bit pixels (stored in a 16-bit word) × both DWT types. Since `-b 16` doesn't fit in the 4-bit-wide header field and is always treated as 0 ("default 16-bit"), this hits an explicit bit-depth branch that pixels16 alone can't reach |
| signed_32 / signed_32(float) / signed16_32 × (t=0,1) | Signed pixels verified across 4 combinations: 8-bit, 8-bit+float DWT, 16-bit (both DWT types) (`ImageWrite`/`ImageWriteFloat`'s signed branches were only hit for 8-bit + integer DWT with signed_32 alone) |
| signed16_32's explicit bit depth (`-b 12`) × (t=0,1) | Since signed16_32's default `-b 16` is internally treated as 0 (default 16-bit), it can't hit this — targets the signed 16-bit-word explicit-bit-depth branch (a different arm of the `PixelMax` computation) |
| Decode-side byte-order inversion (4 cases) | `PixelByteOrder` isn't part of the bitstream header — it's a per-call CLI setting (see §3.7) — so the decode-time output order can be chosen independently of the value used at encode time. Previously decoding was only ever done in the same order as encoding, so `ImageWrite`/`ImageWriteFloat`'s "byte-order inversion needed" branch had never been hit at all. Confirmed C/Rust match on decodes with the order inverted, using pixels16_f0 and signed16_32, both DWT types |
| baseline_256 rate-limited decode sweep (18 cases) | Sweeps segment size 64 (finer than the default 256) × rate values (0.05–3.0 bpp) across 10 steps for t=1 and 8 steps for t=0, driving `AdjustOutPut` (rate-limited mid-decode recovery) through many different stopping positions. All 8 t=0 values match with no mismatches (the earlier version excluded 2 values as a known residual ULP difference, but after §3.3's root-cause fix, all values PASS) |
| checkerboard_256 rate-limited decode sweep (20 cases) | The same sweep run against a high-contrast checkerboard image (large-amplitude, both-sign AC coefficients). No mismatches, including all 10 t=0 values |
| noise_256 rate-limited decode sweep (17 cases) | The same sweep run against a deterministic pseudo-random image (with neither periodicity nor monotonic structure — a third, independent coefficient-distribution shape). No mismatches, including all 3 t=0 values (the earlier version excluded these due to the residual that existed before §3.3's root-cause fix) |
| large_segment_slow | 33,000 blocks in a single segment (regression for a `short i` overflow, weekly CI only) |
| ac_depth1 / ac_depth2 / ac_depth5_16bit × (t=0,1) | Three 64×64 images (values 1, 2, 17) constructed to target the maximum AC coefficient amplitude (`BitDepthAC_5Bits`). The N-dependent branches (N==2, N==5) in `ACDepthEncoder`/`ACDepthDecoder` (AC_BitPlaneCoding.c), and the dedicated 1-bitplane path for `BitDepthAC_5Bits==1`, had never been hit at all, since ordinary baseline/checkerboard/noise images only ever land on N==3 or N==4 |
| Rate-limited sweep of ac_depth2 / ac_depth5_16bit (8 cases) | Sweeps these 2 images across segment size 16 (the whole image is 64 blocks = roughly 4 segments) × 4 rate steps, additionally driving the mid-segment cutoff branches in `ACBpeEncoding`/`ACGaggleDecoding` |
| dc_depth_n2 / dc_depth_n4 / dc_negpow / dc_mixed_sign (4 cases) | Four 64×64 images constructed to target the DC coefficient's effective bit depth (`N = BitDepthDC_5Bits − QuantizationFactorQ`) and the sign of Max_DC (near-black constant values 1 and 2, a signed power-of-two negative -128, and background +20 with a single block set to -120). The N==2/N<=4 branches of `DCEntropyEncoder`/`Decoder`, and the bit-depth computation when `Max_DC` is negative (including the exact-power-of-two correction), had never been hit at all with ordinary images |
| dc_qprime_lo_64 (1 case) | A 64×64, 16-bit image with a low, constant background value (2) and a single isolated bump of amplitude 50 in one pixel per 8×8 block. The `QuantizationFactorQ_prime = BitDepthDC - 3` branch (when `BitDepthDC > 3` and the AC/DC depth difference is small) had never been hit with ordinary images, where AC/DC depth tend to move together |
| Segment-size-diversity sweep (60 cases, integer DWT only) | 256×256 baseline/checkerboard/noise images × 5 segment-size steps (16, 20, 24, 32, 48) × 4 rate steps (0.5, 0.8, 1.2, 2.0). The dozens of checkpoints in `StagesDeCodingGaggles1/2/3` (StagesCodingGaggles.c) that record the rate-limit stopping position (`X/Y_LocationStopDecoding`) were mostly unreachable with a rate sweep alone at the fixed segment size of 64. float DWT (`-t 0`) has been confirmed to produce no mismatches on the same grid after §3.3's root-cause fix, but since `StagesCodingGaggles.c`'s coverage goal is achievable with integer DWT alone, the sweep is deliberately kept to integer DWT only (see §4) |

**Result: 166/166 PASS** (as of 2026-07-26; the first run FAILed 12 cases when the decode-side byte-order-inversion cases were added — see §3.7).

### 2.2 Exhaustive function-level verification (`verify/run_unit_vectors.py`)

| Target function | C reference implementation | Coverage | Verification method |
|---|---|---|---|
| `RiceCoding`/`RiceDecoding` | ricecoding.c | bit_length 1–4 × valid option combinations (10 total) × the entire representable value range for each | The Rust encode's output matches C's byte stream exactly. C's generated byte stream is also fed to the Rust decode to confirm it correctly recovers the original value sequence (cross-decode) |
| `ConvTwosComp` | DC_EnDeCoding.c | leftmost = 2–16 × the entire representable value range for each width | Rust's `conv_twos_comp` output matches C exactly |
| `DPCM_DCMapper`/`DPCM_DCDeMapper` | DC_EnDeCoding.c | N = 4, 8, 16 × 3 series {monotonically increasing, alternating between both extremes, oscillating around the midpoint} plus 1 fixed sequence (10 series total) | Rust's `dpcm_dc_mapper`/`dpcm_dc_demapper` output (MappedDC, reconstructed ShiftedDC) matches C exactly |
| `PatternMapping` | PatternCoding.c | All 7 (sym_len, type) combinations × the entire representable value range for each | Rust's `pattern_mapping` output matches C exactly |
| `DPCM_ACMapper`/`DPCM_ACDeMapper` | AC_BitPlaneCoding.c | N = 2, 3, 4, 5 (the full range `ac_depth_encoder` supports) × the same 3 series | Rust's `dpcm_ac_mapper`/`dpcm_ac_demapper` output matches C exactly (no mismatches, see §3.4) |
| `AdjustOutPut` | AdjustOutput.c | DWTType(2) × stoppedstage(1–4) × b_DC branch(3) × in-block stop coordinates X/Y(8×8) × value sign variant(4) = all 6144 combinations | Rust's `adjust_output` output (in-block int/float coefficients, 3 blocks' worth) matches C exactly (no mismatches, see §3.5, §3.6) |
| `CodingOptions` | PatternCoding.c | sym_len 2/3/4 × every carrying type, the entire single-symbol range, all 64 sym_len-3 pairs, all 4096 sym_len-4 (a,b,c) triples, plus 2 hand-built cases discovered in §4 item 8 (the false direction at lines 328/329) = 4230 combinations | Rust's `coding_options` output (Option[0..3]) matches C exactly (no mismatches) |
| `ACDepthEncoder`/`ACDepthDecoder` | AC_BitPlaneCoding.c | BitDepthAC_5Bits values representative of the entire N(2-5) range `ACDepthEncoder` supports × 5 segment sizes (roughly 1–3 gaggles' worth) × 3 BitMaxAC distribution patterns (ramp/extremes/mid-boundary) = 120 combinations | Rust's `ac_depth_encoder` output byte stream matches C exactly. C's generated byte stream is also fed to `ac_depth_decoder` to confirm it correctly recovers the original BitMaxAC sequence (cross-decode) |
| `DCEntropyEncoder`/`DCEntropyDecoder` | DC_EnDeCoding.c | All reachable patterns among `QuantizationFactorQ_prime`'s 4 branches for N≥2 × 4 N-brackets (2/≤4/≤8/>8) × 5 segment sizes × 3 ShiftedDC distribution patterns = 75 combinations (§4.3.3's extra bitplane, present or absent, both included) | Rust's `dc_entropy_encoder` output byte stream matches C exactly. Cross-decode against `dc_entropy_decoder` is also checked, but only for the cases without an extra bitplane (see the main text for why) |

**Result: 9/9 test groups PASS** (though, as described below, the DC-side DPCM only reached PASS after finding and fixing 2 mismatches on the first pass).

While generating test vectors for `ACDepthEncoder`/`DCEntropyEncoder`, it turned out `CustomWtLL3_2bits` (the LL3 subband's default weight value) was mistakenly zero-initialized — `HeaderInilization` always hardcodes it to `3` (a separate matter from `CustomWtFlag` always being FALSE; this one is never 0), and `DCEntropyEncoder` uses this value via `max()` in both its `QuantizationFactorQ` and `numaddbitplanes` computations. After fixing the generator to use this value, it was confirmed that bpe-rs's own implementation (`dc_entropy_encoder` itself, in `dc/entropy.rs`) had folded in `custom_wt_ll3` correctly from the start — a test-harness initialization gap, not an implementation bug.

### 2.3 Randomized fuzz testing (`verify/fuzz_compat.py`)

§2.1's 166 cases are a hand-built matrix targeting known edge cases, so input combinations outside it remain, in principle, unverified. To compensate, a fuzz harness was added that randomly combines image size, bit depth, signedness, byte order, rate, segment size, and pixel-content pattern, and repeats the same encode-match plus cross-decode-match check as §2.1 at scale. It targets both integer and float DWT (default `--dwt-type both`). The earlier version excluded float DWT as noise from §3.3's "known 1-ULP residual," but now that the root cause has been fixed, float DWT is just as meaningful a target as anything else, with no reason to exclude it. Rate/segment-size combinations the encoder structurally can't accept (`BPE_RATE_ERROR`, an input-validity issue rather than a C/Rust difference) are handled by automatically retrying at a higher rate.

Integrated into CI in two tiers: a lightweight smoke test of 50 fixed-seed cases on every push/PR, and a deep fuzz of 5000 random-seed cases (the `deep-fuzz` job) on a weekly schedule/manual dispatch. Locally, 500 cases (`--seed 123`) and 3000 cases (`--seed 999`) have been run across both DWT types with 0 mismatches confirmed.

### 2.4 Stage-boundary trace comparison (`verify/compare_traces.py`)

Set up a mechanism to diff 5 points beyond the encode side (right after the DWT transform, right after block-string construction): on the decode side, `adjust_output`'s output, right after segment reassembly, right after the inverse-DWT levels 2/1 complete (level-granularity boundaries `post_idwt_level2`/`post_idwt_level1`), and right after the inverse DWT. Integer DWT matches at every stage. Float DWT now also matches at every stage after §3.3's root-cause fix. This level-granularity breakdown was set up precisely in the process of identifying which level §3.3's 1-ULP difference occurred at (`inverse_lifting97f`'s levels) — it's less a standing pass/fail gate than a primary triage tool for the next time a similar mismatch shows up.

## 3. Discovered mismatches and their resolution

Introducing function-level verification (§2.2), and the rate-limited decode sweep added later (§2.1), uncovered and fixed several real bugs that ordinary testing alone couldn't have caught. **All of them only manifest at "boundary values ordinary test images happen not to hit,"** so extending perspective 3's and perspective 1's sweeps was what made them visible.

**→ The current list in §3.8's table is sufficient by itself. §3.1–3.9 below are the chronological investigation log behind each individual discovery.**

### 3.1 float DWT rounding error

The C reference implementation's `(int)(v + 0.5)` performs the addition in double precision, since `0.5` is a `double` literal. The Rust side was adding in single precision (`f32`), so the rounding result shifts by 1 in the rare case where a value's fractional part lands within 1 ULP of 0.5 (3 pixels out of 65536 in a 256×256 image). Found via §2.1's `-t 0` (float DWT) cases. Fixed both `round_away_from_zero` and `dwt_reverse_floating` to add via `f64`.

### 3.2 Two integer-width bugs in DPCM DC mapping

Only manifests when DC depth N=16. Found via §2.2's DPCM vectors (the sequence oscillating around the midpoint).

1. **`-(short)(...)` rounding**: C narrows the intermediate value to a 16-bit `short` before negating. When the intermediate value is exactly 32768 (when the block's raw value's top bit is set), the `short` cast wraps 32768→−32768, so the value after sign inversion becomes +32768 (the opposite sign of the −32768 Rust was computing).
2. **Unsigned wraparound in `theta = min(ShiftedDC − X_Min, X_Max − ShiftedDC)`**: `ShiftedDC`/`X_Max` are unsigned `DWORD32` in C. Once bug (1) above pushes `ShiftedDC` outside the `[X_Min, X_Max]` range (to +32768), the subtraction `X_Max − ShiftedDC` wraps around as an unsigned operation, producing a huge value instead of a negative one. C's `min()` macro then compares against this huge value, so an unintended `theta` gets chosen.

Fixed on the Rust side by using `wrapping_sub` to explicitly reproduce C's unsigned wraparound (the `neg_short`/`theta_from_prev` helpers).

### 3.3 Precision mismatch in the inverse DWT (float) — root cause identified and fixed

The C reference implementation's `inversef97f` (`lifting_97f.c`) has unsuffixed coefficient literals (`0.788486`, etc.), which are `double`, so the whole computation involving the `float` array is promoted to double precision before narrowing back to `float` at the very end. The Rust side computed entirely in single precision — a structural problem where **decoding using float DWT accumulates a small (~1e-6–1e-7) but pervasive error**. This normally didn't cross the boundary of the final pixel rounding (`(int)(v+0.5)`), so §2.1's basic cases never surfaced it, but it actually shifted a pixel by 1 at segment size 64, rate 0.1/0.75 (§2.1's rate sweep).

Changing `inverse_lifting97f`'s formulas to compute via `f64` intermediates (reproducing C's promotion) made 8 of the 10 float-DWT cases targeted by the sweep newly byte-match. **The remaining 2 (0.1, 0.75) still shifted by 1 pixel.** Isolating it to a single level and a single function call with `verify/compare_traces.py` found, in practice, cases where the C and Rust sides used identical coefficients (byte-identical f64 literals, confirmed empirically) yet the results differed by 1 ULP. This was initially, and **incorrectly**, judged to be an unresolvable residual difference stemming from gcc's and rustc's (LLVM's) floating-point rounding implementations, not an algorithm bug. However, after re-investigating in response to a later user request to "seriously resolve" it, it turned out to be **a genuine, fixable bug on the bpe-rs side**.

The same phenomenon had also been confirmed when checkerboard and noise images were added to the same sweep (quantified under §7's former item 2). The frequency varies with image content: baseline_256 (gradient) hit it in 2 of 8 rate values, checkerboard_256 in 0 of 10, and noise_256 (pseudo-random, the widest coefficient distribution) in 3 of 10. The tendency that "images with a wider, more extreme coefficient distribution are more likely to hit the 1-ULP boundary" is consistent with the true cause described below (rounding-error accumulation at the f32-addition stage).

**Two hypotheses rejected under the original misdiagnosis** (which turned out to be off-target, but kept here as a record of the investigation):

1. **`-ffp-contract=off`** (explicitly disabling FMA (fused multiply-add) contraction in the C reference implementation's C compiler): no change in the result. This build environment's gcc default target (the `x86-64` baseline) has `-mfma` disabled and can't generate hardware FMA instructions in the first place, so this correctly confirmed FMA contraction wasn't the cause.
2. **`-O0`** (fully disabling the C compiler's optimizations): this made things *worse* (34646 of 65536 pixels mismatched). The compile-time constant-folding hypothesis was also correctly rejected.

**Added level-granularity trace points to narrow it down**: added `post_idwt_level2`/`post_idwt_level1` to `compare_traces.py` (dumps right after each level's row pass in `lifting_f97_2D`/`lifting_f97_2d`, levels=3 fixed, processing column-pass then row-pass in level order 2→1→0). Running against the known-reproducing case (`baseline_256`, rate=0.1, `-t 0`, segment=64) showed `reassembled` (right before the inverse transform) matching exactly, but a divergence already present at the coarsest level (`post_idwt_level2`) (index 3, diff=-7.63e-6). This narrowed the search from "somewhere in the 3 levels" down to "somewhere within the first level."

**The true root cause**: the oversight was that C's "usual arithmetic conversions" apply **per operator, not to the whole expression**. `inversef97f`'s actual formulas take the form, for example, `0.0406894 * (x[r_idx+1] + x[r_idx-1])`, where both operands of `x[r_idx+1] + x[r_idx-1]` inside the parentheses are `float`, so **that addition itself happens in single precision (`float`) first**. That `float` sum only gets promoted to double precision once it's multiplied by the unsuffixed (`double`) coefficient `0.0406894`. bpe-rs's implementation, however, converted each addend to `f64` individually **before** the addition (`f(x[r_idx+1]) + f(x[r_idx-1])`, an addition of two values that were already `f64`). "Convert to f64 first, then add" and "add in f32, then convert to f64" aren't mathematically equivalent, since rounding error can occur at the f32-addition stage — this was the true cause of the 1-ULP difference.

**Fix**: added `fp(a: f32, b: f32) -> f64` (a helper that adds in f32 and then converts to f64) to `inverse_lifting97f` in `bpe-rs/src/wavelet/lifting97f.rs`, and switched every pairwise-addition site to go through this helper (faithfully reproducing C's per-operator type-conversion rules). Also added a regression test with hardcoded real data (a 64-element array, the actual intermediate values from decoding `baseline_256` at rate=0.1).

**Verification results**:
- Minimal reproducing case (a 64-element standard input): 13 of 64 elements mismatched before the fix → **0** after.
- Known-reproducing case (`baseline_256`, rate=0.1, `-t 0`, segment=64): `post_idwt` matches exactly, and the final output is byte-identical too.
- All 5 rate values previously excluded for `baseline_256`/`noise_256` are back to byte-identical.
- `verify/run_compat.sh --include-slow`: **178/178 PASS** with all exclusions removed.
- `verify/fuzz_compat.py --dwt-type both`: 0 mismatches across both 500 cases (seed=123) and 3000 cases (seed=999).

**Encode-side check**: also checked whether the same kind of bug exists on the encode side (the forward DWT, `forwardf97f`). The C reference implementation's `LowPassFilter`/`HighPassFilter` (the coefficients `forwardf97f` uses) are declared `const float` — unlike the inverse-transform side's `LowPassFilterInverse`/`HighPassFilterInverse` (`double const`), they're single precision from the start. So there's no room for this kind of promotion-timing difference to arise on the forward side, and no bug exists there. This resolves the encode-side concern that §7's former item 2 flagged as "to be checked in the future."

**Conclusion**: the precision mismatch in the inverse DWT (float) has been root-caused and fully fixed. The original judgment — "an unresolvable residual difference due to a compiler difference between gcc and rustc" — was wrong. It was actually a fixable implementation bug rooted in addition order (float-first vs. f64-first).

### 3.4 Additional verification of AC-side DPCM (no bug)

Following the discovery of the DC-side DPCM bug, additional verification (§2.2) was done to check whether AC-side `DPCM_ACMapper`/`DPCM_ACDeMapper` has the same kind of problem. On the AC side, `BitMaxAC`/`MappedAC` are `WORD16` (unsigned 16-bit), which C's integer-promotion rules promote to `int` (signed), so the kind of unsigned wraparound seen with the DC side's `DWORD32` (roughly unsigned 64-bit) can't happen in principle — this type-analysis prediction was actually verified by generating vectors, and **confirmed no mismatches**. This kept faith with this project's verification policy of not leaving an "it shouldn't happen" claim unmeasured.

### 3.5 Exhaustive direct-call verification of `AdjustOutPut` (no bug)

As described later in §4, no matter how much the rate and image content were expanded in the full-pipeline byte-compatibility test (perspective 1), it turned out most of `AdjustOutPut` (the rate-limited mid-decode recovery function, the single largest function at about 49% of the whole core algorithm) — specifically the in-block stopping coordinates (`X_LocationStopDecoding`/`Y_LocationStopDecoding`, 64 combinations of 8×8) — was effectively unreachable, since it depends on wherever the bitstream's remainder happens to run out.

So the same technique used for Rice/DPCM/pattern mapping — "link directly against the C reference implementation's object code and call the function standalone" — was applied to this function too (`verify/c_unit_tests/gen_adjust_output_vectors.c`). It directly constructs `StructCodingPara`/`BitPlaneBits` and directly drives all 1024 combinations of `DWTType` (integer/floating-point) × `stoppedstage` (1–4) × presence of the DC branch (2) × in-block stopping coordinates `X/Y_LocationStopDecoding` (8×8=64), cross-checking the resulting 8×8 block coefficients (both int and float, 3 blocks' worth) between C and Rust.

**Result: 1024/1024 cases, all matching.** No mismatches were found in `AdjustOutPut`/`adjust_output`, including the fine details of stopping coordinates the full-pipeline test couldn't reach.

### 3.6 Value diversification for `AdjustOutPut`'s direct calls (no bug)

§3.5's 1024 cases had complete **structural** coverage of "which DWTType × stoppedstage × X/Y coordinate is exercised," but `AdjustOutput.c`'s coverage plateaued at 97.35%, with 95 lines still unexecuted (§4). Looking into it, **almost all** of the 95 lines were one side of a sign-dependent branch pair of the form `if(coeff > 0) coeff += X; else if(coeff < 0) coeff -= X;`. The cause: `int_val`/`float_val` (values deterministically hashed from a block's (block, m, n)), which §3.5's generator uses, only ever generated a value of **the same sign** for the specific cell position corresponding to a given structural path (a stoppedstage × X/Y-coordinate combination) — even trying all 1024 structural combinations, that cell's value-sign dimension stayed fixed.

As a countermeasure, a `variant` dimension (4 values, 0–3) was introduced to `int_val`/`float_val` that applies sign inversion and phase shift, expanding the existing 1024 combinations to 1024 × 4 variants = 4096. Additionally, it turned out the `else` branch of `b_DC = min(BitPlaneStopDecoding, QuantizationFactorQ)` (the case `BitPlaneStopDecoding > QuantizationFactorQ`) was never true under the previous 2 b_DC settings, so a third b_DC setting satisfying it was added (6144 total).

**Result**: `AdjustOutput.c`'s standalone coverage (the direct-call generator alone, not combined with other tests) rose from 95.70% to 99.55%. The same `variant` logic was replicated on the Rust side in `bpe-rs/src/adjust.rs`'s tests, confirming all 6144 cases match (no mismatches).

Tracing the remaining 16 lines (8 lines × 2 sites, the integer half and the floating-point half respectively) via control-flow analysis found them to be **structurally unreachable redundant guards**: the `else` clause containing these 16 lines corresponds to the `else` side of the condition `if((m<4)&&(n<4))` (i.e. `m>=4 || n>=4`), but by the time control reaches the scope this `else` clause sits in, `m<=3 && n<=3` is already guaranteed by an outer branch (one that handles `m>3||n>3` separately, first). So `(m<4)&&(n<4)` is always true, and that `else` can never mathematically execute. Confirmed purely by reading the source code (the relationship between `AdjustOutput.c:298`'s condition and its outer `m>3||n>3` guard).

This brought `AdjustOutput.c`, combined with §4's full pipeline test, up to 97.35%→99.55% lines and 85.16%→87.15% branches, and **the remaining 16 lines are all confirmed dead code, for 100% effective coverage**.

### 3.7 Decode-side ignoring of `-f` (PixelByteOrder)

Discovered while further investigating `bpe_decoder.c`'s coverage (§4). `PixelByteOrder` isn't part of the bitstream header — in the C reference implementation it's simply a **per-call CLI setting** (defaulting to 0 at `header.c:91`, parsed from `-f` at `main.c:170`). That is, in the C reference implementation, the `-f` given at decode time determines that decode call's output byte order, independent of the value used at encode time.

However, `verify/run_compat.sh` had never once passed `-f` to the decode-side calls (it was only included in the encode side's common arguments, never passed to the 4 decode calls). Since this machine is little-endian, every existing case that didn't pass `-f` was always decoded with the decode side's default (0 = little-endian), so the "swap bytes if the requested order differs from the machine's native order" branch (`PixelByteOrder != machineendianness`) inside `ImageWrite`/`ImageWriteFloat` had never once been taken on either the C or the Rust side.

While filling in `bpe_decoder.c`'s coverage, `run_compat.sh` was fixed to also explicitly pass `-f` to the decode calls (existing cases kept defaulting to their own encode-time value, new cases deliberately specified an inverted value), and **this exposed a mismatch that had been hidden on the bpe-rs side**: 12 cases newly FAILed, including `pixels16_f1` (an existing case that both encodes and decodes with `-f 1`).

Root-cause investigation found: `bpe-rs/src/main.rs` always parsed the CLI's `-f` into a local variable, but only reflected it into `CodingPara.pixel_byte_order` on the **encode branch** — on the decode branch it was silently dropped without being assigned. `image_write`/`image_write_float`'s (`src/image_io.rs`) own swap-decision logic had been correctly ported from the C reference implementation, but since `pixel_byte_order` always stayed at the struct's default value of `0`, whatever `-f` was specified at decode time, only the "no swap needed" side was ever taken.

Fixed by adding `coding.pixel_byte_order = byte_order;` to the decode branch (the same single line as the corresponding line in the encode branch). After the fix, all cases in §2.1 PASS (including the 12 that exposed this mismatch — §2.1 had 87 cases at the time of discovery, and has since grown to the current 166 with subsequent additions for AC/DC/StagesCodingGaggles).

**Implication**: this mismatch went unnoticed all the way to case 87, because the full-pipeline test (perspective 1) had never once included a case that actually requests a non-default byte order at decode time. This is one example where coverage measurement — visualizing where perspective 1 does and doesn't exercise the C code — directly led to discovering not just untouched C code, but also **a gap in the test matrix itself** (a missing pattern that had been hiding a Rust-side bug).

### 3.8 Resolution status summary

| Mismatch | How found | Scope of impact | Status |
|---|---|---|---|
| float DWT rounding error (forward/rescale) | Perspective 1 (full pipeline, `-t 0`) | float DWT usage, rare pixels | Fixed |
| DPCM short-cast wraparound | Perspective 3 (DPCM function vectors) | DC depth N=16, a specific raw-value pattern | Fixed |
| DPCM theta unsigned wraparound | Perspective 3 (DPCM function vectors) | Triggered as a consequence of (1) above, N=16 | Fixed |
| Inverse DWT (inversef97f) precision mismatch | Perspective 1 (rate-limited decode sweep) | float DWT decoding in general, normally not manifesting | Fully fixed. Originally misdiagnosed as a compiler-rounding-implementation difference, later found to be a genuine bug caused by addition order (f32-first vs. f64-first), and fixed |
| AC-side DPCM | Additional perspective-2 verification | — | N/A (no bug confirmed) |
| `AdjustOutPut` (all 1024 stopping-coordinate patterns) | Additional perspective-3 verification (direct calls) | — | N/A (no bug confirmed, §3.5) |
| Decode-side ignoring of `-f` (PixelByteOrder) | Perspective 1 (test-matrix review during `bpe_decoder.c` coverage expansion) | Decoding 16-bit pixels with a non-default byte order specified, in general | Fixed (§3.7) |

Confirmed after the fixes that all tests in §2.1 and §2.2 PASS (with §3.3's then-remaining 2 cases excluded from the sweep at the time).

### 3.9 Verifying the test suite's detection power via mutation testing

Since code coverage only sees "was this code path executed," as extra insurance backing up §7 item 13's point ("100% coverage isn't a proof for all input values"), bugs (mutations) were deliberately injected into bpe-rs's source to check whether the existing test suite actually detects them. `cargo-mutants` (v27.1.0) was introduced, targeting the 8 files where the shared-vector tests (Rice/DPCM/PatternMapping/AdjustOutPut/CodingOptions/ACDepthEncoder/DCEntropyEncoder, all marked `#[ignore]` and only run with `--include-ignored`) actually have an effect.

**Incident and recovery during the automated run**: `cargo-mutants`'s automated mode had 2 constraints. (1) When `--in-place` (a fast mode that mutates the real source directly for testing) was run in the background, the harness's background-task tracking failed to detect the actual process finishing and falsely reported "complete," while the process itself kept running undetected and left `bpe-rs`'s source files mutated. The process was found via `ps aux` and `kill`ed, and recovery was confirmed via `git status`/`git diff` showing every file matched HEAD exactly (no harm done). (2) The default scratch-copy mode (without `--in-place`) is safe, but the `#[ignore]` tests reference a relative path, `../verify/vectors/*.txt`, that doesn't resolve from the temporary copy directory, since it's a file that lives in the `bpe-c-comparison` parent repository — so a run with `--include-ignored` couldn't work at all. Because of this constraint, the automated run's results are a measurement against "only the tests that are always active."

**Automated run results**: across 8 files and 1131 mutants, 383 detected, 655 undetected, 73 unbuildable (invalid mutants), 20 timeouts. The roughly 63% undetected rate is high, but this mainly reflects a limitation of the measurement method itself: `adjust/stage3.rs`/`adjust/stage4.rs` (`AdjustOutPut`'s body, accounting for 324 of the 655 undetected) aren't covered by the always-active tests at all — they're only verified by the `adjust_output` shared-vector test (6144 combinations, §3.5, §3.6) — so the number underestimates the test suite's actual detection power.

**Manual verification for corroboration**: 5 mutants the automated run classified as "undetected" (`pattern/options.rs`, `ac/depth.rs`, `dc/entropy.rs`, `adjust/stage4.rs`, `rice/encode.rs` — each changing a single comparison or arithmetic operator in core logic) were selected, manually applied to the real bpe-rs source, and run through `cargo test -- --include-ignored` for the corresponding shared-vector tests, restoring via `git checkout` each cycle.

| File:line | Mutation | Verdict |
|---|---|---|
| `dc/entropy.rs:74` | `bit_depth_ac_5bits < quantization_factor_q` → `==` (the trigger condition for §4.3.3's extra bitplane — the exact condition where a test-harness bug was found in an earlier round) | **Detected** (`dc_entropy_shared_vectors_match_c_reference` fails) |
| `pattern/options.rs:31` | `sym_len == 1` → `!=` | **Detected** (the `coding_options` shared-vector test fails) |
| `ac/depth.rs:47` | `dpcm_ac_mapper`'s `x_max` computation, `- 1` → `+ 1` | **Detected** (both the AC DPCM and ACDepthEncoder shared-vector tests fail) |
| `adjust/stage4.rs:22` | `ii < stop_block` → `==` (a branch in `AdjustOutPut`'s body) | **Detected** (the `adjust_output` shared-vector test fails — confirming that `stage3`/`stage4`, which the automated run classified as "324 undetected," are in fact almost entirely detectable) |
| `rice/encode.rs:74` | `input_val - 4` → `input_val + 4` (bit_length=4, option[2]==0, the 2-bit remainder for inputs 4–7) | **Turned out to be an equivalent mutant** (see below) |

4 of the 5, despite being classified as "undetected" by the automated run, were reliably detected by the actual full test suite (including `--include-ignored`) — confirming the automated run's figure of 655 is an underestimate caused by the measurement tool's constraint (the relative-path resolution issue), not a weakness in the test suite itself.

**Discovering an equivalent mutant**: the one case that went undetected, `rice/encode.rs:74`, only outputs the low-order bits of the specified bit width (`bits_write` writes out `(bit >> i) & 1` for exactly that many bits), so for `input_val` in the range 4–7, `input_val - 4` (0–3) and `input_val + 4` (8–11) are completely identical in their low 2 bits (e.g. 4 → 0 or 8, both `00` in the low 2 bits). In other words, this mutation changes the output byte stream not at all — a **mathematically equivalent mutation** that no test, however constructed, could ever detect in principle — not a weakness of the test suite, but a known limitation of mutation testing (equivalent mutants occur at some rate under any classification method).

**Conclusion**: as far as manual sampling could corroborate, this project's shared-vector test suite (including `--include-ignored`) has been confirmed to have real, effective detection power against deliberately injected bugs, with a high probability of catching them. A fully automated, large-scale mutation test (mechanically running all 1131 mutants with `--include-ignored`) wasn't feasible this round, due to `cargo-mutants`'s relative-path-resolution constraint — it would be possible by separately building a mechanism that pre-generates test vectors the same way CI's `verify/run_unit_vectors.py` does, then runs `--in-place` safely and automatically, but manual-sampling corroboration was judged sufficient for this round's cost/benefit.

## 4. Coverage evaluation (C code exercise rate)

Measured with `gcov` how much of the C reference implementation's actual code the test suite in §2.1/§2.2 actually exercises (targeting the 15 core algorithm files; `main.c` (CLI argument parsing), the third-party `getopt.c`, and the error-message table `errorhandle.c` are out of scope).

**→ If you just want the current numbers, jump to the three tables below: "Final results," "Reachability breakdown," "Branch reachability breakdown." The other subsections are investigation history (skippable).**

### Correcting the measurement method (a broken `gcov` `.gcda` merge)

Measurements up to this point had **separately recompiled** each shared `.c` file (e.g. `DC_EnDeCoding.c`) with `gcc --coverage -c`, once for the main `bpe` binary (which includes `main.c`) and again for each per-function direct-call generator (e.g. `gen_rice_vectors.c`), before linking. It turned out this approach caused the later-linked side's `--coverage` build to fail to correctly merge the `.gcda` accumulated from the earlier run's execution, silently undercounting the values.

How it was found: adding the 1024-case direct-call generator for `AdjustOutPut` (§3.5) to the existing measurement build, linked in, produced no change whatsoever — not even a fraction of a percent — in `AdjustOutput.c`'s coverage. Running the same generator in an **independent build** that never recompiled the `.c` files it depends on produced 95.70% line coverage for `AdjustOutput.c` alone (matching the baseline value from before §3.6's value diversification) — a result completely different from the existing measurement. This meant not "the generator isn't increasing coverage" but "the measurement method itself was broken."

Fix: changed to compiling every target `.c` file with `gcc -O0 --coverage -c` exactly **once**, and linking both the `bpe` binary and all 6 generators against this shared set of object files (without recompiling) via `gcc --coverage <objs> -o <bin>` (`--coverage` is needed at link time too — for resolving `libgcov` symbols like `__gcov_init`/`__gcov_exit`). Confirmed this approach correctly merges every build's `.gcda` in a single directory. **All figures below are re-measured under this corrected method.** The aggregate figures reported for the 15 core files in past rounds (line 75.96%, branch 66.26%) were undercounts caused by the `.gcda` merge being broken while combining multiple binaries, and are invalidated and superseded by this section's figures. Note that `AdjustOutput.c`'s standalone figures (line 65.02%, branch 56.60%, before §3.5 was applied, measured with the `bpe` binary alone with no merging needed) aren't affected by this merge inconsistency and aren't subject to invalidation — they remain valid as the baseline for §3.5's improvement described later (§4 "Final results").

### Expanding coverage of AC-side functions

`AC_BitPlaneCoding.c` (AC coefficient bitplane coding/decoding) had the lowest line coverage of all files (79.92%/71.88%), so unexecuted lines were identified from `gcov`'s annotated output. The main cause: `ACDepthEncoder`/`ACDepthDecoder` branch on `N` (the bit length of `BitDepthAC_5Bits`), which is derived from the AC coefficients' maximum amplitude, and there's a dedicated 1-bitplane path that bypasses `ACDepthEncoder`/`Decoder` entirely when `BitDepthAC_5Bits==1`. The existing baseline/checkerboard/noise images all landed on either `N==3` or `N==4` (`BitDepthAC_5Bits` 4–15), never hitting the `N==2`, `N==5`, or `BitDepthAC_5Bits==1` branches at all.

Since `BitDepthAC_5Bits` is only known at runtime (dynamically determined from the AC coefficients' maximum amplitude), inputs producing a targeted value had to be searched for **empirically**: a small diagnostic tool was built that reads the header and prints `BitDepthAC_5Bits`, and by repeatedly encoding candidate images and checking the value, it was narrowed down that, for a 64×64 image, "an otherwise near-uniform image with a single +1 offset placed in the very last pixel" gives `BitDepthAC_5Bits==1`, "the same offset placed on an interior pixel" gives `==2`, and "a maximum-amplitude checkerboard (period=1) at 16-bit pixel depth" gives `==17` (`N==5`) — fixed as `ac_depth1_64`/`ac_depth2_64`/`ac_depth5_16bit_64` in `verify/gen_vectors.py`.

Adding these images to `verify/run_compat.sh`, both losslessly (full decode) and in a rate sweep at segment size 16, brought `AC_BitPlaneCoding.c` from 79.92%→86.07% lines and 71.88%→80.73% branches, and `BPEBlockCoding.c` (the block-scan processing AC coding calls) from 93.96%→94.86% lines and 84.20%→85.75% branches. As a side effect, the same segment-size-16 rate sweep also pushed `DC_EnDeCoding.c` (which has the same kind of N-dependent and rate-limit-cutoff branches on the DC side) from 83.04%→88.60% lines and 70.73%→80.08% branches (this wasn't explicitly targeted this time, but since the DC side has a structure paralleling the AC side, the same input set improved both simultaneously).

### Expanding `StagesCodingGaggles.c`'s coverage (segment-size diversification)

Among the 15 core files, `StagesCodingGaggles.c` had the largest absolute number of unexecuted lines (85.93%/81.56%, 82 of 583 lines unexecuted). Reading through it, `StagesDeCodingGaggles1/2/3` decode symbols on the decode side while walking each of stages 1/2/3's quadrant-recursion trees, and there are dozens of branches embedded throughout (right after reading an option, right after reading a value, right after reading a sign bit, etc.) of the form "if the rate limit has just been reached, record the exact position (block number, `X/Y_LocationStopDecoding`) and exit immediately" — this is precisely the code that produces `AdjustOutPut`'s input (where the rate limit stopped).

Most of the unexecuted lines fit this "cut off at exactly this checkpoint" pattern, and which checkpoint actually gets hit depends on where the segment's remaining byte budget runs out. Every sweep up to this point had kept segment size fixed at 64 and varied only the rate, so **varying segment size itself** was an untested dimension. Trying it confirmed that varying segment size reaches new checkpoints far more efficiently than increasing the rate does (since `SEGMENT_S_MIN` in global.h is 16, added 60 cases total: 5 segment-size steps 16–48 × 4 rate steps × 3 images × integer DWT only). float DWT (`-t 0`) was also tried on the same grid; at the time, the same signature as §3.3's 1-ULP residual reproduced at a high rate (11 of 20 combos for noise_256 alone), so it was excluded from this sweep. After §3.3's root cause was later identified and fixed, no mismatches occur on the same grid anymore. However, since `StagesCodingGaggles.c`'s coverage goal is fully achievable with integer DWT alone, the sweep is deliberately kept to integer DWT only.

As a result, `StagesCodingGaggles.c` rose from 85.93%→88.85% lines and 81.56%→83.69% branches. As a side effect, `ricecoding.c` (92.99%→95.15% lines, 88.36%→90.09% branches) and `AdjustOutput.c` (96.93%→97.35% lines, 84.46%→85.16% branches) rose slightly too. The remaining unexecuted lines (65) split between: confirmed dead code from another CLI-unreachable field, `StageStop_2Bits` always fixed at 3 (described later); a defensive branch that's structurally impossible for pattern length to reach 4; harmless no-op debug scaffolding; and the remaining checkpoints (about 54 lines) that still depend on specific timing, of the same nature as `AdjustOutPut`'s remainder.

### Expanding `DC_EnDeCoding.c`'s coverage (N boundaries, negative Max_DC)

While fixing the AC side's N boundary (previous section), it turned out the DC side had the same kind of untouched branch remaining (the DC entropy coder's N==2/N<=4 branches). But the DC side's `N` uses a different formula than the AC side (`N = max(BitDepthDC_5Bits - QuantizationFactorQ, 1)`, where `BitDepthDC_5Bits` itself is a raw value, not a bit-length-of-a-bit-length), so the AC side's "offset a single pixel" technique couldn't be reused, and an empirical re-search was needed.

Investigation with the diagnostic tool found `BitDepthDC_5Bits` tracks the pixel value's magnitude (roughly average pixel value × 8), landing around 12–13 for ordinary natural images. Constructing near-black constant images (values 1, 2) and pairing each with an appropriate lossless/lossy rate confirmed both the `N==2` and `N<=4` branches could be hit (`dc_depth_n2_64`/`dc_depth_n4_64`).

Furthermore, `BitDepthDC_5Bits`'s computation has a separate path (including a correction for the exact-power-of-two case) for when Max_DC is negative in a signed image, and this too was untouched. Combining a signed constant image (a power-of-two negative value, e.g. -128) with an image that's background +20 except a single 8×8 block set to -120 (`dc_negpow_64`/`dc_mixed_sign_64`), all branches of this path were confirmed empirically to be reachable.

As a result, `DC_EnDeCoding.c` rose from 83.04%→93.27% lines and 70.73%→86.18% branches. Almost all of the remaining 23 lines are confirmed dead code (16 lines of `OptDCSelect==FALSE` heuristic branches, 4 lines of defensive `ErrorMsg`); the 2 lines of a specific `QuantizationFactorQ_prime` condition and 1 line of `return k;` that had remained unexplained at the time were settled in a later residual-gap-resolution round (described below) — the former 2 lines were actually covered with a new image, `dc_qprime_lo_64`, and the latter 1 line was confirmed to be confirmed dead code by verifying `DCEntropyDecoder` has no code path that returns anything other than `BPE_OK` (its only error branch calls `ErrorMsg`, which `exit()`s), bringing `DC_EnDeCoding.c` to 100% effective coverage.

### Value diversification for `AdjustOutput.c` (see §3.6 for details)

`AdjustOutput.c`'s coverage plateaued at 97.35% (95 lines unexecuted) with §3.5's structural exhaustiveness (1024 combinations) alone. As discovered in §3.6, almost all of the unexecuted lines were one side of a sign-dependent branch pair, caused by a lack of sign variation in the test values. Introducing a `variant` (sign inversion, phase shift) expanded 1024 to 6144 combinations, plus adding a third case for the `b_DC` computation, brought it to 97.35%→99.55% lines and 85.16%→87.15% branches. The remaining 16 lines were found via control-flow analysis to be confirmed dead code (an `else` clause of `(m<4)&&(n<4)`, a redundant guard already guaranteed true by the time it's reached), reaching 100% effective coverage.

### Resolving the residual gap (direct-call harnesses for `StagesCodingGaggles.c`, the AC/DC side, `BPEBlockCoding.c`, and `PatternCoding.c`)

Effective coverage had reached 99.00% (70 lines remaining) by the end of the previous sections, but rather than continuing to pass on those 70 lines as "lower cost/benefit than `AdjustOutPut` since there are fewer of them," the same technique used for `AdjustOutPut` — "link directly against the C reference implementation's object code and drive the function standalone" — was applied, resolving most of them.

**`StagesCodingGaggles.c` (54 lines of stopping-position checkpoints)**: built `verify/c_unit_tests/gen_stages_gaggles_coverage.c`, which calls `StagesDeCodingGaggles1/2/3` directly and exhaustively sweeps `DecodingAllowedBitsSizeInSegment` (a bit-granular budget) from 0 to 150 against a synthesized bitstream built from real files. Since `BitsRead` (bitsIO.c) sets `RateReached` the instant it detects "exactly this bit exceeded the budget," calling the function fresh while varying the budget one bit at a time mechanically reproduces every possible cutoff point right after every read — running a sweep that multiplies the structural combinations of `TypeP`/`TranD`/`TranGi` (3 bits each, 512 combinations total) against DWTType, the custom-weight threshold, and 3 bit patterns newly covered 23 of the 54 lines. Control-flow analysis of the remaining 31 lines proved **all of them confirmed dead code** (details below).

**`DC_EnDeCoding.c`'s `QuantizationFactorQ_prime = BitDepthDC - 3` branch (2 lines)**: the condition `BitDepthDC > 3` and `BitDepthDC - (1+(BitDepthAC>>1)) <= 1` couldn't be hit with images where AC/DC depth tend to move together (both flat images and checkerboards were tried, but the depth difference between the two always stayed fixed at 2, never satisfying the condition). Measuring `BitDepthDC`/`BitDepthAC` empirically across many image patterns with a diagnostic build found that a pattern of **a low, constant background value (2) with a single isolated bump of amplitude 50 in one pixel per 8×8 block** decouples the two (`dc_qprime_lo_64`, `sparse_bump_per_block` in `verify/gen_vectors.py`), newly covering this branch on both encode and decode.

**`PatternCoding.c`'s `RefBitsDe` trailing checkpoint (2 lines)**: `if(BitPlaneStopDecoding!=-1 && RateReached==TRUE){BlockNoStopDecoding=BlockSeq;}`, an end-of-line recording step that, unlike other checkpoints, has no `!LocationFind` guard. Calling directly with `RateReached`/`BitPlaneStopDecoding`/`LocationFind` initialized to mimic "the stopping position was already fixed on a previous bitplane," and the relevant BlockSeq's `RefineBits`-family fields all zeroed (unprocessed), confirmed this line is reached normally and the whole loop passes through — an example of reproducing, via a plain function call, a different kind of state than `AdjustOutPut`'s stopping position: "a later bitplane call continuing within the same segment."

**`AC_BitPlaneCoding.c`'s `RateReached` early return inside `ACGaggleDecoding` (1 line)**: an unguarded, simple `if(RateReached==TRUE)return;`, reached immediately just by sweeping `DecodingAllowedBitsSizeInSegment` in bits from 0 to 40.

These direct-call generators are wired into the coverage-measurement build only, as `verify/c_unit_tests/gen_stages_gaggles_coverage.c`/`gen_residual_gaps_coverage.c` (there's no corresponding verification on the Rust side, and they don't produce output files, so they aren't included in `verify/run_unit_vectors.py`'s exhaustive-verification suite — their purpose is purely measuring the C reference implementation's coverage; C/Rust cross-checking is already covered by the other perspectives).

### Reclassifying reachability: newly proven patterns of confirmed dead code

Thoroughly pursuing the direct-call sweeps above and then running control-flow analysis on the lines that remained to the very end found that **most of the lines classified as "timing-dependent residual gap" were, in fact, deterministically unreachable dead code**. 3 newly found patterns emerged (all variants of the "self-contained redundant guard" pattern found in `AdjustOutput.c`).

1. **The "a narrower checkpoint always intercepts first" pattern** (`StagesCodingGaggles.c`, 30 lines): inside `StagesDeCodingGaggles1/2/3`, only `bitsIO.c`'s `BitsRead` ever sets `RateReached`, and it always updates `BitPlaneStopDecoding` at the same time. Reading through the 3 functions revealed that **every single bit read is immediately followed by a checkpoint dedicated to that specific read** (including the `!LocationFind` guard). That is, the moment `RateReached` newly becomes true, it's always caught by the nearest checkpoint right after it, and once `LocationFind` becomes true, no later checkpoint can ever become true again (same guard condition). So a broader-scope checkpoint — like an unconditional "end of loop" check sitting after one branch of an if/else that involves no read — is structurally unreachable, since a narrower checkpoint always intercepts before it. Forcing the checkpoint condition true externally via `gdb` confirmed control always actually returns at the first (narrower) checkpoint.
2. **The "branch contradicts its entry condition" pattern** (`StagesCodingGaggles.c`, 4 lines): some checkpoints inside `StagesDeCodingGaggles2`/`3` record the coordinate reached only on the `else` side of a 3-way `if(hit_history & 0x4 == 0){...} else if(hit_history & 0x2 == 0){...} else{...}`, but this `else` (both bits already set) is mathematically incompatible with the precondition for entering that checkpoint at all ("there are still 2 or more elements not yet found") — if both bits are set, at most 1 element remains undiscovered, which can't satisfy the entry condition.
3. **The "RateReached and BitPlaneStopDecoding only ever change together" pattern** (`PatternCoding.c`'s `StagesDeCoding`, 4 lines): the only place in the entire source that sets `RateReached=TRUE` is a single spot in `bitsIO.c`, and that same spot always rewrites `BitPlaneStopDecoding` from `-1` at the same time. But some checkpoints inside `StagesDeCoding` judge only `RateReached`, without looking at `BitPlaneStopDecoding` — and this combination (`RateReached==TRUE` and `BitPlaneStopDecoding==-1`) can never arise given the program's overall invariant. Deliberately constructing this contradictory state externally via `gdb` and calling directly did confirm the checkpoint is reached, but this is an artificial state that never occurs in actual decoding — following the standard already established with `AdjustOutput.c` ("respect the program's actual invariants"), it's reasonable to judge this dead code.
4. **The "two consecutive independent conditions' AND is re-checked separately, one side at a time, later" pattern** (`BPEBlockCoding.c`, 2 lines): the `continue` inside `BlockScanEncode` is judged on the AND of 2 conditions, `(A>=BitPlane) && (B>=BitPlane)`, but a bit later there's a branch that `goto`s on `A>=BitPlane` alone, and further along after that, a `continue` on `B>=BitPlane` alone. Reaching both of these latter 2 branches requires both `A>=BitPlane` and `B>=BitPlane` to be true — exactly identical to the first AND condition, which should already have been handled by the first `continue` — a contradiction, so the later `continue` is unreachable.

These proofs brought `StagesCodingGaggles.c`, `DC_EnDeCoding.c`, and `PatternCoding.c` to 100% effective coverage. The single remaining line each in `AC_BitPlaneCoding.c` and `BPEBlockCoding.c` were also proven confirmed dead code via additional control-flow analysis (confirming that `BlockScanEncode`, which `ACBpeEncoding` calls, never calls any output function at all; tracing `BPEBlockCoding.c`'s symbol-slot `si` consumption order — details in each file's description below), bringing all 15 core files to 100% effective coverage.

### Final results

| Target | Line coverage | Branch coverage (both directions) |
|---|---|---|
| All 15 core files combined | **95.14%** (6968/7324 lines) | **86.79%** (5833/6721, up from 86.77%) |
| `AdjustOutput.c` alone | 99.55% (unchanged) | 87.15% (unchanged) |
| `AC_BitPlaneCoding.c` alone | **86.48%** (211/244 lines, up from 86.07%) | **82.81%** (159/192, up from 80.73%) |
| `DC_EnDeCoding.c` alone | **93.86%** (321/342 lines, up from 93.27%) | **87.80%** (216/246, up from 86.18%) |
| `PatternCoding.c` alone | **96.59%** (368/381 lines, up from 96.06%) | **85.77%** (235/274, up from 85.40% — newly covered line 328's false direction in §4's "Branch reachability breakdown" item 8's additional round) |
| `StagesCodingGaggles.c` alone | **92.80%** (541/583 lines, up from 88.85%) | **87.06%** (491/564, up from 83.69%) |
| `BPEBlockCoding.c` alone | 94.86% (unchanged, 314/331 lines) | **86.27%** (333/386, up from 86.27%) |
| `bitsIO.c` alone | **77.38%** (65/84 lines, up from 75.00%, a side effect) | **76.19%** (32/42, up from 73.81%) |
| `bpe_encoder.c` alone | **76.47%** (156/204 lines, up from 75.98%, a side effect) | **71.43%** (90/126, up from 70.63%) |
| `bpe_decoder.c` alone | 87.87% (unchanged) | 79.76% (unchanged) |
| `header.c` alone | 78.38% (unchanged, dead code already confirmed) | 55.56% (unchanged) |

`AdjustOutput.c` had already reached 100% effective coverage via §3.5/§3.6's direct calls and value diversification (unchanged). This round's residual-gap resolution (previous section) brought `StagesCodingGaggles.c`, `DC_EnDeCoding.c`, and `PatternCoding.c` newly to 100% effective coverage, and `AC_BitPlaneCoding.c`/`BPEBlockCoding.c`'s raw line coverage also rose to 86.48%/94.86% respectively, with their last remaining line each also proven confirmed dead code via additional control-flow analysis — bringing all 15 core files to 100% effective coverage. `bitsIO.c`/`bpe_encoder.c` rose slightly as a side effect of this round's newly added `dc_qprime_lo_64` (a pixel pattern — 16-bit, low background + an isolated bump — no other case had) — exactly which lines isn't tracked, but as before, both files' remaining lines all fall within confirmed dead code (100% effective coverage maintained). `bpe_decoder.c`/`header.c` are outside the scope of either harness, so unchanged.

Per-file breakdown:

| File | Lines | Branches (both directions) |
|---|---|---|
| AdjustOutput.c | 99.55% | 87.15% |
| CoeffGroup.c | 100.00% | 100.00% |
| lifting_97f.c | 96.47% | 90.48% |
| PatternCoding.c | 96.59% | 85.77% |
| lifting_97M.c | 96.03% | 92.42% |
| ricecoding.c | 95.15% | 90.52% |
| BPEBlockCoding.c | 94.86% | 86.27% |
| DC_EnDeCoding.c | 93.86% | 87.80% |
| StagesCodingGaggles.c | 92.80% | 87.06% |
| bpe_decoder.c | 87.87% | 79.76% |
| AC_BitPlaneCoding.c | 86.48% | 82.81% |
| waveletbpe.c | 82.32% | 88.19% |
| header.c | 78.38% | 55.56% |
| bpe_encoder.c | 76.47% | 71.43% |
| bitsIO.c | 77.38% | 76.19% |

### Reachability breakdown ("effective coverage" excluding the dead-code rate)

Raw line-coverage numbers alone don't tell you whether "this can still be pushed higher." Separating **confirmed dead code/error paths** (structurally unreachable from the CLI, or error handling deliberately left out of scope) — as confirmed by the investigation so far — from **remaining reachable gaps** (lines that might still be hit with the right timing or value combination) gives "how much of the reachable lines are covered" = effective coverage.

| File | Total lines | Executed | Unexecuted | Of which confirmed dead/out of scope | Of which reachable gap | Effective coverage (executed/(total − dead)) |
|---|---|---|---|---|---|---|
| CoeffGroup.c | 282 | 282 | 0 | 0 | 0 | **100.00%** |
| header.c | 259 | 203 | 56 | 56 | 0 | **100.00%** |
| bpe_decoder.c | 272 | 239 | 33 | 33 | 0 | **100.00%** |
| bitsIO.c | 84 | 65 | 19 | 19 | 0 | **100.00%** |
| bpe_encoder.c | 204 | 156 | 48 | 48 | 0 | **100.00%** |
| ricecoding.c | 371 | 353 | 18 | 18 | 0 | **100.00%** |
| lifting_97f.c | 85 | 82 | 3 | 3 | 0 | **100.00%** |
| lifting_97M.c | 126 | 121 | 5 | 5 | 0 | **100.00%** |
| DC_EnDeCoding.c | 342 | 321 | 21 | 21 | 0 | **100.00%** |
| PatternCoding.c | 381 | 368 | 13 | 13 | 0 | **100.00%** |
| AC_BitPlaneCoding.c | 244 | 211 | 33 | 33 | 0 | **100.00%** |
| waveletbpe.c | 181 | 149 | 32 | 32 | 0 | **100.00%** |
| BPEBlockCoding.c | 331 | 314 | 17 | 17 | 0 | **100.00%** |
| AdjustOutput.c | 3579 | 3563 | 16 | 16 | 0 | **100.00%** |
| StagesCodingGaggles.c | 583 | 541 | 42 | 42 | 0 | **100.00%** |
| **Total** | **7324** | **6968** | **356** | **356** | **0** | **100.00%** (6968 / (7324−356) = 6968/6968) |

**How to read this**: raw coverage is 95.14%, but excluding 356 lines of confirmed dead code (4.9% of the total), **100.00%** of the 6968 "reachable" lines are covered. The last 2 lines that had remained unexplained (`AC_BitPlaneCoding.c`'s early return for `SegmentFull` inside `ACBpeEncoding`, `BPEBlockCoding.c`'s `si++` bookkeeping) were also proven confirmed dead code — not by reproducing them via direct calls, but through control-flow analysis (confirming `BlockScanEncode` never calls `BitsOutput` at all and so can't modify `SegmentFull`; tracing `si`'s consumption order all the way back to `BitPlaneSymbolReset`'s calling convention) — see each file's detailed description below for the specific investigation. Across all 15 core files, not a single line remains unreached for any reason other than "testing effort genuinely can't reach it."

Of `BPEBlockCoding.c`'s remaining 5.14% (17 lines), 16 turned out to be confirmed dead code. The largest block (13 lines, 357–376) is caused by the loop `for(i=0;i<4;i++) if((TypeC << (1<<(3-i))) != 1) { p=0; break; }`, which judges whether `TypeCi[k]`'s 4 child elements have all been hit — `TypeC` is a 4-bit field (0–15), and once promoted to `int`, at `i=0` (shift amount `1<<3=8`), `TypeC<<8` can only ever be 0 or a multiple of 256, **which can mathematically never equal `1`**. That is, regardless of the actual data, this loop always sets `p=0;break;` on its first iteration (`i=0`), so `p==1` (all hit, the `else` branch that processes only refinement bits) is structurally unreachable — this looks like a typo where `&` should have been written instead of `<<`, but this is how the reference implementation actually behaves, and the Rust side follows suit, preserving compatibility (confirmed by §2.1's full PASS). Together with the remaining 1 line (`ErrorMsg(BPE_BLOCKSCAN_CODING_ERROR)`, existing defensive code), that's 14 confirmed dead-code lines.

Furthermore, lines 149 and 238 (`continue;`, on a branch involving deep bitplanes with `CustomWtHL1_2bits` etc.), which had been left "unexplained" at the time, were proven confirmed dead code by scrutinizing `BlockScanEncode`'s control flow: these 2 lines sit **a bit after** a `continue` gated on the AND of 2 conditions, `if(A>=BitPlane && B>=BitPlane){continue;}`, with an `A`-only `goto CHECK_GC;` in between, leading to a `B`-only `continue;` (lines 149, 238) further along. Reaching this `B`-only `continue` requires both `A>=BitPlane` (for the `goto`) and `B>=BitPlane` (for the `continue` condition) to be true — exactly identical to the first `A&&B` condition, which should already have been handled by the first `continue` — a contradiction, so the later `continue` is structurally unreachable. This holds true regardless of CustomWt's actual value (fixed anyway, since `CustomWtFlag` is always FALSE) — it's a pure logical redundancy, a variant of the self-contained redundant-guard pattern found in `AdjustOutput.c`.

The remaining 1 line that was originally "unexplained" (line 181, `si++;` — a symbol-index update for when `TranB` has already been found) was also proven confirmed dead code by scrutinizing `si`'s consumption order. This line is reached in either of 2 states: "(A) right after the `TypeP` scan (lines 49–97) records a new hit and increments `si`" or "(B) `TypeP` was already fully discovered and `si` is unchanged" — but the immediately preceding lines 99–100 (`if(SymbolsBlock[si].sym_len!=0)si++;`) have already checked **exactly the same condition** and advanced `si` to the appropriate position: in case (A), it's already advanced to a fresh (`sym_len==0`) slot; in case (B), the slot `si` points to was explicitly reset (`sym_len==0`) by `BitPlaneSymbolReset` on a previous bitplane by `StagesEnCodingGaggles1/2/3`. That is, by the time line 181 is reached, `SymbolsBlock[si].sym_len` is always 0 — the same pattern as `AC_BitPlaneCoding.c`'s `ACBpeEncoding` (below), "the immediately preceding check has already consumed the same condition." This brought `BPEBlockCoding.c` to 100% effective coverage.

### Confirming `header.c`, `bpe_decoder.c`, the AC/DC side, `bitsIO.c`, and `StagesCodingGaggles.c`'s remaining uncovered lines as dead code, error paths, or hard-to-reach cutoff timing

Reading `gcov`'s annotated output directly and checking the surrounding code of the lines that never execute found the following aren't cases testing can't reach — rather, they are **code the C reference implementation itself can never structurally reach from the CLI (or an error path deliberately left out of scope, or a branch that's theoretically reachable but not something a black-box test can realistically target given the timing it depends on)**.

`header.c`:

- `CodewordLength_2Bits` (values 1/2/3, codeword lengths 16/24/32-bit): always hardcoded to `0` by `HeaderInilization`, never changed by any CLI option.
- `CustomWtFlag` (user-defined wavelet weights): likewise always hardcoded to `FALSE`.
- The `UseFill = TRUE` branch: the check `if (SegByteLimit_27Bits == 0) UseFill=FALSE; else UseFill=TRUE;` runs inside `HeaderInilization`, but at this point `SegByteLimit_27Bits` is still its initial value `0`, before being computed from the rate (`-r`) — due to the reference implementation's own initialization ordering, this branch is structurally unreachable.

`bpe_decoder.c` (the remaining 12.13%, confirmed all 33 lines unreachable — no room for improvement with additional testing):

- **The `TransposeImg == TRANSPOSE` branch** (inside `DecodingOutputInteger`/`DecodingOutputFloating`, 18 lines total): the encode side's `HeaderInilization` (header.c:56) always hardcodes `TransposeImg` to `NOTRANSPOSE`, and there's no way to change it to `TRANSPOSE` from any CLI option (`main.c` has no such option). The decode side itself correctly reads it from the header (header.c:308), but this reference implementation's own encoder structurally never writes `TRANSPOSE`.
- **The whole `TempCoeffOutput` function** (9 lines): its only call site inside `DecoderEngine` is already commented out (`//	TempCoeffOutput(...)`) — a debug dump function that's now fully disabled.
- **`SegmentBufferFlushDecoder`'s fill skip-over loop** (3 lines, gated on `UseFill == TRUE`): as noted for `header.c` above, since `UseFill` is always written out as `FALSE` on the encode side, this decode-side loop is likewise always unreachable under the reference implementation's actual operation.
- **3 spots of `ErrorMsg(BPE_FILE_ERROR)`** (the `fopen` failure paths for the input/output files): error paths kept out of scope for the normal test matrix. `ErrorMsg` `exit()`s, so these are theoretically reachable with additional testing, but that falls outside this report's focus on C/Rust output byte-compatibility, so it wasn't pursued.

`AC_BitPlaneCoding.c` (100% effective coverage), `DC_EnDeCoding.c` (100% effective coverage) — since both files share the same structure (parallel implementations on the DC/AC sides), the same kinds of unreachable patterns appear in both:

- **The `OptDCSelect`/`OptACSelect == FALSE` "heuristic" sign-selection branch** (inside `ACGaggleEncoding`/`DCGaggleEncoding`, 16 lines each): `HeaderInilization` (header.c:47-48) always hardcodes both flags to `TRUE` (brute-force optimal selection), with no way to change either to `FALSE` from any CLI option — a configuration item the reference implementation's own CLI doesn't expose, the same kind as `header.c`'s `CodewordLength_2Bits`/`CustomWtFlag`.
- **The AC-side `N > 5` `ErrorMsg(BPE_DATA_ERROR)` branch** (`ACDepthEncoder`/`ACDepthDecoder`, 4 lines total): since `N` is computed as the bit length of the 5-bit header field `BitDepthAC_5Bits` (max 31), `N` can mathematically never exceed 5 (31 in binary is 5 digits). This branch is defensive code by design, structurally unreachable from any valid input.
- **`CheckUsefill`** (AC_BitPlaneCoding.c, 10 lines total): exactly the same structure and cause as `bpe_decoder.c`'s `SegmentBufferFlushDecoder` fill skip-over loop (since `UseFill` is always written out as `FALSE` on the encode side, the corresponding decode-side skip-over logic is never reached).
- **The AC-side early return for `BitPlaneStop_5Bits == BitPlane && Part2Flag == TRUE` inside `ACBpeEncoding`** (2 lines total): `BitPlaneStop_5Bits` never changes from its `header.c`-initialized `0` on the encode side (no CLI option changes it), while the bitplane loop this check sits in only runs when `BitPlane > 0`, so `BitPlane` is never 0 — `BitPlaneStop_5Bits(0) == BitPlane` is always false, unreachable regardless of `Part2Flag`. `AC_BitPlaneCoding.c`'s 32 lines of confirmed dead code break down as 16 heuristic-branch lines, 4 `N>5` lines, 10 `CheckUsefill` lines, and 2 lines from this item, totaling 32.
- **The DC-side `N > 10` `ErrorMsg(BPE_DATA_ERROR)` branch** (`DCEntropyDecoder`, a different cause than the AC side): the DC side's `N` (`= max(BitDepthDC_5Bits − QuantizationFactorQ, 1)`) has no bit-width upper bound the way the AC side does, but `QuantizationFactorQ_prime`'s selection formula (DC_EnDeCoding.c:530-537) is designed to set `Q'` to `BitDepthDC−3` or `BitDepthDC−10` depending on `BitDepthDC`'s value, structurally guaranteeing `N` always stays at or below 10 (confirmed empirically too — even for an image where `BitDepthDC_5Bits` reaches 19, `N` never exceeded 10). As long as this guarantee holds, this branch is unreachable. `DCEntropyEncoder`'s side (lines 206-218) doesn't even have this `else` clause — processing continues with `Max_k`/`ID_Length` left at their initial value of 0 — but for the same reason (`N>10` is unreachable), this causes no actual harm (this defensive `ErrorMsg` plus a similar guard near the top of the file, 2 lines total, along with the 16 lines of `OptDCSelect==FALSE` heuristic branches, account for `DC_EnDeCoding.c`'s 19 lines of confirmed dead code).
- **The DC-side `DCEntropyDecoder`'s `if((k=DCEntropyDecoder(...))!=BPE_OK) return k;`** (`return k;`, 1 line): checking `DCEntropyDecoder`'s source shows its only return value is the single `return BPE_OK;` at the function's end; the only branch for `N>10` merely calls `ErrorMsg`, and since `ErrorMsg` itself `exit()`s, control terminates there and no code path returning anything other than `BPE_OK` exists — this caller-side check is structurally unable to become true. `DC_EnDeCoding.c`'s 20 lines of confirmed dead code are the 16 heuristic lines plus 3 `N>10`-family lines plus this item's 1 line, totaling 20.
- **The DC-side `QuantizationFactorQ_prime = BitDepthDC - 3`** (2 lines total, both encode and decode): the condition `BitDepthDC > 3` and `BitDepthDC - (1+(BitDepthAC>>1)) <= 1` couldn't be satisfied with images like flat images or checkerboards, where AC/DC depth tend to move together, always fixing their difference at a constant value. Measuring many patterns empirically with a diagnostic build found that a pattern of **a low, constant background value (2) with a single isolated bump of amplitude 50 in one pixel per 8×8 block** (`dc_qprime_lo_64`) decouples AC/DC depth, covering this branch (see the residual-gap-resolution section above). This brought `DC_EnDeCoding.c` to 100% effective coverage.
- **The AC-side early returns for `SegmentFull`/`RateReached` inside `ACBpeEncoding`/`ACGaggleDecoding`** (2 lines total): of these, the simple, unguarded `if(RateReached==TRUE)return;` inside `ACGaggleDecoding` was immediately covered just by sweeping the bit budget from 0 to 40 via a direct call (described above). The remaining `if(SegmentFull==TRUE)return;` inside `ACBpeEncoding` (the middle of 3 similar checks right around a `BlockScanEncode` call) couldn't be made to reproduce the intended cutoff timing via a direct call plus `SegByteLimit_27Bits` sweep, but `grep`ping all of `BlockScanEncode`'s source (BPEBlockCoding.c) found not a single call to `BitsOutput` — that is, `BlockScanEncode` is a pure scanning function with no way whatsoever to modify `PtrCoding->SegmentFull`. So if the check right before this call (`if(SegmentFull==TRUE)return;`) wasn't true, the identical check right after it can never be true either — the two are mathematically performing the exact same judgment twice, and the latter is structurally unreachable. This brought `AC_BitPlaneCoding.c` to 100% effective coverage.

`bitsIO.c` (the remaining 19 lines, all confirmed unreachable — no room for improvement with additional testing. Used to be 21 lines, but the `dc_qprime_lo_64` test added in the later residual-gap-resolution round happened to cover 2 lines of "segment-trailing fill-byte skip-over loop" as a side effect, bringing it down to 19):

- **`OutputCodeWord`'s `CodeWord_Length` 16/24/32-bit branches** (13 lines total): the same cause as `header.c`'s `CodewordLength_2Bits`. Since `CodewordLength_2Bits` is always hardcoded to `0`, `Bits->CodeWord_Length` can only ever be 8, structurally unreachable for the 16/24/32-bit codeword output paths.
- **`BitsRead`'s `RateReached == TRUE` early return** (2 lines): `grep`ping all of the source found `RateReached = TRUE` and `SegmentFull = TRUE` are always set at the exact same spot in `bitsIO.c` (lines 166–183), and always reset together too (`bpe_decoder.c`'s `PtrCoding->SegmentFull = FALSE; PtrCoding->RateReached = FALSE;` are 2 adjacent lines). So whenever `RateReached == TRUE`, `SegmentFull` is also always `TRUE`, and the `SegmentFull == TRUE` check just before it (line 138) already returns to the caller first, so there's no path that reaches this branch.
- **`BitsOutput`'s `length > 32` branch** (4 lines): checking every `BitsOutput` call site found the fixed-length argument maxes out at 27 bits (`SegByteLimit_27Bits`), and the variable-length argument (the Rice-like "already-coded leading part" length, `(MappedDC/MappedAC >> min_k) + 1`) — given the empirical range of coefficients this reference implementation supports, determined by its maximum pixel bit depth of 16 (this report's 16-bit maximum-amplitude checkerboard test also topped out with `BitDepthDC_5Bits` at 19, see §4) — practically never exceeds 32. There is, in theory, a separate rough edge in the reference implementation, `DCEntropyEncoder`'s N>10 fallback (`Max_k`/`ID_Length` left at their initial value of 0, lines 208-218), but even this rarer situation doesn't increase the amount of value output and doesn't push past 32 bits. Also confirmed as circumstantial evidence: this was never hit once across over 100 test cases (including the 16-bit maximum-amplitude checkerboard).

`PatternCoding.c` (100% effective coverage, 13 of 381 lines confirmed dead code): `case 0: return;` (the early return for sym_len=0, 1 line) and each of 4 `switch` statements' `default: ErrorMsg(BPE_PATTERNING_CODING_ERROR)` (8 lines total) are all defensive code premised on pattern mapping's sym_len/type values always staying within their valid range. The remaining 4 lines (inside `StagesDeCoding`, 2 lines at each of 2 sites) — the only spot in the entire source that sets `RateReached` is a single spot in `bitsIO.c`, confirmed to always rewrite `BitPlaneStopDecoding` from `-1` at the same time — yet these 2 spots judge only `RateReached` without looking at `BitPlaneStopDecoding`, and the combination "`RateReached==TRUE` and `BitPlaneStopDecoding==-1`" can never arise given the program's overall invariant. Note that the same kind of checkpoint inside `RefBitsDe` (2 lines, an end-of-line recording step with no `!LocationFind` guard) was actually covered by reproducing, via a direct call, "the state where a previous bitplane already fixed the stopping position" (described above).

`StagesCodingGaggles.c` (100% effective coverage, 42 of 583 lines confirmed dead code. Previously 54 lines had remained as a residual gap of stopping-position checkpoints, but the direct-call harness covered 23 of them, and control-flow analysis proved the remaining 31 all confirmed dead code):

- **The early return for `StageStop_2Bits == 00`/`==01`** (`StagesEnCodingGaggles1`/`3`, 2 lines total): `header.c`'s `StageStop_2Bits` is always hardcoded to `3` by `HeaderInilization` (the same as `CodewordLength_2Bits`/`TransposeImg`, never changed from any CLI option), so it never becomes `00`/`01`.
- **`StagesEnCodingGaggles1`'s `sym_len==4` branch and the "invalid length" `default: ErrorMsg`** (4 lines total): this switch statement only enumerates `case 1: case 2: case 3:`, so `sym_len==4` falls to the `default:` side (`ErrorMsg`) in the first place — that is, by the time control enters the switch, `sym_len` is already limited to 1–3, so the `sym_len==4` check nested inside it can never be reached at runtime. `StagesEnCodingGaggles2`/`3`'s equivalent `ErrorMsg`s (2 lines total) are the same kind of defensive code, given the structural child-element count (4) of the symbol types (`TYPE_CI`/`TYPE_HIJ`, etc.) they handle can't be exceeded.
- **`StagesDeCodingGaggles3`'s `continue` for `flag==FALSE`** (1 line): since `CustomWtHH1_2bits` is always hardcoded to `0` on the encode side (`header.c` — unlike the other CustomWt-family fields, this band alone has weight 0), `0 >= BitPlane` can never hold for `BitPlane` (which is at least 1), so the corresponding `k==2` iteration always sets `flag=TRUE`, and the case where all 3 iterations `continue` (leaving `flag` `FALSE`) structurally cannot occur.
- **The no-op scaffolding limited to `SegmentCount_8Bits==28 && BitPlane==13 && BlockSeq==10`** (`BlockSeq = BlockSeq;`, 2 lines): a self-assignment that performs no meaningful processing, apparently a leftover debugger-breakpoint hook from development. One of the 2 lines (the condition expression itself) happened to get covered in the process of building the direct-call harness. The other (the self-assignment body, requiring `BlockSeq==10`) remains uncovered, but since it's a meaningless self-assignment, there's nothing meaningful to verify by covering it.
- **The remaining 30 lines of `SegmentFull`/`RateReached` early returns (with the `!LocationFind` guard)**: scrutinizing `StagesDeCodingGaggles1/2/3`'s source confirmed that **every single bit read (every `BitsRead` call) is immediately followed by a checkpoint dedicated to that specific read**. `RateReached` can only ever change once per bit read, and `BitPlaneStopDecoding` updates at the same time (bitsIO.c). So the moment `RateReached` newly becomes true, it's always caught by the nearest (= narrowest-condition) checkpoint right after it, and once `LocationFind` becomes true, broader-scope checkpoints further along — like an unconditional "end of loop" check sitting right after a point where only one side of an if/else did a read and the other side, with no read, merges back in — become structurally unreachable. Forcing the checkpoint condition true externally via `gdb` confirmed control always actually returns at the nearest checkpoint.
- **The remaining 4 lines of coordinate-selection `else` branches**: some checkpoints record the stopping coordinate via a 3-way branch, `if(hit_history&0x4==0){...} else if(hit_history&0x2==0){...} else{...}`, but reaching this `else` (both bits already set) contradicts the precondition for entering that checkpoint at all ("there are still 2 or more elements not yet found") — if both bits are set, at most 1 element remains undiscovered, which can't satisfy the entry condition.

None of these can be reproduced no matter how the test cases are engineered (short of manually tampering with the header), fall outside this report's verification scope, or are already-proven unreachable dead code — so they're excluded from future work. `AC_BitPlaneCoding.c`'s `SegmentFull` early return inside `ACBpeEncoding`, and `BPEBlockCoding.c`'s `si++`, have also been proven confirmed dead code via additional control-flow analysis (see each file's description above for details).

### Branch reachability breakdown ("effective branch coverage")

The reachability breakdown so far (the first half of §4) was at the **line** granularity. Since line coverage only sees "was this line executed," a line like `if(coeff > 0) ... else if(coeff < 0) ...` counts as 100% executed as a line even if `coeff`'s sign only ever swings one way — and in fact, this project's DPCM sign bug and `AdjustOutPut` sign bug both manifested in exactly this form: "the line executes, but only one direction of the branch is ever taken." So it's worth evaluating reachability per branch (each direction of a condition), independent of line coverage.

**Method**: among the branch entries `gcov -b` outputs (up to 2 entries per condition expression, taken/not-taken), the unexecuted ones (`taken 0%` or `never executed`) were classified in the following order:

1. **The line itself is already confirmed dead** (already confirmed in the line-level breakdown, first half of §4): if a line never executes, its branch entries are automatically unexecuted too — no new proof needed.
2. **Matches a known dead-code pattern**: CLI-fixed fields like `CustomWtFlag`/`CustomWtH?[HL]?[123]_2bits`, the `N<=5`/`N<=10` family, the `RateReached`/`LocationFind` interception chain, the `TypeC<<(1<<...)` typo, the proof that `BlockScanEncode` never calls `BitsOutput`, the proof that `DCEntropyDecoder` always returns `BPE_OK` — plus the following newly confirmed families this round:
   - **Sign fixed by type**: `diff_AC[i]` is derived from `MappedAC`, which comes from `WORD16` (unsigned), so its sign is structurally fixed (`AC_BitPlaneCoding.c`); `BitMaxAC` is `UCHAR8` (unsigned), so `< 0` is impossible.
   - **A duplicate check of a guard already established on the immediately preceding line**: `bpe_decoder.c`'s `EngImgFlg != TRUE` (already `break`-ed if `EngImgFlg==TRUE` on the preceding line), `bitsIO.c`'s `SegmentFull != TRUE` (already `return`-ed if `SegmentFull==TRUE` just before), `header.c`'s `SegByteLimit_27Bits==0` (already assigned 0 by itself 2 lines earlier).
   - **Error paths (an extension of the existing `fopen(` convention)**: `stat()` failure (`bpe_encoder.c`'s `res != 0`), `ImageSize`/`ImageRead`'s error branches, `fopen` failure (`infile == NULL`) — all applications of the existing "abnormal paths are out of scope" policy, stemming from the tested input file always existing and being readable.
   - **The false direction of the `for(;BlockCounter<TotalBlocks;)` loop condition** (`bpe_encoder.c`): the loop body always exits via `break` when `EngImgFlg==TRUE`, and `EngImgFlg` is constructed to always become TRUE when "the next segment will use up (or overrun) all blocks" — so there's no path where the loop condition itself becomes false and the loop ends naturally — a structural proof that `break` is the only exit.
   - **Encode/decode round-trip consistency**: the `for` loop inside `PatternCoding.c`'s `DeMappingPattern` (the reverse lookup of `bit2_pattern`/`bit3_pattern`/`bit4_pattern_TypeCi`) can never exhaust the table without finding a match and fall out of the loop, given that the encode side's `PatternMapping` wrote the value using this same table. Likewise, `PatternMapping`'s `switch(sym_len)`'s `case 0` and `default` are unreachable, given the structural guarantee that no sym_len==0 symbol is ever generated and sym_len never exceeds 4.
   - **A guarantee that this encoder's own output satisfies the spec**: `header.c`'s `S_20Bits < 16` is unreachable on the side that isn't satisfied, given this encoder is constructed to always compute a spec-conforming value.
   - `BitsPerPixel != 0` (`header.c`) is based on `main.c` initializing it to a default of 1, and this verification's CLI usage never having a test case that explicitly specifies 0 (the same kind of pattern as the other "value fixed by the CLI" family).
3. **`AdjustOutput.c`'s sign guard tautology (486 branches)**: `AdjustOutPut` has 12 blocks (confirmed via `grep`: pairs for stoppedstage 1/2/3's integer and float representations each, plus 3 pairs equivalent to stoppedstage 4), each with a guard at the top of the loop that excludes 0 in the form `for(m..)for(n..){ if((m==0&&n==0)||(value==0)) continue; ...}`. Since 0 is already excluded by this guard, wherever inside the block a sign branch of the form `if(value>0) ... else if(value<0) ...` sits, the `else if` side, once reached, is always true — "neither 0 nor positive" — so the `else if`'s own false direction (`value>=0` yet still reaching here) is mathematically unreachable. All 12 guard positions were confirmed directly in the source, and this invariant was verified to hold across each one's entire scope, but not every individual `else if(value<0)` occurrence — which appears 652 times in the text within scope — was re-verified one by one: this is positioned as a structural confirmation of the mechanism's applicable range, not a claim of the same rigor as the line-level individual proofs.
4. **Implicit defaults where a `switch` statement has mathematically exhausted the value domain (16 branches)**: for something like `switch(BitLength)`, where the source value is limited to a fixed-width bitfield or bitmask's value domain, and every pattern in that domain has an explicit `case`, `gcov` still emits one branch for the implicit "doesn't match any `case`" — since the value domain is mathematically exhausted, this branch is unreachable. Detected structurally (not by text pattern) as "a `switch` line where only the last branch is unexecuted, with 2 or more total branches."
5. **Elimination across a `case`/`if` chain (domain elimination, newly added)**: `CodingOptions`'s `sym_len==4` (sym_len's domain is only {1,2,3,4}, 1 is already `continue`d, and 2 and 3 are already ruled out by the immediately preceding `if`/`else if`); `StagesEnCodingGaggles1`'s `sym_len==3` (the domain is limited to {1,2,3} by the outer `switch(sym_len){case 1:2:3:}`, then further eliminated); `StagesEnCodingGaggles2/3`'s `sym_len==4` (the `else` side is `ErrorMsg`, confirmed unexecuted via gcov's execution count `#####` — for this `type` set, only 2/3/4 are valid values for `sym_len`). `PatternMapping`/`DeMappingPattern`'s `case 4` (a 4th implicit branch for when `type` is none of `TYPE_CI`/`TRAN_HI`/`TYPE_HIJ` — sym_len==4 symbols are only ever generated for these 3 `type` values).
6. **Structural proof of a tie-breaking chain via transitivity (newly added)**: for `CodingOptions`'s Rice-split option selection (the `<=` chains for `bitsCounter_3Bits`/`bitsCounter_4Bits`), some of the later `else if` clauses were individually proven to be mathematically forced by the very fact that the earlier clauses failed. For example, 3-bit's 2nd clause `(c0<=c1)&&(c0<=c2)`'s `c0<=c2` side: once the 1st clause `(c2<=c0)&&(c2<=c1)` fails (whether because `c2>c0` or `c2>c1`), combining that with `c0<=c1` always forces `c0<=c2` — any attempted counterexample runs into a contradiction. The same kind of transitivity was confirmed for 3-bit's 3rd clause (the `c1<=c2` side) and 4-bit's 2nd clause (the `c0<=c3` side).
7. **`RefBitsDe`'s cutoff-position recorder (newly added)**: the remaining 2 spots involving `!PtrCoding->DecodingStopLocations.LocationFind` — this check is only ever reached when "`RateReached` transitioned from false to true *during this call*" (having passed the early `return` on `SegmentFull||RateReached` at the top of the range means it must have been false at the start of the call). `LocationFind` can only already be true from some *other*, earlier call — but that call must also have made `RateReached` true, which would then hit that same early-`return` guard on the *next* call and end the function before reaching this depth. That is, "reaching here while `LocationFind` is already true" is structurally impossible.
8. **The remaining 5 branches of the 4-bit Rice-split option selection → all resolved (1 case reached via an added test, 4 proven structurally unreachable)**: for `CodingOptions`'s 4-bit-side `else if` chain (judging in order whether `bitsCounter_4Bits[0..3]` is the minimum, lines 316/322/328/334, corresponding to Option 3/0/1/2 respectively), 5 branches at lines 328, 330, 334, 335, 336 had long remained unresolved — "no counterexample could be deliberately constructed from real data, and no mathematical proof of unreachability existed either." Digging further into this `else if` chain's structure resolved all of them, as follows.
   - **Line 328 (the false direction of `bitsCounter_4Bits[1] <= bitsCounter_4Bits[0]`): turned out reachable, resolved by adding a test**. Mixing 4 instances of TYPE_CI's pattern value 0 and 3 of pattern value 6 (via `bit4_pattern_TypeCi`'s reverse lookup, `sym_val` = 8×4, 3×3) yields `bitsCounter_4Bits` = `(25,26,24,28)` — line 316 (`c3` minimal) and line 322 (`c0` minimal) both fail, yet `c1(26) > c0(25)`, making line 328's first condition false — a direction no existing case had produced. Added this case to `verify/c_unit_tests/gen_codingoptions_coverage.c`, and confirmed empirically (via `gcov -b`, a standalone direct-call build of `PatternCoding.c`) that line 328's false direction flipped from 0% to executed.
   - **Line 330 (the false direction of `bitsCounter_4Bits[1] <= bitsCounter_4Bits[3]`): proven structurally unreachable**. Assuming lines 328–329 are true (`c1` is at or below `c0` and `c2`) while line 330 is false (`c1 > c3`) derives, by transitivity, `c3 < c1 <= c0` and `c3 < c1 <= c2`; combined with `c3 <= c1` (trivially true from the assumption), all 3 of line 316's conditions (`c3<=c1 && c3<=c0 && c3<=c2`) end up satisfied — but line 316 is evaluated before line 328, so by the time control reaches line 328, line 316 must already be false. Contradiction. So the state "lines 328–329 true and line 330 false" can never arise.
   - **Lines 334, 335, 336 (all 3 conditions judging whether `bitsCounter_4Bits[2]` is minimal): proven structurally unreachable**. For any 4 real numbers, "at least one of them is at or below the other 3" (i.e. is a minimum, possibly tied) is a tautology — once lines 316, 322, 328's conditions (whether `c3`/`c0`/`c1` is a minimum candidate, respectively) are all false and control reaches line 334's judgment, the remaining 4th option of this 4-way exhaustion, "`c2` is a minimum candidate" (line 334-336's entire conjunction), is automatically guaranteed true. Since the conjunction is true, each of lines 334, 335, 336's individual conditions must also be true — so this trio's false direction (the `taken 0%` side) can never occur for any input.

   Also, line 328's counterpart (line 329's false direction, `c1 <= c0` true but `c1 > c2`) was likewise unexecuted in the same standalone build, so a counterexample was sought and found reachable with pattern value 0×1, 6×1 (`sym_val={8,3}`). However, line 329 wasn't originally part of this "5-branch" list (328, 330, 334, 335, 336) — it was implicitly included in the existing tally (241 branches executed) (since this report's overall tally combines the pipeline test and other generators, it's likely this had already been hit by some other test even though this direct-call generator alone couldn't reach it) — judged that adding a new test case for it wasn't necessary.

| File | Total branches | Executed | Confirmed dead | Unclassified remainder | Effective branch coverage |
|---|---|---|---|---|---|
| CoeffGroup.c | 232 | 232 | 0 | 0 | **100.00%** |
| waveletbpe.c | 129 | 112 | 17 | 0 | **100.00%** |
| bitsIO.c | 48 | 32 | 16 | 0 | **100.00%** |
| bpe_decoder.c | 176 | 141 | 35 | 0 | **100.00%** |
| bpe_encoder.c | 137 | 89 | 48 | 0 | **100.00%** |
| header.c | 75 | 30 | 45 | 0 | **100.00%** |
| lifting_97M.c | 69 | 61 | 8 | 0 | **100.00%** |
| lifting_97f.c | 45 | 38 | 7 | 0 | **100.00%** |
| ricecoding.c | 242 | 210 | 32 | 0 | **100.00%** |
| AC_BitPlaneCoding.c | 195 | 161 | 34 | 0 | **100.00%** |
| AdjustOutput.c | 3970 | 3460 | 510 | 0 | **100.00%** |
| BPEBlockCoding.c | 387 | 335 | 52 | 0 | **100.00%** |
| DC_EnDeCoding.c | 250 | 215 | 35 | 0 | **100.00%** |
| StagesCodingGaggles.c | 569 | 487 | 82 | 0 | **100.00%** |
| PatternCoding.c | 279 | 242 | 37 | 0 | **100.00%** |
| **Total** | **6803** | **5845** | **958** | **0** | **100.00%** (5845 / (6803−958) = 5845/5845) |

**How to read this**: excluding 958 confirmed-dead branches (244 lines themselves confirmed dead + 208 matching a known pattern + 486 of `AdjustOutput.c`'s sign guard + 16 `switch` implicit defaults + 4 of `PatternCoding.c`'s 4-bit selection chain proven structurally unreachable, totaling 958), **100.00%** of the 5845 "reachable" branches are covered. `PatternCoding.c` was the last remaining file after item 8's analysis above, bringing all 15 core files to 100% effective branch coverage.

The improvement from the previous measurement (99.25%, 44 remaining) mainly comes from the following:
- **`AdjustOutput.c`'s cutoff-position boundary (3→0)**: found the cause of why the existing harness directly calling `AdjustOutPut` couldn't reach the `X_LocationStopDecoding==1 && Y_LocationStopDecoding==0` branch by actually instrumenting the source with debug output — proved structurally that when `stoppedstage==1`, this special branch can only ever be reached when `Y_LocationStopDecoding==0` (in the "parent" quadrant where `m,n` only take 0 or 1, if `Y_Loc>=1`, a preceding `if` always consumes it first). Likewise, the `BitPlaneStopDecoding==0` check was resolved via the value-domain contract that a `char`-typed value only ever gets assigned `-1` (a sentinel, already excluded by an upper guard), `BitPlane-1` (>=0), or the literal `0`.
- **AC/DC-side `uncoded_flag` (5→0)**: added an all-1s bit pattern (`0xFF`) Rice-decode direct-call sweep to the existing `gen_residual_gaps_coverage.c`, producing the case where `min_k` makes all `ID_Length` digits 1 (the equivalent of the encoder's "uncoded" mode).
- **`BPEBlockCoding.c`'s bitplane-significance history (7→0)**: added a new direct-call generator for `BlockScanEncode` (`gen_blockscan_coverage.c`). Resolved with 2 amplitude/threshold combinations, calling `TranB` twice in a row against the same `BitPlaneBits` (reproducing the actual encoder's behavior where a previous bitplane's call carries its written `SymbolsBlock` value over to the next call). The remaining 2 lines (the `TypeCi` loop's `for` and `if(p==0)`) were structurally proven as a direct consequence of the existing "typo" pattern — `TypeCi.TypeC << (1<<(3-i))`, being an integer left shift, can never equal `1`.
- **`StagesCodingGaggles.c`'s bitplane-significance history (5→0)**: the remaining 2 branches of `TranD&0x2==0`/`TranGi&0x2==0` were resolved via a transitivity proof showing the preconditions for reaching them (`counter>=2`, and a higher bit already set by the preceding `if`) force the value into exactly 1 possibility. The remaining 3 (the `sym_len==3`/`==4` branches) were resolved with a new direct-call generator for `StagesEnCodingGaggles1/2/3` (`gen_stages_encode_coverage.c`), after fixing a case that initially crashed on `ErrorMsg` from not realizing `TYPE_P` symbols only go up to length 3 (only `case 1:2:3`, no `case 4`).
- **`PatternCoding.c`'s type branches and rate-cutoff chain (16→5)**: directly called `CodingOptions`/`PatternMapping`/`DeMappingPattern` with a new generator, `gen_codingoptions_coverage.c`, exhaustively sweeping `sym_len` and `sym_val` (working backward through `PatternMapping`'s table conversion so `sym_mapped_pattern` lands on the targeted value). `RefBitsDe`'s 8-branch rate-cutoff chain was initially targeted with an added test aimed at "`LocationFind` carrying over across multiple calls," which had no effect, but was resolved once item 7's structural proof above showed that direction was unreachable in the first place.

**Lessons learned in the process (recorded for future investigations of the same kind)**: (a) `gcov`'s branch label `branch N taken X% (fallthrough)`, on a line with a chain of `if`/`else if`s, refers to "that line's condition being true and falling through to the code right after it" (i.e. the `if` side is the `fallthrough`, not the `else` side) — this was mistaken at one point, leading to added tests written in the wrong direction that had to be redone. It's recommended to always corroborate with execution counts (`gcov -c`'s raw counts, or matching against the corresponding body line's execution count). (b) Repeatedly recompiling and relinking the same `.o` file with different content can corrupt the accumulated coverage data for the affected file via a `.gcda` checksum mismatch (this actually happened this round, recovered by rebuilding and re-running every harness from object generation, once, straight through) — when continuing measurement across multiple rounds, avoid partial rebuilds along the way, and always do the final confirmation with one clean, complete measurement run.

## 5. Overall assessment

- **For the ordinary encode/decode path (no rate limiting, or mild lossy compression), compatibility has been confirmed with high confidence.** Both the byte-compatibility and exhaustive function-verification tests PASS, and integer DWT shows 0 mismatches across the range tested.
- **Both function-level verification and the rate sweep independently found real bugs**: the 2 DC-side DPCM bugs were found via §2.2 (function level), and the inverse-DWT precision mismatch via §2.1's rate sweep. The premise that "the byte-compatibility test passing" doesn't mean "matches under every input and condition" was independently corroborated by 2 methods of a different nature.
- **The 1-ULP-level residual that used to exist in float DWT decoding has been root-caused and fully fixed** (§3.3). It was originally misdiagnosed as "an unresolvable residual difference due to a compiler difference between gcc and rustc," but it was actually a fixable implementation bug: bpe-rs's `inverse_lifting97f` failed to reproduce C's per-operator type-conversion rules (add in f32, then promote to f64). After the fix, 0 mismatches across both integer and float DWT (178/178 cases, 3500 fuzz-test cases).
- **Rate-limited mid-decode recovery (`AdjustOutPut`) broke past the full-pipeline test's limits via direct function calls, rising from 65.02%→96.90% lines** (§3.5, §4). The in-block stopping coordinates (`X`/`Y_LocationStopDecoding`, 64 combinations of 8×8) were almost entirely unreachable from the full-pipeline test, due to depending on the bitstream's remainder — switching to the technique of linking directly against the C reference implementation's object code and driving the function standalone (already proven with Rice/DPCM/pattern mapping) made it possible to directly exhaust all 1024 combinations of DWTType × stoppedstage × DC branch × X/Y coordinate, with 0 mismatches.
- **`AdjustOutPut`'s remaining 95 lines (found to be caused by a lack of value-sign variation) were pushed to 97.35%→99.55% lines by adding a sign-inversion/phase-shift `variant` dimension to the test values, expanding to 6144 combinations, and the last 16 lines were proven confirmed dead code (a redundant guard's `else` clause) via control-flow analysis** (§3.6, §4). `AdjustOutPut.c` ultimately reached 100% effective coverage (fully exhausted once dead code is excluded), and since this single function alone accounts for about 49% of the entire core algorithm's line count, it was a major factor pushing up the 15 core files' overall effective coverage (ultimately 100.00%, §4).
- **The 15 core files' overall line coverage reached 95.14% (86.79% branches), well above the originally targeted 90%.** This is the cumulative result of `AdjustOutPut`'s direct-call harness and value diversification, plus the direct-call harnesses for `StagesCodingGaggles.c`, the AC/DC side, and `PatternCoding.c` (described later) — the remaining gap sits at a near-ceiling level of just 1 line each in `AC_BitPlaneCoding.c` and `BPEBlockCoding.c`.
- **All 5 remaining branches in `PatternCoding.c`'s 4-bit Rice-split option-selection chain have been resolved (§4 "Branch reachability breakdown" item 8).** 1 branch (line 328's false direction) was resolved by newly discovering a concrete counterexample symbol-value combination and adding it as a test; the remaining 4 (lines 330, 334, 335, 336) were proven structurally unreachable via the tautology "of 4 real numbers, one is always at or below the other 3" plus transitivity. This brought all 15 core files to 100.00% effective coverage (both line and branch), leaving no "unproven" item remaining in this report.
- **Excluding confirmed dead code (356 lines, 4.9% of the total), "reachable code"-based effective coverage reaches 100.00%** (§4's reachability breakdown table). The raw figure of 95.14% alone can't tell you whether "the rest can still be filled in with more testing effort, or genuinely can't be" — so unexecuted lines were manually classified, file by file, into "confirmed dead code/error path" versus "reachable residual gap." Originally about 1.0% (70 lines) of genuine headroom remained, but applying the same "direct call + exhaustive sweep" technique used for `AdjustOutPut` to `StagesCodingGaggles.c`, the AC/DC side, and `PatternCoding.c` resolved almost all of it (actually covering it) or proved it confirmed dead code (verified via externally forcing state with `gdb`) — with the final 1 remaining line each in `AC_BitPlaneCoding.c` and `BPEBlockCoding.c` also proven confirmed dead code via control-flow analysis (confirming the former's callee never has any output function at all; tracing the latter's symbol-slot consumption order) — bringing the genuinely unexplained residual gap across all 15 core files to **zero**.
- **`StagesCodingGaggles.c` (the code that records the rate-limited decode's stopping position), following the segment-size-varying sweep (a previous round, 85.93%→88.85% lines), rose further to 88.85%→92.80% lines and 83.69%→87.06% branches by newly building a harness that directly calls and exhaustively sweeps the bit budget of `StagesDeCodingGaggles1/2/3`** (§4). The remaining 42 lines were all proven confirmed dead code via 2 patterns — the structural redundancy where a narrower checkpoint always intercepts first, or a coordinate-selection `else` branch that mathematically contradicts its own entry condition — reaching 100% effective coverage.
- **Discovered and fixed a measurement bug in the coverage-measurement method itself: a `gcov` `.gcda` merge inconsistency** (§4). Because shared `.c` files were being recompiled per binary, a later link failed to correctly merge the earlier run's `.gcda`, and every figure reported in past rounds (e.g. 75.96% lines across the 15 core files) was an undercount. Changed to compiling every target file exactly once and reusing the shared objects across all binaries; this report's figures are the corrected measurement.
- **`bpe_decoder.c` rose from 66.18%→87.87% lines and 50.00%→79.17% branches by exhaustively expanding the combinations of pixel format, DWTType, and byte order** (§4). In particular, an added case targeting the point that "decode-time byte order is an independent CLI setting, unrelated to the value used at encode time" led directly to discovering a real Rust bug (§3.7). The remaining ~12% is the `TransposeImg` branch, a disabled dump function, the `UseFill`-gated fill skip-over loop, and error paths — all confirmed unreachable code or out of this report's scope (see §4).
- **`bpe_encoder.c` had already grown substantially (+8-9 points, a previous round) by increasing pixel-format diversity (signed, non-default bit depths).** Reading `gcov`'s annotations directly to identify unexecuted lines, and targeting only the ones reachable from the CLI, proved effective.
- **`header.c`'s remaining ~22% (~44% of branches) was determined to be dead code that testing can't fill in.** There's no point adding further test cases to this file.
- **Added function-level verification for AC-side DPCM and confirmed no bug** (§3.4). The same concern raised on the DC side has been resolved.
- **The AC side's remaining major functions (`ACGaggleEncoding`/`ACGaggleDecoding`/`ACDepthEncoder`/`ACDepthDecoder`) rose from 79.92%→86.07% lines and 71.88%→80.73% branches with 3 images constructed to target AC coefficient amplitude (`BitDepthAC_5Bits`), then further to 86.48% lines and 82.81% branches with a direct-call sweep of `ACGaggleDecoding`** (§4). `BPEBlockCoding.c` also rose slightly as a side effect. The remaining gap is the heuristic branch unreachable because `OptDCSelect`/`OptACSelect` are always TRUE (the same kind as `header.c`'s dead code), and the defensive `ErrorMsg` for `N>5` (mathematically unreachable) — the last line inside `ACBpeEncoding` (the `SegmentFull` early return right after a `BlockScanEncode` call) was also proven mathematically equivalent to the identical check right before it, since `BlockScanEncode` never calls any output function, bringing `AC_BitPlaneCoding.c` to 100% effective coverage.
- **The DC side (`DC_EnDeCoding.c`) likewise rose from 83.04%→93.27% lines and 70.73%→86.18% branches with 4 images targeting the same kind of N-boundary and negative-Max_DC issues, then further to 93.86% lines and 87.80% branches with the `dc_qprime_lo_64` (low background + isolated bump) image and analysis of `DCEntropyDecoder`'s return value, reaching 100% effective coverage** (§4). Unlike the AC side, the DC side's `N` has no bit-width mathematical upper bound, but `QuantizationFactorQ_prime`'s selection formula is designed to always keep `N` at or below 10, confirming the defensive `ErrorMsg` for `N>10` is structurally unreachable.
- **`bitsIO.c` (the remaining 19 lines) was, after further investigation, confirmed entirely and deterministically unreachable, with no further gains possible from additional test cases** (§4). The `CodeWord_Length` 16/24/32-bit branches share the same cause as the known dead code from `CodewordLength_2Bits` always being 0. The `RateReached==TRUE` early return was confirmed, via a full source grep, to always be set and reset simultaneously with `SegmentFull`, and the preceding `SegmentFull` check always returns first. The `length>32` branch was judged practically unreachable based on the empirical coefficient range implied by the reference implementation's 16-bit pixel-depth ceiling (max `BitDepthDC_5Bits` observed was 19). The segment-trailing fill skip-over loop (2 lines) was actually covered as a side effect of adding the `dc_qprime_lo_64` test.
- **The very act of raising coverage became the trigger for discovering a real, previously hidden Rust bug (the decode-side ignoring of `-f`, §3.7).** This shows that filling in code-coverage gaps isn't merely improving a number — it's also a substantive verification act that exposes oversights in the test matrix itself (patterns that had been hiding a Rust-side bug).
- **Overall, "compatibility has been proven" can only be said for the test scope described in this report** — it isn't a state where "fully compatible for all inputs" has been mathematically proven unconditionally (see §7 item 13). That said, known differences currently stand at **zero** (float DWT's 1-ULP residual has also been fixed, in this section). Future test expansion (especially the still-untouched AC-side functions) is designed to further widen the scope of this guarantee (each script under `verify/` can be extended following the same pattern).

## 6. Cross-checking against external reference documents

Cross-checked, item by item, the issues raised against the C reference implementation in [oresat/libbpe's `UNL_readme_kielymods.txt`](https://github.com/oresat/libbpe/blob/master/bpe/source/UNL_readme_kielymods.txt) (accessed 2026-07-25) against the current code in `original/source/*.c`. This document is **word-for-word identical** to this repository's `original/readme_kielymods.rtf` (Aaron Kiely, June–July 2008), and `original/source/*.c` was brought into this repository as a version that already has this document's fixes applied in the first place. Results after actually reading the code to confirm:

| # | Issue raised | Location | Verdict |
|---|---|---|---|
| 1 | With `gaggles==0` (the final segment of a single block), `ACGaggleEncoding`/`ACGaggleDecoding`/`DCGaggleDecoding`/`DCEncoder` exit without doing anything | AC_BitPlaneCoding.c, DC_EnDeCoding.c | **Fixed**. All 4 functions consistently use `gaggles` as a "count of values" via `for (i = StartIndex; i < StartIndex + gaggles; i++)`; there's no early return for `gaggles==0` |
| 2a | `DCEntropyEncoder` codes `q - WtLL3` DC extra bitplanes per section 4.3.3 (too many) | DC_EnDeCoding.c:239-243 | **Fixed**. Computes `q - max(BitDepthAC, WtLL3)` |
| 2b | `DCDeCoding`'s section-4.3.3 bit-decoding logic remains commented out | DC_EnDeCoding.c:693-714 | **Fixed**. Present as active code (explicitly marked `/* --- Begin/End bug fix (Kiely) --- */`) |
| 2c | A stray fragment of section-4.3.3 decoding code remains in `ACBpeDecoding`, in the wrong position, **after** the `ACDepthDecoder` call | AC_BitPlaneCoding.c | **Fixed**. That code fragment doesn't exist (already moved to the DCDeCoding side, as in 2b) |
| 3 | Inside `AdjustOutPut`, the `DeConvTwosComp` call for `ShiftedDC + DecodingDCRemainder` lacks a `(long int)` cast, which can overflow | AdjustOutput.c:50-57 | **Fixed**. Both operands have a `(long int)` cast. The buggy version remains only as a comment |
| 4 | `DCGaggleDecoding`/`ACGaggleDecoding`'s loop variable `i` is `short`, overflowing at 2^15 or more blocks/segments | DC_EnDeCoding.c:262, AC_BitPlaneCoding.c:295 | **Fixed**. Both are `long int i` |
| 5 | A bug in the DPCM mapping's sign-inversion formula and `DPCM_DCDeMapper`'s branch structure | DC_EnDeCoding.c (DPCM_DCMapper/DeMapper) | **Fixed**. Both the `-(short)( ((X^Bits1)&Bits1)+1 )` form and the `if (MappedDC > 2*theta) { if ((long int)ShiftedDC[i-1] < 0) ... }` form are applied. The buggy version remains only as a comment |
| 6 | For a 15-block image, the segment-splitting while loop's boundary is off, causing a segfault | DC_EnDeCoding.c, AC_BitPlaneCoding.c (4 functions) | **Fixed (in a different form)**. Rather than the literal string the document shows (`while(S_20Bits >= gaggles + GaggleStartIndex)`), it's been refactored into a form that reliably caps the remainder with `min()`: `while(GaggleStartIndex < S_20Bits) { gaggles = min(GAGGLE_SIZE, S_20Bits - GaggleStartIndex); ... }`. This functionally avoids the same problem, including the 15-block boundary (`verify/run_compat.sh`'s `blocks_15` case actually exercises this path and PASSes) |
| 7 | `DeConvTwosComp` special-cases `leftmost==1`, which can make decoding an all-zero image fail | DC_EnDeCoding.c:47 | **Fixed**. The guard condition has no `\|\| (leftmost==1)` |
| 8 | Endianness handling: byte swapping doesn't work / the default depends on the runtime environment | bpe_encoder.c (ImageRead), bpe_decoder.c (ImageWrite, ImageWriteFloat) | **Fixed**. All 3 functions compute `machineendianness` at runtime and compare it against `PixelByteOrder` |

**Conclusion: all 8 items are fixed, and no code changes were needed.** Only #6 doesn't match the exact post-fix string the document shows, but the implementation has been refactored into a more robust form (capping via `min()`), functionally equal to or better than the fix at avoiding the boundary case. This cross-check produced no additional code changes to `original/source/*.c`, so §2–§5's test results and coverage evaluation remain fully valid as-is.

## 7. Future work

1. ~~`AdjustOutPut`'s coverage~~ → Resolved. Improved from 65.02%→96.90% lines via the direct-call harness (§3.5), confirming no mismatches across all 1024 combinations of DWTType × stoppedstage × DC branch × X/Y coordinate. Analyzing the remaining 95 lines found a lack of sign variation, so a `variant` (sign inversion, phase shift) dimension was added to the test values, expanding to 6144 combinations (§3.6), improving to 99.55% lines. The last 16 lines were also proven confirmed dead code (a redundant guard's `else` clause) via control-flow analysis, reaching 100% effective coverage.
2. ~~§3.3's 1-ULP residual's frequency was quantified across 3 images and 28 rate values (5/28 cases, most in noise_256). Going forward, check whether it can also occur in other, as-yet-unexamined float DWT paths (particularly on the **encode side**).~~ → Resolved. The root cause (`inverse_lifting97f` failing to reproduce C's per-operator type-conversion rules) was identified and fixed (§3.3). After the fix, 0 mismatches, including all 5 cases. Also checked the encode side (`forwardf97f`) and confirmed there's no room for the same kind of bug, since the C reference implementation's `LowPassFilter`/`HighPassFilter` are declared `const float` from the start.
3. ~~Other AC-side functions (`ACGaggleEncoding`/`ACGaggleDecoding` etc., beyond DPCM)~~ → Resolved. Added 3 images with AC coefficient amplitude (`BitDepthAC_5Bits`) controlled to 1, 2, and 17 (lossless + a segment-size-16 rate sweep), improving `AC_BitPlaneCoding.c` from 79.92%→86.07% lines and 71.88%→80.73% branches (as a side effect, `DC_EnDeCoding.c` also rose to 83.04%→88.60%, `BPEBlockCoding.c` to 93.96%→94.86%). The residual gap is the unreachable heuristic branch from `OptDCSelect`/`OptACSelect` always TRUE, the mathematically unreachable defensive `ErrorMsg` for `N>5`, and a few timing-dependent early returns of the same kind as `AdjustOutPut`'s X/Y coordinates — all confirmed in §4. Building a direct-call harness this far was judged lower cost/benefit relative to the remaining scope, and was passed on.
4. ~~`bpe_decoder.c`/`header.c`'s branch coverage~~ → Resolved. `bpe_decoder.c` improved from 66.18%→87.87% lines and 50.00%→79.17% branches by expanding combinations of pixel format × DWTType × byte order (this process discovered and fixed the Rust decode-side `-f`-ignoring bug, §3.7). The residual gap is confirmed unreachable/out-of-scope code such as the `TransposeImg` branch, disabled functions, the `UseFill`-gated loop, and error paths (§4). `header.c`'s residual gap was likewise confirmed CLI-unreachable dead code, needing no further work.
5. ~~`StagesCodingGaggles.c`'s coverage~~ → Resolved. Following the segment-size-diversification sweep (85.93%→88.85% lines), added a direct-call-plus-exhaustive-bit-budget-sweep harness for `StagesDeCodingGaggles1/2/3`, improving to 88.85%→92.80% lines and 83.69%→87.06% branches. The remaining 31 lines were also proven confirmed dead code via control-flow analysis, via 2 patterns — "a narrower checkpoint always intercepts first" and "a coordinate-selection `else` branch contradicts its entry condition" — reaching 100% effective coverage.
6. ~~`bpe_encoder.c`'s (75.98%/70.63%) remaining 49 lines~~ → Investigation complete, confirmed all lines are confirmed dead code/out-of-scope error paths. The legacy file-size-guessing fallback (42 lines, inside `ImageSize`) is structurally unreachable from the CLI, since `main.c` always initializes `ImageRows`/`ImageWidth` to the default 256 (never 0, even when `-w`/`-h` are omitted). The rest is the `UseFill`-gated fill loop (dead, known cause) and defensive `fopen`/return-value-check code. Judged no further testing necessary.
7. ~~DC-side N boundaries, negative Max_DC~~ → Resolved. Using the same technique as the AC side (an empirical search for BitDepthDC-controlling images), improved `DC_EnDeCoding.c` from 83.04%→93.27% lines and 70.73%→86.18% branches. The 3 lines that had originally remained unexplained (2 for a specific `QuantizationFactorQ_prime` condition, 1 for `return k;`) were each resolved via the `dc_qprime_lo_64` image and analysis of `DCEntropyDecoder`'s return value, reaching 100% effective coverage.
8. ~~Computing the reachability breakdown (effective coverage)~~ → Resolved. Quantified for the first time in §4, computing an effective coverage of 99.00% after excluding 317 lines of confirmed dead code. This separated, out of the raw coverage of 94.72%, "the area that no amount of test engineering can fill in" from "the area that still has headroom." The headroom at the time (70 lines, about 1.0% of the total) was almost entirely resolved (actually covered) or proven confirmed dead code by applying the same direct-call-harness technique used for `AdjustOutPut` to `StagesCodingGaggles.c`, the AC/DC side, and `PatternCoding.c` — the 15 core files' overall effective coverage ultimately reached **100.00%** (see item 11).
9. ~~`lifting_97M.c`/`waveletbpe.c`~~ → Investigation complete, confirmed all remaining lines in both files are confirmed dead code (100% effective coverage). `lifting_97M.c`'s `half<=1` branch is mathematically unreachable given the combination of `IMAGE_WIDTH_MIN`/`IMAGE_ROWS_MIN` (17) and the fixed level count (3); `waveletbpe.c`'s `FLOAT_WAVELET` branch inside `DWT_Reverse` is unreachable because its one and only caller (`bpe_decoder.c`'s `DecodingOutputInteger`) already guarantees `DWTType==INTEGER_WAVELET` — a new dead-code pattern, distinct from CLI-fixed-value dead code, of "dead code due to the caller's precondition." No further testing needed.
10. ~~`BPEBlockCoding.c`'s CustomWt-band-dependent branches~~ → Resolved. 13 of 16 lines turned out structurally unreachable, since the comparison expression `TypeC << (1<<(3-i)) != 1` can mathematically never be true for `TypeC` (a 4-bit field) — apparently a typo where `&` should have been written. Lines 149 and 238, originally "unexplained," turned out to be confirmed dead code too — a redundant structure where a later stage separately re-checks one side of a 2-condition `continue`'s AND. The remaining 1 line (line 181, `si++;`) was also proven confirmed dead code by scrutinizing `si`'s consumption order, finding the immediately preceding lines (99-100) had already checked exactly the same condition (100% effective coverage).
11. ~~Resolving the residual coverage gap (`StagesCodingGaggles.c`, the AC/DC side, `PatternCoding.c`)~~ → Resolved. Applied the "direct call + exhaustive sweep" technique established with `AdjustOutPut` to these too, pushing the 15 core files' overall effective coverage from 99.00%→100.00%. Built `verify/c_unit_tests/gen_stages_gaggles_coverage.c` (exhaustively sweeping `StagesDeCodingGaggles1/2/3` across bit budgets 0–150) and `gen_residual_gaps_coverage.c` (for `RefBitsDe`/`ACGaggleDecoding`), raising raw line coverage from 94.72%→95.14%. Additionally, control-flow analysis of the remaining gaps found 5 confirmed-dead-code patterns — "a narrower checkpoint always intercepts first," "a coordinate-selection `else` branch mathematically contradicts its entry condition," "`RateReached` and `BitPlaneStopDecoding` only ever change together," "the callee has no means of output or state change at all," and "the immediately preceding check has already consumed the identical condition" — bringing all 15 core files (including `AdjustOutput.c`, `StagesCodingGaggles.c`, `DC_EnDeCoding.c`, `PatternCoding.c`, `AC_BitPlaneCoding.c`, `BPEBlockCoding.c`) to 100% effective coverage.
12. ~~`PatternCoding.c`'s `CodingOptions`'s remaining 5 branches of branch coverage~~ → Resolved. 1 branch (the 4-bit Rice-split option-selection chain's line 328 false direction) was resolved by newly discovering a concrete counterexample symbol-value combination and adding it as a test; the remaining 4 (lines 330, 334, 335, 336) were proven structurally unreachable via transitivity and the tautology "of 4 real numbers, one is always at or below the other 3." This brought all 15 core files (including `PatternCoding.c`) to 100.00% effective coverage (both line and branch), leaving no "unproven" item remaining in this report.
13. **Future expansion policy in light of the point that 100% coverage isn't "a formal proof of complete compatibility."** Code coverage (line and branch) only guarantees "this code path was executed with at least 1 value, and matched Rust" — not "every other value that takes the same path" too. In fact, every real bug this project found (DPCM N=16, the float DWT rounding bug, the decode-side `-f` ignoring bug) was "a bug lurking in an untouched code path," and improving coverage was effective at closing this kind of hole — but it has no power to rule out a bug stemming from "a different value on an already-touched path." To get closer to a formal proof across all input values, the following are worth considering, in priority order:
    - ~~**Add randomized/fuzzing tests**~~ → Resolved. Built `verify/fuzz_compat.py`, randomly combining image size, bit depth, signedness, byte order, rate, segment size, and pixel content to repeat perspective 1's byte-compatibility check (§2.3). Both integer and float DWT are the default target now (there's no longer a reason to exclude float DWT after §3.3's root-cause fix). Ran 500 and 3000 cases locally across both DWT types, 0 mismatches. Integrated into CI in 2 tiers: a lightweight smoke test of 50 fixed-seed cases on every push/PR, and a weekly/manual deep run of 5000 random-seed cases (the `deep-fuzz` job).
    - ~~**Extend the existing "direct call + exhaustive (or near-exhaustive) enumeration" technique (already applied to Rice/DPCM/PatternMapping/`AdjustOutPut`, §2.2) to other accumulator-involving functions**~~ → Resolved. Following `CodingOptions` (an exhaustive sweep of single/pair/triple symbols, 4230 combinations), added direct calls for `ACDepthEncoder`/`ACDepthDecoder` (the entire N range × 5 segment sizes × 3 distribution patterns, 120 combinations) and `DCEntropyEncoder`/`DCEntropyDecoder` (all reachable branches of `QuantizationFactorQ_prime` × 4 N-brackets × 5 segment sizes × 3 distribution patterns, 75 combinations), no mismatches in either (§2.2). `ACGaggleEncoding`/`ACGaggleDecoding` and `DCEncoder`/`DCGaggleDecoding` themselves (gaggle-level internal functions, private on the bpe-rs side too) aren't tested directly — they're driven indirectly through the publicly exposed layer above them, like `ACDepthEncoder`. Testing these functions themselves directly would require a visibility change on the bpe-rs side, so it was passed on. In the process, it turned out the DC-side test-vector generator was mistakenly zero-initializing `CustomWtLL3_2bits` (the LL3 subband's default weight, which `HeaderInilization` always hardcodes to `3`), but bpe-rs's own implementation had it correctly implemented from the start — confirmed to be a test-harness initialization gap (see the main text).
    - ~~**Mutation testing**~~ → Resolved (§3.9). Ran `cargo-mutants` automatically across 8 files and 1131 mutants (383 detected, 655 undetected, 73 invalid, 20 timeouts); manually applying 5 of the "undetected" mutants to the real source and re-verifying with `--include-ignored` found 4/5 were reliably detected by the shared-vector tests (the automated run's figure of 655 turned out to be an underestimate, caused by the measurement tool's constraint of not resolving the relative path `../verify/vectors/*.txt`), and the remaining 1 (`rice/encode.rs`'s `input_val - 4` → `+4`, bit_length=4) was confirmed to be a **mathematically equivalent mutation** that doesn't change the output byte stream at all (since `bits_write` only outputs the low-order bits, `-4` and `+4` agree in the low 2 binary bits over the range 4–7) — not a weakness of the test suite, but a mutation that's undetectable in principle.
    - ~~**Resolving float DWT's 1-ULP residual (§3.3)**~~ → Resolved. Originally misdiagnosed as "a compiler rounding-implementation difference," but after empirically confirming neither `-ffp-contract=off` nor `-O0` resolved it, added level-granularity trace points (`post_idwt_level2`/`post_idwt_level1`) to `compare_traces.py`, narrowing the divergence down to the first level, and ultimately identified the true cause: C's "usual arithmetic conversions" apply per operator, not to the whole expression, so `inversef97f`'s pairwise additions (e.g. `x[r_idx+1] + x[r_idx-1]`) actually happen as f32-to-f32 additions, only promoting to f64 once multiplied by the coefficient. bpe-rs converted each addend to f64 individually before the addition, and this difference in promotion timing was exactly the 1-ULP difference. Fixed by adding a helper to `inverse_lifting97f` that adds in f32 first and then converts to f64, confirming 0 mismatches across 178/178 cases and 3500 fuzz cases (§3.3).
    - (Lowest priority, given the expected low cost/benefit) **A mathematical proof of equivalence via formal methods/symbolic execution**. Since the CCSDS spec itself is a set of simple bit manipulations, this isn't impossible in theory, but is likely not worth the effort it would take.

## Reproduction steps

```sh
git submodule update --init --recursive
verify/run_compat.sh --include-slow      # §2.1
verify/run_unit_vectors.py                # §2.2
verify/fuzz_compat.py --iterations 2000                  # §2.3
verify/compare_traces.py <raw> <w> <h> [--float-tol=N] [bpe args...]   # §2.4, for triage
```

Coverage measurement (§4) was done by compiling every target `.c` file exactly **once** with `gcc -O0 --coverage -c`, linking both the `bpe` binary and every generator in `verify/c_unit_tests/` against this shared set of objects (without recompiling) with `--coverage`, running both in the same directory, and running `gcov -b` against the 15 core files (see §4's "Correcting the measurement method" — recompiling shared source per binary breaks the `.gcda` merge, so be careful of that). Not wired into standing CI (since it needs a temporary measurement-only build).
