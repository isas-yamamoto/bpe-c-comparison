# 調査ログ — C/Rust 互換性検証

[English](INVESTIGATION_LOG.md)

これは [COMPATIBILITY_REPORT.ja.md](COMPATIBILITY_REPORT.ja.md) の結論に至った時系列の調査ログ——個々のバグ調査・カバレッジ計測ラウンド・未解決項目の全てを、実際に起きた順序で記録している。現状のまとめは同ファイル（§0 要約、§1 検証観点、§2 試験内容と結果、§5 総合評価、§6 外部参照ドキュメントとの照合）を参照。

このファイルの節番号（§3・§4・§7）は、両ファイルがまだ1つだった頃と同じものを維持している。そのため既存の「（§3.3）」のような引用は、両ファイルを通してそのまま正しい場所を指し続ける。このファイルは追記され続けるだけで、`COMPATIBILITY_REPORT.ja.md`は現状に合わせて随時書き換わる。

## 3. 発見した不一致とその対応

関数レベル検証（COMPATIBILITY_REPORT.ja.md §2.2）と、後から追加したレート制限デコードの掃引（COMPATIBILITY_REPORT.ja.md §2.1）を導入したことで、通常の試験だけでは検出できなかった複数の実バグを発見・修正した。**いずれも「通常のテスト画像がたまたま踏まない境界値」でのみ発現する**ため、観点3・観点1の掃引拡張を行わなければ気づけなかった。

**→ 現状の一覧は§3.8の表で足りる。以下の§3.1〜3.10は個々の発見に至った調査の経緯（時系列ログ）。**

### 3.1 float DWT の丸め誤差

C参照実装の `(int)(v + 0.5)` は `0.5` が `double` リテラルであるため加算が倍精度で行われる。Rust側は単精度(`f32`)のまま加算していたため、値の小数部が 0.5 の1 ULP以内に来る稀なケース（256×256画像で65536画素中3画素）でのみ丸め結果が1ずれる。COMPATIBILITY_REPORT.ja.md §2.1 の `-t 0`（float DWT）ケースで発見。`round_away_from_zero`・`dwt_reverse_floating` の両方を `f64` 経由の加算に修正。

### 3.2 DPCM DC マッピングの整数幅バグ2件

DC 深度 N=16 のときにのみ発現。COMPATIBILITY_REPORT.ja.md §2.2 の DPCM ベクタ（中間値付近の振動系列）で発見。

1. **`-(short)(...)` の丸め込み**: Cは負値化の前に中間値を16bitの `short` へ縮小する。中間値がちょうど 32768 になる場合（ブロックの生値の最上位ビットが立っている時）、`short` キャストが 32768→−32768 に折り返り、符号反転後の値が +32768 になる（本来 Rust が計算していた −32768 とは符号が逆）。
2. **`theta = min(ShiftedDC − X_Min, X_Max − ShiftedDC)` の符号なし折り返し**: `ShiftedDC`/`X_Max` は C では符号なし `DWORD32`。上記(1)によって `ShiftedDC` が `[X_Min, X_Max]` の範囲外（+32768）に出ると、`X_Max − ShiftedDC` の引き算が符号なし演算として折り返り、負値の代わりに巨大な値になる。C の `min()` マクロはこの巨大値で比較してしまうため、意図と異なる `theta` が選ばれる。

Rust側は `wrapping_sub` を使い、Cの符号なし折り返しを明示的に再現するよう修正（`neg_short`/`theta_from_prev` ヘルパー）。

### 3.3 逆DWT（float）の精度不一致 — 根本原因を特定し修正済み

C参照実装の `inversef97f`（`lifting_97f.c`）は係数リテラル（`0.788486` 等）が無サフィックスのため `double` 型であり、`float` 配列との演算全体が倍精度に昇格してから最後に `float` へ narrowing される。Rust側は単精度のまま演算しており、**float DWT を使うデコード全般で微小（~1e-6〜1e-7）だが広範囲な誤差が累積する**構造的な問題だった。通常は最終画素丸め（`(int)(v+0.5)`）の境界を越えないため COMPATIBILITY_REPORT.ja.md §2.1 の基本ケースでは顕在化しなかったが、セグメントサイズ64・レート0.1/0.75（COMPATIBILITY_REPORT.ja.md §2.1のレート掃引）で実際に1画素ずれた。

`inverse_lifting97f` の該当式を `f64` 中間演算に変更（Cの昇格を再現）したところ、掃引対象の float DWT ケース10件中8件が新たにバイト一致するようになった。**残り2件（0.1, 0.75）は依然として1画素ずれていた。** `verify/compare_traces.py` で単一レベル・単一関数呼び出しに切り分けたところ、C側とRust側は同一の係数（バイト単位で完全一致するf64リテラル、実測で確認済み）を使っているにもかかわらず結果が1 ULP異なるケースが実測で確認された。当初はこれを gcc と rustc（LLVM）の浮動小数点丸めの実装差による、アルゴリズムのバグではない解消不能な残存差と**誤って**判断していた。しかし、後日ユーザから「本当に解決したい」との要望を受けて再調査した結果、**実際には解消可能な、bpe-rs側の本物のバグだった**ことが判明した。

その後、チェッカーボード・ノイズ画像を同じ掃引に追加した際にも同じ現象を確認していた（§7旧課題2の定量化）。画像内容によって発生頻度が変わる: baseline_256（グラデーション）は8レート値中2件、checkerboard_256は10件中0件、noise_256（擬似ランダム、係数分布が最も広範）は10件中3件で発生。「係数分布が広い・極端な画像ほど1 ULP境界を踏みやすい」という傾向は、後述の真の原因（f32加算段での丸め誤差蓄積）とも整合する。

**当初の誤診断で棄却した2つの仮説**（結果的には的外れだったが、探索の記録として残す）:

1. **`-ffp-contract=off`**（C参照実装のCコンパイラでFMA(融合積和)による縮約を明示的に禁止）: 結果は不変。このビルド環境のgccデフォルトターゲット（`x86-64`ベースライン）は`-mfma`が無効でハードウェアFMA命令自体を生成し得ないため、FMA縮約が原因でないことは正しく確定できていた。
2. **`-O0`**（Cコンパイラの最適化を全面的に無効化）: むしろ悪化した（65536画素中34646画素が不一致）。コンパイル時定数畳み込みが原因という仮説も正しく棄却できていた。

**レベル単位のトレース地点を追加して絞り込み**: `compare_traces.py` に `post_idwt_level2`/`post_idwt_level1`（`lifting_f97_2D`/`lifting_f97_2d` の各レベルの行パス直後のダンプ、levels=3固定・列パス→行パスをレベル2→1→0の順に処理）を新設。既知の再現ケース（`baseline_256`・rate=0.1・`-t 0`・segment=64）で実行したところ、`reassembled`（逆変換直前）は完全一致するが、最も粗いレベル（`post_idwt_level2`）の時点で既に乖離していた（index 3、diff=-7.63e-6）。これで探索範囲を「3レベルのどこか」から「最初のレベルの中のどこか」まで絞り込めた。

**真の根本原因**: C の「usual arithmetic conversions（通常の算術型変換）」は**式全体ではなく演算子1つ1つに適用される**、という点を見落としていた。`inversef97f` の実際の式は例えば `0.0406894 * (x[r_idx+1] + x[r_idx-1])` の形をしており、括弧内の `x[r_idx+1] + x[r_idx-1]` はどちらも `float` なので、**この加算自体はまず単精度(`float`)で行われる**。その `float` の加算結果が、無サフィックス（`double`）の係数 `0.0406894` と掛け算される段になって初めて倍精度に昇格する。ところがbpe-rs側の実装は、加算の**前に**それぞれの被加数を個別に `f64` へ変換していた（`f(x[r_idx+1]) + f(x[r_idx-1])`、両方ともすでに `f64` 同士の加算）。「先にf64化してから足す」と「f32のまま足してからf64化する」は、丸め誤差がf32加算の段で発生し得る分、数学的に等価ではない——これが1 ULP差の真因だった。

**修正**: `bpe-rs/src/wavelet/lifting97f.rs` の `inverse_lifting97f` に `fp(a: f32, b: f32) -> f64`（f32のまま加算してからf64へ変換するヘルパー）を追加し、ペア加算箇所全てをこのヘルパー経由に変更（Cの演算子単位の型変換規則を忠実に再現）。ハードコードされた実データ（64要素、`baseline_256`をrate=0.1でデコードした際の実際の中間値）による回帰テストも追加した。

**検証結果**:
- 最小再現ケース（64要素の標準入力）: 修正前は64要素中13要素が不一致 → 修正後 **0件**。
- 既知の再現ケース（`baseline_256`・rate=0.1・`-t 0`・segment=64）: `post_idwt` 完全一致、最終出力もバイト完全一致。
- `baseline_256`/`noise_256` で除外していた5つのレート値、全てバイト完全一致に復帰。
- `verify/run_compat.sh --include-slow`: 除外を全て解除した上で **178/178 PASS**。
- `verify/fuzz_compat.py --dwt-type both`: 500件（seed=123）・3000件（seed=999）ともに不一致0件。

**エンコード側の確認**: 同種のバグがエンコード（順方向DWT、`forwardf97f`）側にもないか確認した。C参照実装の `LowPassFilter`/`HighPassFilter`（`forwardf97f` が使う係数）は `const float` で宣言されており、逆変換側の `LowPassFilterInverse`/`HighPassFilterInverse`（`double const`）とは異なり最初から単精度である。したがって順方向側にはこの種の昇格タイミング差が生じる余地自体がなく、バグは存在しない。§7旧課題2で「今後の確認事項」としていたエンコード側の懸念はこれで解消した。

**結論**: 逆DWT（float）の精度不一致は根本原因を特定し完全に修正済み。「gcc/rustcのコンパイラ差による解消不能な残存差」という当初の判断は誤りだった——実際は加算順序（float先か、f64先か）の違いという、修正可能な実装バグだった。

### 3.4 AC側DPCMの追加検証（bug なし）

DC側DPCMのバグ発見を受け、AC側 `DPCM_ACMapper`/`DPCM_ACDeMapper` にも同種の問題がないか COMPATIBILITY_REPORT.ja.md §2.2 に追加検証を行った。AC側は `BitMaxAC`/`MappedAC` が `WORD16`（符号なし16bit）であり、Cの整数拡張規則により `int`（符号付き）に昇格するため、DC側の `DWORD32`（符号なし64bit相当）のような符号なし折り返しは原理的に起こらない——という型解析上の予想を、実際にベクタを生成して検証し、**不一致なしと確認**した。「起こらないはず」を実測せず放置しないという、本プロジェクトの検証方針の一貫性を保った。

### 3.5 `AdjustOutPut` の関数直接呼び出し網羅検証（bug なし）

§4で後述する通り、`AdjustOutPut`（レート制限時の途中停止デコード修復関数、コアアルゴリズム全体の約49%を占める最大の関数）は、全パイプラインのバイト一致試験（観点1）をどれだけレート・画像内容を増やして掃引しても、ブロック内の停止座標（`X_LocationStopDecoding`/`Y_LocationStopDecoding`、8×8の64通り）はビットストリームの端数が偶然どこで切れるかに依存するため、大半が実質的に踏めないことが判明した。

そこで、Rice/DPCM/パターンマッピングと同じ「C参照実装のオブジェクトコードに直接リンクし、関数を単体で呼び出す」手法をこの関数にも適用した（`verify/c_unit_tests/gen_adjust_output_vectors.c`）。`StructCodingPara`/`BitPlaneBits` を直接構築し、`DWTType`（整数/浮動小数点）× `stoppedstage`（1〜4）× DC分岐の有無（2通り）× ブロック内停止座標 `X/Y_LocationStopDecoding`（8×8=64通り）の全1024通りを直接駆動、結果の8×8ブロック係数（int/float 双方、3ブロック分）をCとRustで突き合わせた。

**結果: 1024/1024ケース、全て一致。** 全パイプライン試験では到達不能だった停止座標の細部まで含め、`AdjustOutPut`/`adjust_output` に不一致は確認されなかった。

### 3.6 `AdjustOutPut` 直接呼び出しの値多様化（bug なし）

§3.5の1024ケースは「どのDWTType×stoppedstage×X/Y座標を通るか」という**構造的な**網羅は完全だったが、`AdjustOutput.c`のカバレッジは97.35%止まりで95行が未実行のままだった（§4）。中身を調べると、95行の**ほぼ全て**が `if(coeff > 0) coeff += X; else if(coeff < 0) coeff -= X;` という符号依存の分岐対のうち片側だった。原因は、§3.5のジェネレータが使う `int_val`/`float_val`（ブロック内の(block,m,n)から決定論的にハッシュ生成する値）が、ある特定の構造的パス（stoppedstage×X/Y座標の組み合わせ）に対応する具体的なセル位置では**常に同じ符号**の値しか生成していなかったこと——構造は1024通り全て試しても、そのセルの値の符号というもう1つの次元は固定されたままだった。

対策として、`int_val`/`float_val` に符号反転・位相シフトを加える `variant`（0〜3の4通り）を導入し、既存の1024通り × 4variant = 4096通りに拡張。加えて、`b_DC = min(BitPlaneStopDecoding, QuantizationFactorQ)` の`else`分岐（`BitPlaneStopDecoding > QuantizationFactorQ`の場合）が旧来の2通りのb_DC設定では一度も真にならないと判明したため、これを満たす3つ目のb_DC設定を追加（計6144通り）。

**結果**: `AdjustOutput.c`単体のカバレッジ（直接呼び出しジェネレータのみ、他の試験と合算しない状態）が95.70%→99.55%まで上昇。Rust側は同じ`variant`ロジックを`bpe-rs/src/adjust.rs`のテストに複製し、6144ケース全て一致を確認（不一致なし）。

残る16行（8行×2箇所、整数半分と浮動小数点半分それぞれ）を制御フロー解析で追跡した結果、**構造的に到達不能な冗長ガード**と判明した：この16行を含む`else`節は`if((m<4)&&(n<4))`という条件の`else`側（すなわち`m>=4 || n>=4`）に対応するが、この`else`節が置かれているスコープに到達する時点で、`m<=3 && n<=3`は既に外側の分岐（`m>3||n>3`を先に別処理する分岐）によって保証済みである。つまり`(m<4)&&(n<4)`は常に真であり、その`else`は数学的に実行され得ない。ソースコードの読解のみで確認済み（`AdjustOutput.c:298`の条件と、その外側の`m>3||n>3`ガードの相互関係）。

これにより`AdjustOutput.c`は§4のパイプライン試験全体と合わせて行97.35%→99.55%・分岐85.16%→87.15%まで伸び、**残存16行は全て確定デッドコードで実効カバレッジ100%**に達した。

### 3.7 デコード側 `-f`（PixelByteOrder）の無視

`bpe_decoder.c` のカバレッジを追加調査中（§4）に発見。`PixelByteOrder` はビットストリームヘッダに含まれない、C参照実装では**呼び出しごとの単なるCLI設定**である（`header.c:91` のデフォルト0、`main.c:170` の `-f` パース）。つまりC参照実装では、デコード時に指定した `-f` がエンコード時の値と無関係にそのデコード呼び出しの出力バイト順を決める。

ところが `verify/run_compat.sh` はこれまで、デコード側の呼び出しに `-f` を一度も渡していなかった（エンコード側の共通引数にだけ含まれ、デコードの4呼び出しには渡されていなかった）。この機械はリトルエンディアンのため、`-f` を渡さない全ての既存ケースは常にデコード側のデフォルト（0=リトルエンディアン）で復号され、`ImageWrite`/`ImageWriteFloat` 内の「要求された順がマシンのネイティブ順と異なる場合にバイトを入れ替える」分岐（`PixelByteOrder != machineendianness`）は、C側・Rust側ともに一度も「入れ替えが必要」側を通っていなかった。

`bpe_decoder.c` のカバレッジを埋める過程で、デコード呼び出しにも `-f` を明示的に渡すよう `run_compat.sh` を修正したところ（既存ケースは自分自身のエンコード時の値をデフォルトとして継続、新規ケースは意図的に反転した値を指定）、**bpe-rs側でこれまで隠れていた不一致が露呈した**: `pixels16_f1`（`-f 1` でエンコード・デコードする既存ケース）を含む12件が新たにFAILした。

原因調査の結果: `bpe-rs/src/main.rs` はCLIの `-f` を常にローカル変数へパースするが、それを `CodingPara.pixel_byte_order` に反映していたのは**エンコード分岐のみ**であり、デコード分岐では代入されずに握り潰されていた。`image_write`/`image_write_float`（`src/image_io.rs`）自体のスワップ判定ロジックはC参照実装を正しく移植済みだったが、`pixel_byte_order` が常に構造体のデフォルト値`0`のままだったため、デコード時に何を`-f`指定しても常に「スワップ不要」側しか通っていなかった。

デコード分岐に `coding.pixel_byte_order = byte_order;`（エンコード分岐の対応行と同じ1行）を追加して修正。修正後、COMPATIBILITY_REPORT.ja.md §2.1 の全ケースPASS（この不一致を露呈させた12件を含む。発見当時の§2.1は87ケースで、その後のAC/DC/StagesCodingGaggles向け追加で現在の166ケースまで増えている）。

**含意**: この不一致は、全パイプライン試験（観点1）がこれまで「デコード時に非デフォルトのバイト順を実際に要求する」ケースを一度も含んでいなかったために、87ケース目まで一切気づかれなかった。カバレッジ計測（観点1がCコードのどこを踏んでいるかの可視化）が、C側の未踏コードだけでなく、**試験マトリクス自体の見落とし**（Rust側のバグを隠していた欠落パターン）の発見にも直接つながった一例。

### 3.8 対応状況まとめ

| 不一致 | 発見手段 | 影響範囲 | 状態 |
|---|---|---|---|
| float DWT 丸め誤差（forward/rescale） | 観点1（全パイプライン、`-t 0`） | float DWT 使用時、稀な画素 | 修正済み |
| DPCM short キャスト折り返し | 観点3（DPCM関数ベクタ） | DC深度 N=16、特定の生値パターン | 修正済み |
| DPCM theta 符号なし折り返し | 観点3（DPCM関数ベクタ） | 上記(1)の結果として誘発、N=16 | 修正済み |
| 逆DWT（inversef97f）精度不一致 | 観点1（レート制限デコード掃引） | float DWTデコード全般、通常は非顕在化 | 完全に修正済み。当初はコンパイラの丸め実装差と誤診断していたが、加算順序（f32先かf64先か）に起因する本物のバグと判明し修正 |
| AC側DPCM | 観点2追加検証 | — | 該当なし（バグ確認されず） |
| `AdjustOutPut`（全1024の停止座標パターン） | 観点3追加検証（直接呼び出し） | — | 該当なし（バグ確認されず、§3.5） |
| デコード側 `-f`（PixelByteOrder）の無視 | 観点1（`bpe_decoder.c`カバレッジ拡充に伴うマトリクス見直し） | 16bit画素のデコードで非デフォルトのバイト順を指定した場合全般 | 修正済み（§3.7） |
| FMA縮約によるfloat DWT乖離（forward/inverse双方） | クロスアーキテクチャ検証（Apple M5・ARM64/macOS、ユーザによる実機検証） | float DWT全般、FMA命令がベースISAのターゲット（ARM64等）限定。x86-64ベースラインは無関係 | 修正済み（`-ffp-contract=off`をMakefileに固定、§3.10） |

修正後、COMPATIBILITY_REPORT.ja.md §2.1・§2.2 の全試験が PASS することを確認済み（§3.3 の残存2ケースは掃引対象から除外の上で）。

### 3.9 ミューテーションテストによる試験群の検出力検証

コードカバレッジは「そのコードパスを実行したか」しか見ないため、§7項目13の指摘（「カバレッジ100%は全入力値の証明ではない」）を補強する保険として、bpe-rsのソースに意図的にバグ（ミューテーション）を注入し、既存の試験群が実際に検出できるかを確認した。`cargo-mutants`（v27.1.0）を導入し、共有ベクタ試験（Rice/DPCM/PatternMapping/AdjustOutPut/CodingOptions/ACDepthEncoder/DCEntropyEncoder、いずれも`#[ignore]`付きで`--include-ignored`時のみ実行）が実際に効いている8ファイルを対象にした。

**自動実行時の事故と復旧**: `cargo-mutants`の自動実行モードには2つの制約があった。(1) `--in-place`（実ソースを直接変異させて試験する高速モード）をバックグラウンドで実行した際、ハーネスのバックグラウンドタスク追跡が実際のプロセス終了を検知できず「完了」と誤報告し、プロセス自体は検知漏れのまま動き続けて`bpe-rs`のソースファイルを変異させたまま放置する事態が発生した。`ps aux`で該当プロセスを発見し`kill`、`git status`/`git diff`で全ファイルがHEADと完全一致することを確認して復旧した（実害なし）。(2) デフォルトのスクラッチコピー方式（`--in-place`なし）は安全だが、`#[ignore]`テストが参照する`../verify/vectors/*.txt`という相対パスが、コピー先の一時ディレクトリには存在しない`bpe-c-comparison`親リポジトリ側のファイルのため解決できず、`--include-ignored`を指定した実行は成立しなかった。この制約により、自動実行結果は「常時有効なテストのみ」を母数とした計測になっている。

**自動実行の結果**: 8ファイル・1131変異中、383件検出・655件未検出・73件ビルド不能（無効な変異）・20件タイムアウト。未検出率がおよそ63%と高いが、これは主に`adjust/stage3.rs`・`adjust/stage4.rs`（`AdjustOutPut`の本体、未検出655件中324件を占める）が、常時有効なテストでは全くカバーされておらず`adjust_output`共有ベクタ試験（6144通り、§3.5・§3.6）でのみ検証されている——という、この計測方法自体の限界を反映した数字であり、試験群の実際の検出力を過小評価している。

**手動検証による裏付け**: 自動実行が「未検出」と分類した変異から5件（`pattern/options.rs`・`ac/depth.rs`・`dc/entropy.rs`・`adjust/stage4.rs`・`rice/encode.rs`、いずれもコアロジックの比較演算子や算術演算子を1箇所ずつ変える変異）を選び、実際の`bpe-rs`ソースに手動で適用し、`cargo test -- --include-ignored`で該当する共有ベクタ試験を実行、その都度`git checkout`で復元するというサイクルを回した。

| ファイル:行 | 変異内容 | 判定 |
|---|---|---|
| `dc/entropy.rs:74` | `bit_depth_ac_5bits < quantization_factor_q` → `==`（§4.3.3追加ビットプレーンの発火条件、前ラウンドでテストハーネス側のバグを発見した条件式そのもの） | **検出**（`dc_entropy_shared_vectors_match_c_reference`が失敗） |
| `pattern/options.rs:31` | `sym_len == 1` → `!=` | **検出**（`coding_options`共有ベクタ試験が失敗） |
| `ac/depth.rs:47` | `dpcm_ac_mapper`の`x_max`計算、`- 1` → `+ 1` | **検出**（AC DPCM・ACDepthEncoder共有ベクタ試験の両方が失敗） |
| `adjust/stage4.rs:22` | `ii < stop_block` → `==`（`AdjustOutPut`本体の分岐） | **検出**（`adjust_output`共有ベクタ試験が失敗——自動実行が「324件未検出」とした`stage3/4`が、実際にはほぼ全て検出可能であることを裏付け） |
| `rice/encode.rs:74` | `input_val - 4` → `input_val + 4`（bit_length=4、option[2]==0、入力4〜7の2bit残り部分） | **等価変異と判明**（後述） |

4/5は、自動実行では「未検出」と分類されたにもかかわらず、`--include-ignored`込みの実際の試験一式では確実に検出された——自動実行の655件という数字は、試験群自体の弱さではなく、計測ツールの制約（相対パス解決の問題）による過小評価であることを裏付けた。

**等価変異（equivalent mutant）の発見**: 唯一検出されなかった`rice/encode.rs:74`は、`bits_write`が指定ビット幅の下位ビットしか出力しない（`(bit >> i) & 1`をビット幅分だけ書き出す）ため、`input_val`が4〜7の範囲では`input_val - 4`（0〜3）と`input_val + 4`（8〜11）が2進数下位2bitで完全に一致する（例: 4→0か8、いずれも下位2bit `00`）。つまりこの変異は出力バイト列を一切変えない**数学的に等価な変異**であり、どんな試験を用意しても原理的に検出不可能——試験群の弱さではなく、ミューテーションテストの既知の限界（等価変異はどの分類手法でも一定割合発生する）に該当する。

**結論**: 手動サンプリングで裏付けた限りにおいて、本プロジェクトの共有ベクタ試験群（`--include-ignored`込み）は意図的に注入したバグを高い確率で検出する実効的な検出力を持つことを確認した。自動化された大規模ミューテーションテスト（全1131変異を`--include-ignored`込みで機械的に実行）は、`cargo-mutants`の相対パス解決の制約により今回は実施できなかった——CIの`verify/run_unit_vectors.py`と同じ要領でテストベクタを事前生成した上で`--in-place`かつ安全に自動実行する仕組みを別途整備すれば可能だが、本ラウンドの費用対効果としては手動サンプリングによる裏付けで十分と判断した。

### 3.10 クロスアーキテクチャ検証（Apple Silicon/ARM64）でのFMA縮約による新規の乖離を発見・修正

COMPATIBILITY_REPORT.ja.mdの「厳しい意見」レビューで、「全ての測定が単一のビルド環境（x86-64ベースライン、FMA無効）でしか行われておらず、FMA有効なターゲットでの再現性は未検証」という限界を指摘していた（COMPATIBILITY_REPORT.ja.md §5参照）。ユーザがApple M5搭載Macで実際にこの検証を行い、**真の新規バグを発見した**。

**症状**: macOS・Apple clang・ARM64 (Apple Silicon)でビルドし`verify/run_compat.sh --include-slow`を実行したところ、178ケース中56件がFAIL——**float DWT（`-t 0`）を使う全ケースが例外なく失敗**（整数DWTは全PASS）。しかも`baseline_256_t0_r0`等では**エンコード段階の`.bpe`出力自体**が一致せず、多くのケースで`encode .bpe`のMISMATCHが先頭付近のバイト（例: char 69, char 124）から発生していた——§3.3で扱った「稀に1画素だけずれる」規模とは全く異なる、系統的かつ大規模な乖離だった。

**原因**: `original/source/makefile`の`BPEFLAGS`には`-ffp-contract`の指定が一切なく、コンパイラのFP_CONTRACT既定値（`a*b+c`を1回の丸めで計算するFMA命令への融合を許すかどうか）に任されていた。x86-64ベースラインターゲットにはFMA命令自体が存在しないため、この既定値がどうであれ融合は実行され得ず、事実上無効化されていた——§3.3で「`-ffp-contract=off`を試したが無関係だった」と結論した実験は、まさにこの「ハードウェア側にFMA命令がなかったから無関係だった」ことの裏返しである。一方ARM64（Apple Siliconを含む）はFMA命令がベースISAに標準搭載されており、同じ既定値の下で実際に融合が発生する。bpe-rs側は`f32`/`f64`の`*`・`+`演算子を常に分離したまま計算し自動融合しない（Rustの基本算術演算子の仕様上の保証）ため、C側でFMA融合が発生する環境では、**エンコード側（`forwardf97f`、純粋な`float`演算のみで§3.3の型昇格問題とは無関係）ですら**Rustと結果が食い違う。

**修正**: `BPEFLAGS`・`BPEFLAGS_TRACE`双方に`-ffp-contract=off`を追加し、全ターゲットでIEEE754準拠の演算子単位の丸め（FMA融合なし）を強制するようMakefileを修正。これによりCの浮動小数点演算セマンティクスが、ハードウェア依存の偶然ではなく、明示的にRust側の非融合セマンティクスと一致する設計になった。

**検証結果**: 修正後、報告元のMac（Apple M5・ARM64・macOS）で178/178 PASSを確認。Linux側でも再ビルド・全試験（`run_compat.sh --include-slow`・`run_unit_vectors.py`）を再実行し、178/178 PASS・回帰なしを確認した（元々FMA命令が到達不能なため、Linux側ではこの変更は意味論上no-opのはず、という予想を実測でも裏付けた）。

**含意**: この発見は、COMPATIBILITY_REPORT.ja.mdが自ら指摘していた「単一環境でしか検証していない」という限界（COMPATIBILITY_REPORT.ja.md §5）を、実際にユーザが別環境で検証したことで解消した——同時に、その限界の指摘自体が的確だったことも証明した（現に新しいバグが見つかったため）。§3.3の1ULP問題とは異なる、別のコンパイラ設定に起因する乖離であり、こちらは「Rustが仕様上決して行わない最適化（FMA融合）をCが暗黙に行い得る」という、型昇格の問題とは独立したクラスのリスクだったことも明確になった。

## 4. カバレッジ評価（Cコード網羅率）

COMPATIBILITY_REPORT.ja.md §2.1・§2.2 の試験群が、C参照実装の実コードのどれだけの範囲を実際に踏んでいるかを `gcov` で計測した（コアアルゴリズム15ファイル対象。CLI引数解析の `main.c`、サードパーティ製 `getopt.c`、エラー文言表の `errorhandle.c` は対象外）。

**→ 現状の数値だけ知りたい場合は、下にある「最終結果」「到達可能性内訳」「分岐到達可能性内訳」の3つの表へ。それ以外の節は調査の経緯（読み飛ばし可）。**

### 計測方法の訂正（`gcov` の `.gcda` マージが壊れていた問題）

これまでの計測は、`main.c` を含む `bpe` 本体と、各関数直接呼び出しジェネレータ（`gen_rice_vectors.c` 等）を、共有する `.c` ファイル（例: `DC_EnDeCoding.c`）ごとに**別々に `gcc --coverage -c` で再コンパイル**してからリンクしていた。この方式では、後からリンクした側の `--coverage` ビルドが前段の実行で蓄積された `.gcda` を正しくマージせず、値が静かに過小集計されることが判明した。

発見の経緯: `AdjustOutPut` の1024ケース直接呼び出しジェネレータ（§3.5）を既存の計測ビルドに追加リンクしても `AdjustOutput.c` のカバレッジが小数点以下まで一切変化しなかった。同じジェネレータを、依存する `.c` ファイルを一度もコンパイルし直さない**独立したビルド**で実行したところ、`AdjustOutput.c` 単体で行95.70%（§3.6で述べる値多様化前の基準値と一致）という、既存の計測値とは全く違う結果が出た。これは「ジェネレータがカバレッジを増やせていない」のではなく「計測方法自体が壊れていた」ことを意味する。

修正: 全ての対象 `.c` ファイルを**一度だけ** `gcc -O0 --coverage -c` でコンパイルし、`bpe` 本体・6個のジェネレータいずれも、この共有オブジェクトファイル群に対して（再コンパイルなしで）`gcc --coverage <objs> -o <bin>` でリンクする方式に改めた（リンク時にも `--coverage` が必要 — `__gcov_init`/`__gcov_exit` 等 `libgcov` のシンボル解決に要る）。この方式で全ビルドの `.gcda` が単一ディレクトリで正しくマージされることを確認した。**以下の数値は全てこの訂正後の方式で再計測したもの。** 過去のラウンドで報告していたコア15ファイル全体の集計値（行75.96%・分岐66.26%）は、複数バイナリを合算する過程で`.gcda`マージが壊れていたことに起因する過小集計であり、本訂正により無効・本節の数値に置き換える。なお`AdjustOutput.c`単体の行65.02%・分岐56.60%（§3.5適用前、`bpe`本体単独でのマージ不要な計測）はこのマージ不整合の影響を受けないため無効化の対象ではなく、後述（§4「最終結果」）の§3.5改善量の起点として引き続き有効な数値である。

### AC側関数のカバレッジ拡充

`AC_BitPlaneCoding.c`（AC係数のビットプレーン符号化/復号）が全ファイル中最も行カバレッジが低かった（79.92%/71.88%）ため、`gcov` の注釈出力で未実行行を特定した。主な原因は、`ACDepthEncoder`/`ACDepthDecoder` が AC係数の最大振幅から導かれる `N`（`BitDepthAC_5Bits` のビット長）で分岐すること、および `BitDepthAC_5Bits==1` の場合は `ACDepthEncoder`/`Decoder` 自体を迂回する専用の1ビット面パスがあることだった。既存の baseline/checkerboard/noise 画像はいずれも `N==3` か `N==4`（`BitDepthAC_5Bits` 4〜15）にしか収まらず、`N==2`・`N==5`・`BitDepthAC_5Bits==1` の分岐を一切踏んでいなかった。

`BitDepthAC_5Bits` は実行時にしか分からない（AC係数最大振幅から動的に決まる）ため、狙った値になる入力を**実測で**探索した：ヘッダを読んで `BitDepthAC_5Bits` を出力する小さな診断ツールを作り、候補画像を何度も符号化して値を確認しながら絞り込んだ結果、64×64画像で「ほぼ一様な画素の中に1画素だけ+1のズレを最後尾ピクセルに置く」と `BitDepthAC_5Bits==1`、「同じズレを内部ピクセルに置く」と `==2`、「16bit画素で振幅最大の市松模様（period=1）」で `==17`（`N==5`）になることを確認し、これを `verify/gen_vectors.py` の `ac_depth1_64`/`ac_depth2_64`/`ac_depth5_16bit_64` として固定した。

これらの画像をロスレス（フルデコード）と、セグメントサイズ16でのレート掃引の両方で `verify/run_compat.sh` に追加した結果、`AC_BitPlaneCoding.c` は行79.92%→86.07%・分岐71.88%→80.73%に、`BPEBlockCoding.c`（AC符号化が呼ぶブロックスキャン処理）も行93.96%→94.86%・分岐84.20%→85.75%に伸びた。副産物として、同じセグメントサイズ16のレート掃引が `DC_EnDeCoding.c`（DC側の同種のN依存分岐・レート制限打ち切り分岐）も行83.04%→88.60%・分岐70.73%→80.08%まで押し上げた（DC側は今回明示的に狙ったわけではないが、AC側と対になる構造を持つため、同じ入力群が両方を同時に伸ばした）。

### `StagesCodingGaggles.c` のカバレッジ拡充（セグメントサイズの多様化）

コア15ファイル中、未実行行数が絶対値で最大だったのが `StagesCodingGaggles.c`（85.93%/81.56%、583行中82行未実行）。中身を読むと、`StagesDeCodingGaggles1/2/3` はデコード側でステージ1/2/3それぞれの象限再帰木を辿りながらシンボルを復号する処理で、各所（オプション読み取り直後・値読み取り直後・符号ビット読み取り直後など）に「ちょうど今レート制限に達したなら、その具体的な位置（ブロック番号・`X/Y_LocationStopDecoding`）を記録して即座に抜ける」という分岐が数十箇所埋め込まれている——これはまさに `AdjustOutPut` への入力（レート制限がどこで止まったか）を作っている側のコードである。

未実行行の大半はこの「特定のチェックポイントでちょうど打ち切られる」パターンで、どのチェックポイントが実際に踏まれるかはセグメントの残りバイト予算がどこで尽きるかに依存する。これまでの掃引は全てセグメントサイズ64固定でレートだけを変えていたため、**セグメントサイズ自体を変える**という次元が未検証だった。試したところ、セグメントサイズを変えるだけでレートを増やすよりずっと効率よく新しいチェックポイントに到達することを確認した（`SEGMENT_S_MIN`（global.h）が16のため、16〜48の5段階 × レート4段階 × 3画像 × 整数DWTのみ、計60ケースを追加）。float DWT（`-t 0`）も同じ格子で試したところ、当初は §3.3 の1ULP残存差と同一の signature が高頻度（noise_256だけで20combo中11件）で再現し、この掃引から除外していた。その後 §3.3 の根本原因を特定・修正したため、現在は同じ格子で試しても不一致は出ない。ただし `StagesCodingGaggles.c` のカバレッジ目的自体は整数DWTだけで十分達成できるため、掃引は意図的に整数DWTのみに留めている。

結果、`StagesCodingGaggles.c` は行85.93%→88.85%・分岐81.56%→83.69%まで伸びた。副産物として `ricecoding.c`（行92.99%→95.15%・分岐88.36%→90.09%）と `AdjustOutput.c`（行96.93%→97.35%・分岐84.46%→85.16%）もわずかに伸びた。残る未実行行（65行）は、`StageStop_2Bits` 常時3固定という別のCLI到達不能フィールド（後述）・パターン長が構造上4になり得ない防御的分岐・デバッグ用の無害なno-opスキャフォールドなどの確定デッドコードと、依然として特定タイミングに依存する残りのチェックポイント（約54行、`AdjustOutPut`の残存分と同種の性質）に分かれる。

### `DC_EnDeCoding.c` のカバレッジ拡充（N境界・負のMax_DC）

AC側のN境界（前節）を修正した際、DC側にも同型の未踏分岐（DC entropy coderのN==2/N<=4分岐）が残っていることが判明した。ただしDC側の`N`はAC側と計算式が異なる（`N = max(BitDepthDC_5Bits - QuantizationFactorQ, 1)`で、`BitDepthDC_5Bits`自体がビット長のビット長ではなく生の値）ため、AC側と同じ「1画素だけずらす」手法は使えず、実測での再探索が必要だった。

診断ツールで調べたところ、`BitDepthDC_5Bits`は画素値の絶対値（おおよそ画素平均×8）に連動しており、通常の自然画像は12〜13になる。ほぼ真っ黒に近い定数画像（値1・2）を作り、それぞれ適切なロスレス/非可逆レートと組み合わせたところ、`N==2`・`N<=4`の両分岐を踏めることを確認した（`dc_depth_n2_64`/`dc_depth_n4_64`）。

さらに、`BitDepthDC_5Bits`計算には符号付き画像でMax_DCが負になる場合の別経路（ちょうど2のべき乗になる場合の補正を含む）があり、これも未踏だった。符号付きの定数画像（値が2のべき乗の負数、例: -128）と、背景+20に対し8×8ブロック1個だけ-120にした画像（`dc_negpow_64`/`dc_mixed_sign_64`）を組み合わせることで、この経路の全分岐を踏めることを実測で確認した。

結果、`DC_EnDeCoding.c` は行83.04%→93.27%・分岐70.73%→86.18%まで伸びた。残る23行はほぼ全て確定デッドコード（`OptDCSelect==FALSE`のヒューリスティック分岐16行・防御的`ErrorMsg`4行）で、当時未解明のまま残っていた`QuantizationFactorQ_prime`の特定条件分岐2行と`return k;`1行は、後の残存ギャップ解消ラウンド（後述）で決着した——前者2行は`dc_qprime_lo_64`という新しい画像で実際にカバーし、後者1行は`DCEntropyDecoder`が`BPE_OK`以外を返す経路自体が存在しない（唯一のエラー分岐は`ErrorMsg`が`exit()`するため）ことを確認し確定デッドコードと証明、`DC_EnDeCoding.c`は実効カバレッジ100%に到達した。

### `AdjustOutput.c` の値多様化（詳細は§3.6）

`AdjustOutput.c`は§3.5の構造的網羅（1024通り）だけではカバレッジが97.35%止まりだった（95行未実行）。§3.6で発見した通り、未実行行のほぼ全ては符号依存分岐対の片側で、テスト値の符号バリエーション不足が原因だった。`variant`（符号反転・位相シフト）を導入し1024→6144通りに拡張、加えて`b_DC`計算の3通り目のケースを追加した結果、行97.35%→99.55%・分岐85.16%→87.15%まで伸びた。残る16行は制御フロー解析により確定デッドコード（`(m<4)&&(n<4)`という、到達時点で既に真であることが保証された冗長ガードの`else`節）と判明し、実効カバレッジ100%に達した。

### 残存ギャップの解消（`StagesCodingGaggles.c`・AC/DC側・`BPEBlockCoding.c`・`PatternCoding.c`の直接呼び出しハーネス）

前節までで実効カバレッジは99.00%（残存70行）に達していたが、「`AdjustOutPut`ほど数が多くないため費用対効果が低い」として見送っていたこの70行についても、`AdjustOutPut`と同じ「C参照実装のオブジェクトコードに直接リンクして関数を単体駆動する」手法を適用し、大部分を解消した。

**`StagesCodingGaggles.c`（54行の停止位置チェックポイント）**: `StagesDeCodingGaggles1/2/3`を直接呼び出し、実ファイルによる合成ビットストリームと`DecodingAllowedBitsSizeInSegment`（ビット単位の予算）を0から150まで全数掃引する`verify/c_unit_tests/gen_stages_gaggles_coverage.c`を新設した。`BitsRead`（bitsIO.c）は「ちょうどこの1ビットで予算超過」を検知した瞬間に`RateReached`を立てるため、予算を1ビットずつ変えて関数を素の状態から呼び直せば、どの読み出しの直後で打ち切られるかを機械的に全通り再現できる——`TypeP`/`TranD`/`TranGi`（各3bit、計512通り）の構造的組み合わせとDWTType・カスタム重み閾値・3種のビットパターンを掛け合わせた掃引を実行した結果、54行中23行が新たにカバーされた。残る31行を制御フロー解析したところ、**全て確定デッドコードと証明できた**（詳細は後述）。

**`DC_EnDeCoding.c`の`QuantizationFactorQ_prime = BitDepthDC - 3`分岐（2行）**: `BitDepthDC > 3`かつ`BitDepthDC - (1+(BitDepthAC>>1)) <= 1`という条件は、AC/DC双方の深度が連動しがちな画像（フラット画像・チェッカーボードいずれも試したが、両者の深度差が常に2で固定されてしまい条件を満たせなかった）では踏めなかった。診断ビルドで多数の画像パターンの`BitDepthDC`/`BitDepthAC`を実測した結果、**低い一定背景値（2）に、8×8ブロックごとに1画素だけ振幅50の孤立バンプを置く**パターンで両者を切り離せることを発見し（`dc_qprime_lo_64`、`verify/gen_vectors.py`の`sparse_bump_per_block`）、エンコード・デコード両方でこの分岐を新たにカバーした。

**`PatternCoding.c`の`RefBitsDe`末尾チェックポイント（2行）**: `if(BitPlaneStopDecoding!=-1 && RateReached==TRUE){BlockNoStopDecoding=BlockSeq;}`という、他のチェックポイントと違い`!LocationFind`のガードを持たない行末の記録処理。直接呼び出しで`RateReached`/`BitPlaneStopDecoding`/`LocationFind`を「前のビットプレーンで停止位置が既に確定した状態」を模した値に初期化し、当該BlockSeqの`RefineBits`系フィールドを全てゼロ（未処理）にして呼び出したところ、正常にこの行へ到達しループ全体を通過することを確認した——`AdjustOutPut`の停止位置とは異なる種類の「同一セグメント内で後続ビットプレーンの呼び出しが続く」という状態を素の関数呼び出しで再現できた例。

**`AC_BitPlaneCoding.c`の`ACGaggleDecoding`内`RateReached`早期return（1行）**: ガードなしの単純な`if(RateReached==TRUE)return;`で、`DecodingAllowedBitsSizeInSegment`をビット単位で0から40まで掃引するだけで即座に到達した。

これらの直接呼び出しジェネレータは`verify/c_unit_tests/gen_stages_gaggles_coverage.c`・`gen_residual_gaps_coverage.c`としてカバレッジ計測ビルドにのみ組み込んでいる（Rust側の対応する検証がなく、出力ファイルも生成しないため、`verify/run_unit_vectors.py`の全数検証スイートには含めていない——目的はあくまでC参照実装側のガバレッジ計測であり、C/Rustの突き合わせは他の観点で既に確保されている）。

### 到達可能性の再分類：新たに証明された確定デッドコードのパターン

上記の直接呼び出し掃引を徹底しても最後まで残った行を制御フロー解析した結果、**「タイミング依存の残存ギャップ」に分類していた行の大半が、実は確定的に到達不能なデッドコードだった**と判明した。新たに見つかったのは以下の3パターン（いずれも`AdjustOutput.c`で見つかった「自己完結的な冗長ガード」パターンの変種）。

1. **「より狭いチェックポイントが必ず先に横取りする」パターン**（`StagesCodingGaggles.c`、30行）: `StagesDeCodingGaggles1/2/3`内の`RateReached`はビット読み出し1回ごとに`bitsIO.c`の`BitsRead`だけが設定し、設定と同時に`BitPlaneStopDecoding`も必ず更新される。3関数を読解すると、**全てのビット読み出し直後には必ずそのビット読み出し専用のチェックポイント（`!LocationFind`込み）が置かれている**ことが分かった。つまり`RateReached`が新たに真になる瞬間は必ずその直後の最も近いチェックポイントで捕捉され、`LocationFind`が真になって以降は後続のチェックポイントは（同じガード条件のため）二度と真になれない。したがって、if/elseの片方（読み出しなし）を通った後にある「ループの終わりの無条件チェック」のような、より広いスコープのチェックポイントは、それより先に横取りする狭いチェックポイントが必ず存在するため、構造的に到達不能——`gdb`でチェックポイント条件を外部から強制的に真にして検証したところ、実際に常に最初の（より狭い）チェックポイントで返ることを確認した。
2. **「入力先の条件と矛盾する分岐」パターン**（`StagesCodingGaggles.c`、4行）: `StagesDeCodingGaggles2`/`3`内の一部チェックポイントは、`if(hit_history & 0x4 == 0){...} else if(hit_history & 0x2 == 0){...} else{...}`という3方向のうち`else`側でのみ到達する座標を記録するが、この`else`（=両ビットとも既に立っている）は、そのチェックポイント自体に入るための前提条件（「まだ見つかっていない要素が2つ以上ある」）と数学的に両立しない——2つのビットが両方立っていれば未発見要素は最大1つしかなく、入室条件を満たせない。
3. **「RateReachedとBitPlaneStopDecodingは常に同時にしか変化しない」パターン**（`PatternCoding.c`の`StagesDeCoding`、4行）: `RateReached=TRUE`を設定する箇所はソース全体で`bitsIO.c`の1箇所のみで、その同じ箇所が必ず同時に`BitPlaneStopDecoding`も`-1`から書き換える。ところが`StagesDeCoding`内の一部チェックポイントは`BitPlaneStopDecoding`を見ずに`RateReached`だけを判定しており、この組み合わせ（`RateReached==TRUE`かつ`BitPlaneStopDecoding==-1`）はプログラム全体の不変条件から見て発生し得ない——`gdb`で意図的にこの矛盾する状態を外部から作って直接呼び出したところ、実際にそのチェックポイントに到達することは確認できたが、これは実際のデコード処理では決して起こらない人為的な状態であり、`AdjustOutput.c`で既に確立している「実際のプログラム不変条件を尊重する」基準に従えばデッドコードと判定するのが妥当と判断した。
4. **「連続する2つの独立条件のANDが、後段で片方だけを再チェックする」パターン**（`BPEBlockCoding.c`、2行）: `BlockScanEncode`内の`continue`は`(A>=BitPlane) && (B>=BitPlane)`という2条件のANDで判定されるが、その少し後に`A>=BitPlane`単独で`goto`する分岐があり、さらにその先で`B>=BitPlane`単独の`continue`がある。後者2つの分岐に両方到達するには`A>=BitPlane`と`B>=BitPlane`が両方真である必要があるが、それは最初のAND条件と完全に同一であり、最初の`continue`で既に処理済みのはずである——矛盾のため後段の`continue`は到達不能。

これらの証明により、`StagesCodingGaggles.c`・`DC_EnDeCoding.c`・`PatternCoding.c`は実効カバレッジ100%に到達した。`AC_BitPlaneCoding.c`・`BPEBlockCoding.c`に残っていた最後の1行ずつも、追加の制御フロー解析（`ACBpeEncoding`が呼び出す`BlockScanEncode`が出力関数を一切持たないこと、`BPEBlockCoding.c`のシンボルスロット`si`の消費順序）により確定デッドコードと証明でき（詳細は後述の各ファイルの記述）、コア15ファイル全体が実効カバレッジ100%に到達した。

### 最終結果

| 対象 | 行カバレッジ | 分岐カバレッジ（両方向） |
|---|---|---|
| コア15ファイル全体 | **95.14%**（6968/7324行） | **86.79%**（5833/6721、直前86.77%） |
| `AdjustOutput.c` のみ | 99.55%（変化なし） | 87.15%（変化なし） |
| `AC_BitPlaneCoding.c` のみ | **86.48%**（211/244行、直前86.07%） | **82.81%**（159/192、直前80.73%） |
| `DC_EnDeCoding.c` のみ | **93.86%**（321/342行、直前93.27%） | **87.80%**（216/246、直前86.18%） |
| `PatternCoding.c` のみ | **96.59%**（368/381行、直前96.06%） | **85.77%**（235/274、直前85.40%、§4「分岐到達可能性内訳」項目8の追加ラウンドで328行目の偽方向を新規カバー） |
| `StagesCodingGaggles.c` のみ | **92.80%**（541/583行、直前88.85%） | **87.06%**（491/564、直前83.69%） |
| `BPEBlockCoding.c` のみ | 94.86%（変化なし、314/331行） | **86.27%**（333/386、直前86.27%） |
| `bitsIO.c` のみ | **77.38%**（65/84行、直前75.00%、副産物） | **76.19%**（32/42、直前73.81%） |
| `bpe_encoder.c` のみ | **76.47%**（156/204行、直前75.98%、副産物） | **71.43%**（90/126、直前70.63%） |
| `bpe_decoder.c` のみ | 87.87%（変化なし） | 79.76%（変化なし） |
| `header.c` のみ | 78.38%（変化なし、デッドコード確定済み） | 55.56%（変化なし） |

`AdjustOutput.c` は §3.5・§3.6 の直接呼び出し・値多様化で既に実効カバレッジ100%に到達済み（変化なし）。今回の残存ギャップ解消（直前の節）で`StagesCodingGaggles.c`・`DC_EnDeCoding.c`・`PatternCoding.c`が新たに実効カバレッジ100%へ、`AC_BitPlaneCoding.c`・`BPEBlockCoding.c`もraw行カバレッジはそれぞれ86.48%・94.86%まで伸び、残った最後の1行ずつも追加の制御フロー解析で確定デッドコードと証明できたため、コア15ファイル全部が実効カバレッジ100%に到達した。`bitsIO.c`・`bpe_encoder.c`は今回追加した`dc_qprime_lo_64`（16bit・低背景値+孤立バンプという他のケースにない画素パターン）の副産物としてわずかに伸びた——具体的にどの行かは追っていないが、両ファイルとも従来通り残存行は全て確定デッドコードの範囲に収まっている（実効カバレッジ100%を維持）。`bpe_decoder.c`/`header.c`はいずれのハーネスの対象外のため変化なし。

ファイル別の内訳:

| ファイル | 行 | 分岐(両方向) |
|---|---|---|
| AdjustOutput.c | 99.55% | 87.15% |
| CoeffGroup.c | 100.00% | 100.00% |
| lifting_97f.c | 96.47% | 90.48% |
| PatternCoding.c | 96.59% | 85.77% |
| lifting_97M.c | 96.03% | 92.42% |
| ricecoding.c | 95.15% | 90.52% |
| BPEBlockCoding.c | 94.86% | 86.27% |
| DC_EnDeCoding.c | 93.86% | 87.80% |
| StagesCodingGaggles.c | 92.80% | 87.06% |
| bpe_decoder.c | 87.87% | 79.76% |
| AC_BitPlaneCoding.c | 86.48% | 82.81% |
| waveletbpe.c | 82.32% | 88.19% |
| header.c | 78.38% | 55.56% |
| bpe_encoder.c | 76.47% | 71.43% |
| bitsIO.c | 77.38% | 76.19% |

### 到達可能性内訳（デッドコード率を除いた「実効カバレッジ」）

行カバレッジの絶対値だけでは「これ以上上げられるか」の判断がつかない。これまでの調査で確認した**確定デッドコード／エラーパス（CLIから構造的に到達不能、または意図的に対象外としたエラー処理）**と、**残存する到達可能ギャップ（タイミングや値の組み合わせを工夫すればまだ踏める可能性がある行）**を分離し、「到達可能な行のうちどれだけ踏めているか」＝実効カバレッジを算出した。

| ファイル | 総行数 | 実行済み | 未実行 | うち確定デッド/対象外 | うち到達可能ギャップ | 実効カバレッジ（実行済み/(総行数−デッド)） |
|---|---|---|---|---|---|---|
| CoeffGroup.c | 282 | 282 | 0 | 0 | 0 | **100.00%** |
| header.c | 259 | 203 | 56 | 56 | 0 | **100.00%** |
| bpe_decoder.c | 272 | 239 | 33 | 33 | 0 | **100.00%** |
| bitsIO.c | 84 | 65 | 19 | 19 | 0 | **100.00%** |
| bpe_encoder.c | 204 | 156 | 48 | 48 | 0 | **100.00%** |
| ricecoding.c | 371 | 353 | 18 | 18 | 0 | **100.00%** |
| lifting_97f.c | 85 | 82 | 3 | 3 | 0 | **100.00%** |
| lifting_97M.c | 126 | 121 | 5 | 5 | 0 | **100.00%** |
| DC_EnDeCoding.c | 342 | 321 | 21 | 21 | 0 | **100.00%** |
| PatternCoding.c | 381 | 368 | 13 | 13 | 0 | **100.00%** |
| AC_BitPlaneCoding.c | 244 | 211 | 33 | 33 | 0 | **100.00%** |
| waveletbpe.c | 181 | 149 | 32 | 32 | 0 | **100.00%** |
| BPEBlockCoding.c | 331 | 314 | 17 | 17 | 0 | **100.00%** |
| AdjustOutput.c | 3579 | 3563 | 16 | 16 | 0 | **100.00%** |
| StagesCodingGaggles.c | 583 | 541 | 42 | 42 | 0 | **100.00%** |
| **合計** | **7324** | **6968** | **356** | **356** | **0** | **100.00%**（6968 / (7324−356) = 6968/6968） |

**読み方**: 生カバレッジは95.14%で、確定デッドコード356行（全体の4.9%）を除いた「到達可能コード」6968行の**100.00%**を踏めている。最後まで未解明として残っていた2行（`AC_BitPlaneCoding.c`の`ACBpeEncoding`内`SegmentFull`早期return、`BPEBlockCoding.c`の`si++`ブックキーピング）も、直接呼び出しでの再現ではなく制御フロー解析（`BlockScanEncode`が`BitsOutput`を一切呼ばないため`SegmentFull`を変更し得ないこと、`si`の消費順序を`BitPlaneSymbolReset`の呼び出し規約まで遡って追跡したこと）により確定デッドコードと証明できた——具体的な調査経緯は後述の詳細な各ファイルの記述を参照。コア15ファイル全体で真に「テストの工夫では埋まらない」以外の理由で未踏な行は、もはや1行も残っていない。

`BPEBlockCoding.c` の残り5.14%（17行）のうち16行は確定デッドコードと判明した。最大のブロック（13行、357-376行目）は「`TypeCi[k]`の4子要素が全てヒット済みかどうか」を判定する `for(i=0;i<4;i++) if((TypeC << (1<<(3-i))) != 1) { p=0; break; }` というループが原因——`TypeC` は4bitフィールド（0-15）で、`int`昇格後 `i=0`（シフト量`1<<3=8`）の時点で `TypeC<<8` は0か256の倍数にしかならず**`1`と等しくなることは数学的にあり得ない**。つまりこのループは実データに関わらず必ず1回目の反復（`i=0`）で`p=0;break;`となり、`p==1`（全ヒット済み、洗練ビットのみ処理する`else`分岐）には構造的に到達しない——`&`と書くべきところを`<<`と誤記したタイプミスと見られるが、参照実装の挙動としてはこの通りであり、Rust側もこれに追随して互換性は保たれている（COMPATIBILITY_REPORT.ja.md §2.1の全ケースPASSで確認済み）。残る1行（`ErrorMsg(BPE_BLOCKSCAN_CODING_ERROR)`、既存の防御的コード）と合わせ14行が確定デッドコード。

さらに、当時「未解明」としていた149・238行目の`continue;`（`CustomWtHL1_2bits`等が絡む深いビットプレーンでの分岐）は、`BlockScanEncode`の制御フローを精査した結果、以下の理由で確定デッドコードと証明できた: この2行は`if(A>=BitPlane && B>=BitPlane){continue;}`という2条件ANDの`continue`の**少し後**にあり、間に`if(A>=BitPlane){goto CHECK_GC;}`という`A`単独の`goto`を挟んで、その先で`if(B>=BitPlane){continue;}`という`B`単独の`continue`（149・238行目）に到達する構造になっている。この`B`単独の`continue`に到達するには`A>=BitPlane`（`goto`のため）と`B>=BitPlane`（`continue`条件のため）が両方真である必要があるが、それは最初の`A&&B`条件と完全に同一であり、最初の`continue`で既にそのケースは処理済みのはず——矛盾のため後段の`continue`は構造的に到達不能。これはCustomWtの実際の値（`CustomWtFlag`が常にFALSEなため固定）に関わらず成り立つ純粋な論理的redundancyであり、`AdjustOutput.c`で見つかった自己完結的な冗長ガードパターンの一種。

当初「未解明」としていた残り1行（181行目、`si++;`——`TranB`が既に見つかっている状態でのシンボルインデックスの更新）も、`si`の消費順序を精査した結果、確定デッドコードと証明できた。この行は「(A)`TypeP`スキャン（49-97行目）が新規ヒットを記録し`si`をインクリメントした直後」または「(B)`TypeP`が全て既発見で`si`が未変更のまま」のいずれかの状態で到達するが、直前の行99-100（`if(SymbolsBlock[si].sym_len!=0)si++;`）が**全く同じ条件**を既にチェックして`si`を適切な位置まで進めている——ケース(A)ではその時点で新しい（`sym_len==0`の）スロットに進んでおり、ケース(B)では`si`が指すスロットは前のビットプレーンで`StagesEnCodingGaggles1/2/3`が`BitPlaneSymbolReset`で明示的にリセット済み（`sym_len==0`）である。つまり181行目に到達する時点で`SymbolsBlock[si].sym_len`は常に0——`AC_BitPlaneCoding.c`の`ACBpeEncoding`（後述）と同種の、「直前のチェックが同一条件を既に消費している」パターン。これにより`BPEBlockCoding.c`は実効カバレッジ100%に到達した。

### `header.c`・`bpe_decoder.c`・AC/DC側・`bitsIO.c`・`StagesCodingGaggles.c`の残存未カバー行はデッドコード／エラーパス／到達困難な打ち切りタイミングと確認

`gcov` の注釈出力を直接読み、実行されない行の周辺コードを確認したところ、以下はテストで踏めないのではなく **C参照実装自体がCLIからは絶対に到達できないコード（または意図的に対象外としたエラーパス、あるいは到達自体は理論上可能だが黒箱テストでは実質狙い撃ちできないタイミング依存の分岐）** だと判明した。

`header.c`:

- `CodewordLength_2Bits`（値1/2/3、コードワード長16/24/32bit）: `HeaderInilization` で常に `0` にハードコードされ、CLIのどのオプションからも変更されない。
- `CustomWtFlag`（ユーザー定義ウェーブレット重み）: 同様に常に `FALSE` にハードコード。
- `UseFill = TRUE` 分岐: `if (SegByteLimit_27Bits == 0) UseFill=FALSE; else UseFill=TRUE;` という判定が `HeaderInilization` 内で行われるが、この時点では `SegByteLimit_27Bits` はまだレート(`-r`)から計算される前の初期値 `0` — 参照実装自体の初期化順序の都合で、この分岐は構造的に到達不能。

`bpe_decoder.c`（残り12.13%、33行全て到達不能と確認 — 追加テストでの改善余地なし）:

- **`TransposeImg == TRANSPOSE` 分岐**（`DecodingOutputInteger`/`DecodingOutputFloating` 内、計18行）: エンコード側 `HeaderInilization`（header.c:56）が `TransposeImg` を常に `NOTRANSPOSE` にハードコードし、CLIのどのオプションからもこれを `TRANSPOSE` に変更する手段が存在しない（`main.c` に該当オプションなし）。デコード側自体はヘッダから正しく読み取っているが（`header.c:308`）、この参照実装自身のエンコーダが `TRANSPOSE` を書き出すことは構造的にない。
- **`TempCoeffOutput` 関数全体**（9行）: `DecoderEngine` 内の唯一の呼び出し箇所がコメントアウト済み（`//	TempCoeffOutput(...)`）——デバッグ用のダンプ関数で、現在は完全に無効化されている。
- **`SegmentBufferFlushDecoder` のフィル読み飛ばしループ**（3行、`UseFill == TRUE` がゲート）: 上記 `header.c` の指摘の通り、`UseFill` はエンコード側で常に `FALSE` のまま書き出されるため、このデコード側ループも参照実装の運用上は常に到達不能。
- **`ErrorMsg(BPE_FILE_ERROR)` の3箇所**（入力/出力ファイルの `fopen` 失敗パス）: 正常系の試験マトリクスの対象外としているエラーパス。`ErrorMsg` は `exit()` するため理論上は追加テストで踏めるが、C/Rustの出力バイト一致という本レポートの主眼からは外れるため見送った。

`AC_BitPlaneCoding.c`（実効カバレッジ100%）・`DC_EnDeCoding.c`（実効カバレッジ100%）— 両ファイルは同じ構造（DC側/AC側で並行実装）を持つため、同種の未到達パターンが両方に現れる:

- **`OptDCSelect`/`OptACSelect == FALSE` の「ヒューリスティック」符号選択分岐**（`ACGaggleEncoding`/`DCGaggleEncoding` 内、各16行）: `HeaderInilization`（header.c:47-48）が両フラグを常に `TRUE`（ブルートフォース最適選択）にハードコードし、CLIのどのオプションからも `FALSE` に変更する手段が存在しない。`header.c` の `CodewordLength_2Bits`/`CustomWtFlag` と同種の、参照実装自身のCLIが露出させていない設定項目。
- **AC側 `N > 5` を示す `ErrorMsg(BPE_DATA_ERROR)` 分岐**（`ACDepthEncoder`/`ACDepthDecoder`、計4行）: `N` は5bitヘッダフィールド `BitDepthAC_5Bits`（最大31）のビット長として計算されるため、数学的に `N` は最大でも5にしかならない（`31` の2進数は5桁）。この分岐は設計上の防御的コードであり、有効な入力からは構造的に到達不能。
- **`CheckUsefill`（AC_BitPlaneCoding.c、計10行）**: `bpe_decoder.c` の `SegmentBufferFlushDecoder` フィル読み飛ばしループと全く同じ構造・同じ原因（`UseFill` がエンコード側で常に `FALSE` のまま書き出されるため、対応するデコード側の読み飛ばし処理に到達しない）。
- **AC側 `ACBpeEncoding` 内の `BitPlaneStop_5Bits == BitPlane && Part2Flag == TRUE` 早期return（計2行）**: `BitPlaneStop_5Bits` はエンコード側で`header.c`初期化時の`0`から変化せず（変更するCLIオプションが存在しない）、一方でこのチェックが置かれているビットプレーンループは`BitPlane > 0`を条件に回るため`BitPlane`が0になることは決してない——`BitPlaneStop_5Bits(0) == BitPlane`は常に偽で、`Part2Flag`の値に関わらず到達不能。AC_BitPlaneCoding.cの確定デッドコード32行の内訳はヒューリスティック分岐16行・`N>5`4行・`CheckUsefill`10行・本項目2行で合計32行。
- **DC側 `N > 10` を示す `ErrorMsg(BPE_DATA_ERROR)` 分岐**（`DCEntropyDecoder`、AC側とは別原因）: DC側の `N`（`= max(BitDepthDC_5Bits − QuantizationFactorQ, 1)`）はAC側と違いビット幅による上限がないが、`QuantizationFactorQ_prime` の選定式（DC_EnDeCoding.c:530-537）が `BitDepthDC` の値に応じて `Q'` を `BitDepthDC−3` や `BitDepthDC−10` に設定するよう設計されており、結果として `N` は常に10以下に収まるよう構造的に保証されている（実測でも `BitDepthDC_5Bits` が19に達する画像でも `N` は10を超えなかった）。この保証がある限りこの分岐には到達しない。`DCEntropyEncoder`側（206-218行目）にはこの `else` 節自体が存在せず `Max_k`/`ID_Length` が初期値0のまま処理が続くが、同じ理由で `N>10` に到達しないため実害はない（この防御的`ErrorMsg`とファイル冒頭付近の類似ガード計2行に加え、`OptDCSelect==FALSE`のヒューリスティック分岐16行で、DC_EnDeCoding.cの確定デッドコード19行を占める）。
- **DC側 `DCEntropyDecoder` の `if((k=DCEntropyDecoder(...))!=BPE_OK) return k;`（`return k;`、1行）**: `DCEntropyDecoder`のソースを確認すると、返り値は関数末尾の`return BPE_OK;`一箇所のみで、`N>10`時の唯一の分岐は`ErrorMsg`を呼ぶだけであり、`ErrorMsg`自体が`exit()`するため制御はそこで終了しBPE_OK以外を返す経路自体が存在しない——呼び出し側のこのチェックは構造的に真になり得ない。DC_EnDeCoding.cの確定デッドコード20行はヒューリスティック16行・`N>10`系3行に本項目1行を加えて計20行。
- **DC側 `QuantizationFactorQ_prime = BitDepthDC - 3`（計2行、エンコード・デコード双方）**: `BitDepthDC > 3` かつ `BitDepthDC - (1+(BitDepthAC>>1)) <= 1` という条件は、フラットな画像やチェッカーボードのようにAC/DC深度が連動しがちな画像では常に両者の差が一定値に固定されてしまい満たせなかった。診断ビルドで多数のパターンを実測した結果、**低い一定背景値（2）に8×8ブロックごとに1画素だけ振幅50の孤立バンプを置く**パターン（`dc_qprime_lo_64`）でAC/DC深度を切り離せることを発見し、この分岐をカバーした（後述の残存ギャップ解消の節参照）。これによりDC_EnDeCoding.cは実効カバレッジ100%に到達した。
- **AC側 `ACBpeEncoding`/`ACGaggleDecoding` 内の `SegmentFull`/`RateReached` 早期return（計2行）**: このうち`ACGaggleDecoding`内の単純な`if(RateReached==TRUE)return;`（ガードなし）は、直接呼び出しでビット予算を0から40まで掃引するだけで即座にカバーできた（後述）。残る`ACBpeEncoding`内の`if(SegmentFull==TRUE)return;`（`BlockScanEncode`呼び出し直後、前後にも同型のチェックがある3つ中の中間）は、直接呼び出し＋`SegByteLimit_27Bits`掃引では想定した打ち切りタイミングを再現できなかったが、`BlockScanEncode`（BPEBlockCoding.c）のソース全体を`grep`した結果`BitsOutput`呼び出しが1件も存在しないと確認した——つまり`BlockScanEncode`は`PtrCoding->SegmentFull`を変更し得る手段を一切持たない純粋な走査関数である。したがって、この呼び出し直前のチェック（`if(SegmentFull==TRUE)return;`）が真でなければ、直後の同じチェックも常に偽のまま——両者は数学的に同一の判定を2回行っているにすぎず、後者は構造的に到達不能。これにより`AC_BitPlaneCoding.c`は実効カバレッジ100%に到達した。

`bitsIO.c`（残り19行、全て到達不能と確認 — 追加テストでの改善余地なし。従来21行だったが、後述の残存ギャップ解消ラウンドで追加した`dc_qprime_lo_64`テストが副産物として「セグメント末尾のフィルバイト読み飛ばしループ」2行を偶然カバーし、19行に減った）:

- **`OutputCodeWord` の `CodeWord_Length` 16/24/32bit 分岐**（計13行）: `header.c` の `CodewordLength_2Bits` と同一原因。`CodewordLength_2Bits` が常に `0` にハードコードされているため `Bits->CodeWord_Length` は常に8にしかならず、16/24/32bitのコードワード出力経路は構造的に到達不能。
- **`BitsRead` の `RateReached == TRUE` 早期return**（2行）: 全ソースを `grep` した結果、`RateReached = TRUE` と `SegmentFull = TRUE` は `bitsIO.c` 内の同一箇所（166-183行目）で必ず同時に設定され、両者のリセットも常に同時（`bpe_decoder.c` の `PtrCoding->SegmentFull = FALSE; PtrCoding->RateReached = FALSE;` が隣接する2行）。よって `RateReached == TRUE` の時点で `SegmentFull` も必ず `TRUE` であり、その手前の `SegmentFull == TRUE` チェック（138行目）が先に呼び出し元へ返すため、この分岐に到達する経路が存在しない。
- **`BitsOutput` の `length > 32` 分岐**（4行）: 全 `BitsOutput` 呼び出し箇所を確認したところ、固定長引数は最大27bit（`SegByteLimit_27Bits`）で、可変長引数（Rice風の「符号化済み先頭部」の長さ `(MappedDC/MappedAC >> min_k) + 1`）もこの参照実装がサポートする最大画素ビット深度16bitから決まる係数の実測レンジ（本レポートで16bit最大振幅市松模様を試した際も `BitDepthDC_5Bits` は19止まり、§4参照）を踏まえると32を超えることは実質的にない。理論上は`DCEntropyEncoder`のN>10時のフォールバック（`Max_k`/`ID_Length`が初期値の0のまま、208-218行目)という参照実装側の別の粗さはあるが、これはさらに稀な状況でも出力される値の量自体は増えず32bit超にはつながらない。100件超のテストケース（16bit最大振幅市松模様を含む）で一度も踏まれなかったことも状況証拠として確認済み。

`PatternCoding.c`（実効カバレッジ100%、381行中13行が確定デッドコード）: `case 0: return;`（sym_len=0の早期return、1行）と、4箇所の`switch`文それぞれの`default: ErrorMsg(BPE_PATTERNING_CODING_ERROR)`（計8行）は、いずれもパターンマッピングが扱うsym_len/type値が有効範囲に収まることを前提とした防御的コード。残り4行（`StagesDeCoding`内、計2箇所×2行）は、`RateReached`を設定する箇所がソース全体で`bitsIO.c`の1箇所のみであり、その箇所が必ず同時に`BitPlaneStopDecoding`も`-1`から書き換えることを確認した——ところがこの2箇所は`BitPlaneStopDecoding`を見ずに`RateReached`のみを判定しており、「`RateReached==TRUE`かつ`BitPlaneStopDecoding==-1`」という組み合わせはプログラム全体の不変条件上発生し得ない。なお`RefBitsDe`内の同種チェックポイント2行（`!LocationFind`のガードを持たない末尾の記録処理）は、直接呼び出しで「前のビットプレーンで停止位置が既に確定した状態」を再現することで実際にカバーできた（後述）。

`StagesCodingGaggles.c`（実効カバレッジ100%、583行中42行が確定デッドコード。従来は54行が停止位置チェックポイントとして残存ギャップ扱いだったが、直接呼び出しハーネスで23行をカバーし、残る31行を制御フロー解析で全て確定デッドコードと証明した）:

- **`StageStop_2Bits == 00`/`==01` での早期return**（`StagesEnCodingGaggles1`/`3`、計2行）: `header.c` の `StageStop_2Bits` は `HeaderInilization` で常に `3` にハードコードされ（`CodewordLength_2Bits`/`TransposeImg` と同様、CLIのどのオプションからも変更されない）、`00`/`01` になることはない。
- **`StagesEnCodingGaggles1` の `sym_len==4` 分岐と「無効な長さ」の `default: ErrorMsg`**（計4行）: このswitch文は `case 1: case 2: case 3:` のみを列挙しており、`sym_len==4` はそもそも `default:` 側（`ErrorMsg`）に落ちる——つまりswitchに入った時点で `sym_len` は1〜3のいずれかに限定され、その内側にある `sym_len==4` の分岐チェックは実行時に到達し得ない。`StagesEnCodingGaggles2`/`3` の同種の `ErrorMsg`（計2行）も、扱うシンボル種別（`TYPE_CI`/`TYPE_HIJ` 等）の構造上の子要素数（4個）を超える長さにはならないための同種の防御的コード。
- **`StagesDeCodingGaggles3` の `flag==FALSE` による `continue`**（1行）: `CustomWtHH1_2bits` はエンコード側で常に `0` にハードコードされており（`header.c`、他のCustomWt系フィールドとは異なりこの帯域だけ重み0）、`0 >= BitPlane` は `BitPlane`（1以上）に対して成立し得ないため、対応する `k==2` の反復で必ず `flag=TRUE` になり、3反復とも `continue` する（`flag`が`FALSE`のまま）ケースは構造的に発生しない。
- **`SegmentCount_8Bits==28 && BitPlane==13 && BlockSeq==10` 限定のno-opスキャフォールド**（`BlockSeq = BlockSeq;`、2行）: 有効な処理を何も行わない自己代入で、デバッガのブレークポイント用フックと見られる開発時の残骸。うち1行（条件式そのもの）は直接呼び出しハーネスの過程で偶然カバーされた。残り1行（自己代入の本体、`BlockSeq==10`を要求）は未カバーのままだが、無意味な自己代入である以上、意味のある検証にはならない。
- **残る30行の `SegmentFull`/`RateReached` 早期return（`!LocationFind`ガード付き）**: `StagesDeCodingGaggles1/2/3`のソースを精査した結果、**全てのビット読み出し（`BitsRead`呼び出し）の直後には必ずそのビット読み出し専用のチェックポイントが置かれている**ことを確認した。`RateReached`はビット読み出し1回ごとにしか変化し得ず、変化と同時に`BitPlaneStopDecoding`も更新される（bitsIO.c）。したがって`RateReached`が新たに真になる瞬間は必ずその直後の最も近い（＝最も条件の狭い）チェックポイントで捕捉され、`LocationFind`が真になった時点でそれより広いスコープの後続チェックポイント（if/elseの一方だけが読み出しを行い、他方は読み出しなしで合流する箇所の直後にある、無条件の「ループ末尾」チェックなど）は構造的に到達不能となる。`gdb`でチェックポイント条件を外部から強制的に真にして検証し、実際に常に最も近いチェックポイントで返ることを確認した。
- **残る4行の座標選択`else`分岐**: 一部のチェックポイントは`if(hit_history&0x4==0){...} else if(hit_history&0x2==0){...} else{...}`という3方向分岐で停止座標を記録するが、この`else`（両ビットとも既に立っている）に到達するには、そのチェックポイント自体に入るための前提条件（「まだ見つかっていない要素が2つ以上ある」）と矛盾する——2つのビットが両方立っていれば未発見要素は最大1つしかなく、入室条件を満たせない。

いずれもテストケースをどう工夫しても再現できない（手動でヘッダを改変しない限り）、本レポートの検証範囲の対象外である、または到達不能と証明済みのデッドコードであるため、今後の課題からは除外する。`AC_BitPlaneCoding.c`の`ACBpeEncoding`内`SegmentFull`早期return・`BPEBlockCoding.c`の`si++`も、追加の制御フロー解析により確定デッドコードと証明済み（詳細は前掲の各ファイルの記述を参照）。

### 分岐到達可能性内訳（「実効分岐カバレッジ」）

これまでの到達可能性内訳（§4前半）は**行**単位だった。行カバレッジは「その行が実行されたか」しか見ないため、`if(coeff > 0) ... else if(coeff < 0) ...`のような行は、`coeff`の符号が片方向にしか振れなくても行としては100%実行済みになる——実際、本プロジェクトで見つかったDPCMの符号バグ・`AdjustOutPut`の符号バグは、まさにこの「行は実行されるが分岐の片方向だけ踏まれていない」形で発現した。したがって行カバレッジとは独立に、分岐（条件の各方向）ごとの到達可能性も評価する意味がある。

**方法**: `gcov -b`が出力する分岐エントリ単位（1つの条件式につきtaken/not-takenの最大2エントリ）で、未踏（`taken 0%`または`never executed`）のものを対象に、以下の順で分類した。

1. **その行自体が既に確定デッド**（§4前半の行レベル内訳で確認済み）: 行が一度も実行されなければ、その行が持つ分岐エントリも自動的に未踏——新規の証明は不要。
2. **既知のデッドコードパターンに一致**: `CustomWtFlag`/`CustomWtH?[HL]?[123]_2bits`等のCLI固定フィールド、`N<=5`/`N<=10`系、`RateReached`/`LocationFind`の横取りチェーン、`TypeC<<(1<<...)`のタイプミス、`BlockScanEncode`が`BitsOutput`を呼ばない証明、`DCEntropyDecoder`が常に`BPE_OK`を返す証明などに加え、今回のラウンドで新たに確認した以下の系統を追加した：
   - **型による符号確定**: `diff_AC[i]`は`WORD16`（符号なし）由来の`MappedAC`から導出されるため符号が構造的に固定（`AC_BitPlaneCoding.c`）、`BitMaxAC`は`UCHAR8`（符号なし）なので`< 0`は不可能。
   - **直前行で既に確立済みのガードの重複チェック**: `bpe_decoder.c`の`EngImgFlg != TRUE`（直前行で`EngImgFlg==TRUE`なら`break`済み）、`bitsIO.c`の`SegmentFull != TRUE`（直前で`SegmentFull==TRUE`なら`return`済み）、`header.c`の`SegByteLimit_27Bits==0`（2行前に自分で0を代入済み）。
   - **エラーパス（既存の`fopen(`規約の拡張）**: `stat()`失敗（`bpe_encoder.c`の`res != 0`）、`ImageSize`/`ImageRead`のエラー分岐、`fopen`失敗（`infile == NULL`）——全て、検証対象の入力ファイルが常に存在・読み取り可能であることに起因する、既存の「異常系はスコープ外」方針の適用。
   - **`for(;BlockCounter<TotalBlocks;)`ループ条件の偽方向（`bpe_encoder.c`）**: ループ本体は`EngImgFlg==TRUE`時に必ず`break`で抜け、`EngImgFlg`は「次のセグメントで全ブロックを使い切る（またはオーバーする）」場合に必ずTRUEになるよう構成されているため、ループ条件自体が偽になって自然終了する経路は存在しない——`break`以外の出口がない構造的証明。
   - **エンコード/デコードの往復整合性**: `PatternCoding.c`の`DeMappingPattern`内の`for`ループ（`bit2_pattern`/`bit3_pattern`/`bit4_pattern_TypeCi`の逆引き）は、エンコード側の`PatternMapping`が同じテーブルで値を書き込んでいる以上、テーブルを引き切って見つからずにループを抜ける経路はあり得ない。同様に`PatternMapping`の`switch(sym_len)`の`case 0`と`default`は、`sym_len==0`のシンボルが生成されない・`sym_len`が4を超えないという構成上の保証から到達不能。
   - **自エンコーダの出力が仕様を満たす保証**: `header.c`の`S_20Bits < 16`は、本エンコーダが常に仕様適合な値を計算する構成である以上、満たされない側は到達不能。
   - `BitsPerPixel != 0`（`header.c`）は、`main.c`がデフォルト値1で初期化し、本検証のCLI利用方法では0を明示指定するテストケースが存在しないことに基づく（他の「CLIで固定される値」系パターンと同種）。
3. **`AdjustOutput.c`の符号ガード・トートロジー（486分岐）— 【注意: この486分岐は他の確定デッド判定と厳密さの水準が異なる】**: `AdjustOutPut`は`stoppedstage`（1〜4）×整数/浮動小数点表現の組み合わせごとに、`for(m..)for(n..){ if((m==0&&n==0)||(value==0)) continue; ...}`という形の、ループ先頭で0を除外するガードを持つブロックが12箇所ある（`grep`で確認済み：stoppedstage 1/2/3の整数・浮動小数点それぞれの対、およびstoppedstage 4相当の3対）。このガードで0が既に除外されているため、ブロック内のどこにあっても`if(value>0) ... else if(value<0) ...`という符号分岐の`else if`側は、届いた時点で「0でも正でもない」＝必ず真——`else if`自身の偽方向（＝`value>=0`のままここに来る）は数学的に到達不能。全12箇所のガード位置を直接ソースで確認し、この不変条件がそれぞれのスコープ全体に及ぶことを検証したが、**スコープ内で652回テキスト上出現する個々の`else if(value<0)`全てを1件ずつ再検証してはいない**——同一メカニズムの適用範囲を構造的に確認したという位置づけで、行レベルの個別証明ほどの厳密さは主張しない。958件の確定デッド分岐のうち過半数（486件）がこの1つの構造的議論に依拠している点に留意されたい。
4. **`switch`文が値域を数学的に尽くしている場合の暗黙default（16分岐）**: `switch(BitLength)`等、切り出し元の値が固定幅ビットフィールドやビットマスクで値域が限定され、かつその値域の全パターンに明示`case`がある場合、`gcov`はそれでも「どの`case`にも一致しない」暗黙の分岐を1本出力する——値域が数学的に尽くされている以上、この分岐は到達不能。構造的に（テキストパターンでなく）「`switch`行かつ最後の分岐のみ未踏、分岐総数2以上」で検出。
5. **`case`/`if`チェーンの消去法（domain elimination、新規追加）**: `CodingOptions`の`sym_len==4`（`sym_len`の定義域は{1,2,3,4}のみで1は既に`continue`済み、2・3は直前の`if`/`else if`で既に外れている）、`StagesEnCodingGaggles1`の`sym_len==3`（外側`switch(sym_len){case 1:2:3:}`で定義域が{1,2,3}に制限された上でのさらなる消去法）、`StagesEnCodingGaggles2/3`の`sym_len==4`（`else`側が`ErrorMsg`で、gcovの実行回数`#####`＝未実行と確認済み——`sym_len`はこの`type`集合では2/3/4のみが正当）。`PatternMapping`/`DeMappingPattern`の`case 4`（`type`が`TYPE_CI`/`TRAN_HI`/`TYPE_HIJ`のいずれでもない、という4本目の暗黙分岐——`sym_len==4`シンボルはこの3種類の`type`でしか生成されない）。
6. **推移律による同点判定チェーンの構造的証明（新規追加）**: `CodingOptions`のRice分割オプション選択（`bitsCounter_3Bits`/`bitsCounter_4Bits`の`<=`連鎖）で、後続の`else if`節の一部は、それ以前の節が失敗した事実そのものから数学的に強制されることを個別に証明した。例えば3-bitの第2節`(c0<=c1)&&(c0<=c2)`の`c0<=c2`側は、第1節`(c2<=c0)&&(c2<=c1)`が失敗した時点で（`c2>c0`または`c2>c1`のいずれであっても）`c0<=c1`と組み合わせると必ず`c0<=c2`が成り立つ——反例を作ろうとすると必ず矛盾する。同様の推移律を3-bitの第3節（`c1<=c2`側）・4-bitの第2節（`c0<=c3`側）でも確認した。
7. **`RefBitsDe`の打ち切り位置レコーダ（新規追加）**: `!PtrCoding->DecodingStopLocations.LocationFind`を含む条件の残り2箇所——このチェックに到達するのは「`RateReached`が*この呼び出し中に*偽から真へ遷移した」ときに限られる（区間の先頭にある`SegmentFull||RateReached`の早期`return`をまだ通過できていた＝呼び出し開始時点では偽だったはず）。`LocationFind`が既に真になり得るのは*別の*（より前の）呼び出しだけだが、その呼び出しで`RateReached`も真になっているはずで、それは次回呼び出し時の同じ早期`return`ガードに引っかかり、この深さまで到達する前に関数が終わってしまう——つまり「ここに到達しつつ`LocationFind`が既に真」という状況は構造的に発生し得ない。
8. **4-bit Rice分割オプション選択の残り5分岐 → 全て解消（1件は追加テストで到達、4件は構造的到達不能と証明）**: `CodingOptions`の4-bit側`else if`連鎖（`bitsCounter_4Bits[0..3]`が最小かを順に判定、316/322/328/334行目、それぞれOption 3/0/1/2に対応）について、328・330・334・335・336行目の5分岐が「反例を実データから狙い撃ちで作れず、数学的にも到達不能と証明できていない残存」として長く未解決だった。改めてこの`else if`連鎖の構造を突き詰めたところ、以下の通り全て解消できた。
   - **328行目（`bitsCounter_4Bits[1] <= bitsCounter_4Bits[0]`の偽方向）: 到達可能と判明、テスト追加で解消**。`bitsCounter_4Bits`はTYPE_CIのパターン値0を4個・パターン値6を3個混ぜる（`bit4_pattern_TypeCi`逆引きで`sym_val`は8×4・3×3）と`(25,26,24,28)`になり、316行目（`c3`が最小）・322行目（`c0`が最小）はどちらも成立しないのに`c1(26) > c0(25)`となって328行目の第一条件が偽になる——この方向は他のどの既存ケースでも作れていなかった。`verify/c_unit_tests/gen_codingoptions_coverage.c`にこのケースを追加し、実測（`gcov -b`、`PatternCoding.c`単体の直接呼び出しビルド）で328行目の偽方向が0%→実行済みに変わることを確認した。
   - **330行目（`bitsCounter_4Bits[1] <= bitsCounter_4Bits[3]`の偽方向）: 構造的に到達不能と証明**。328・329行目が真（`c1`が`c0`・`c2`以下）の状態で330行目が偽（`c1 > c3`）だと仮定すると、`c3 < c1 <= c0`および`c3 < c1 <= c2`が推移律で導かれ、`c3 <= c1`（仮定より自明）と合わせて316行目の3条件（`c3<=c1 && c3<=c0 && c3<=c2`）が全て満たされてしまう——しかし316行目は328行目より前に評価されるため、328行目に到達している時点で316行目は既に偽と確定している。矛盾。よって「328・329行目が真かつ330行目が偽」という状態は発生し得ない。
   - **334・335・336行目（`bitsCounter_4Bits[2]`が最小かの判定、3条件とも）: 構造的に到達不能と証明**。任意の4つの実数について「そのうち少なくとも1つは他の3つ以下（＝最小値、同値なら複数該当）」は恒真——316・322・328行目の条件（`c3`／`c0`／`c1`がそれぞれ最小候補か）が全て偽で334行目の判定に到達した時点で、この4択のうち残る「`c2`が最小候補」（334-336行目の論理積全体）は自動的に真であることが保証される。論理積が真である以上、334・335・336行目の個々の条件も全て真でなければならない——したがってこの3行の偽方向（`taken 0%`側）はどのような入力でも発生しようがない。

   合わせて、328行目の対（329行目の偽方向、`c1 <= c0`は真だが`c1 > c2`）も同じ単体ビルドで未踏だったため反例を探したところ、パターン値0×1・6×1（`sym_val={8,3}`）で到達可能と確認した。ただし329行目は元々この「5分岐」の一覧（328・330・334・335・336）に含まれておらず、既存の集計（実行済み241分岐）に暗黙に含まれていた（本レポート全体の集計はパイプライン試験・他のジェネレータも合算した状態のため、この直接呼び出しジェネレータ単体では踏めていなくても他の試験で既に踏まれていた可能性が高い）——新規のテストケース追加は不要と判断した。

| ファイル | 総分岐 | 実行済み | 確定デッド | 未分類残存 | 実効分岐カバレッジ |
|---|---|---|---|---|---|
| CoeffGroup.c | 232 | 232 | 0 | 0 | **100.00%** |
| waveletbpe.c | 129 | 112 | 17 | 0 | **100.00%** |
| bitsIO.c | 48 | 32 | 16 | 0 | **100.00%** |
| bpe_decoder.c | 176 | 141 | 35 | 0 | **100.00%** |
| bpe_encoder.c | 137 | 89 | 48 | 0 | **100.00%** |
| header.c | 75 | 30 | 45 | 0 | **100.00%** |
| lifting_97M.c | 69 | 61 | 8 | 0 | **100.00%** |
| lifting_97f.c | 45 | 38 | 7 | 0 | **100.00%** |
| ricecoding.c | 242 | 210 | 32 | 0 | **100.00%** |
| AC_BitPlaneCoding.c | 195 | 161 | 34 | 0 | **100.00%** |
| AdjustOutput.c | 3970 | 3460 | 510 | 0 | **100.00%** |
| BPEBlockCoding.c | 387 | 335 | 52 | 0 | **100.00%** |
| DC_EnDeCoding.c | 250 | 215 | 35 | 0 | **100.00%** |
| StagesCodingGaggles.c | 569 | 487 | 82 | 0 | **100.00%** |
| PatternCoding.c | 279 | 242 | 37 | 0 | **100.00%** |
| **合計** | **6803** | **5845** | **958** | **0** | **100.00%**（5845 / (6803−958) = 5845/5845） |

**読み方**: 確定デッド958分岐（行自体が確定デッド244＋既知パターン一致208＋`AdjustOutput.c`符号ガード486＋`switch`暗黙default16＋`PatternCoding.c`の4-bit選択チェーンの構造的到達不能4分岐、合計958）を除いた「到達可能な分岐」5845のうち**100.00%**を踏めている。上記8.の解析により`PatternCoding.c`が最後に残っていた1ファイルで、コア15ファイル全てが実効分岐カバレッジ100%に到達した。**ただし958件のうち486件（過半数）は上記項目3の`AdjustOutput.c`符号ガードという単一の構造的議論に依拠しており、他の項目ほど個別に厳密な証明ではない——「100.00%」という数字の内実として留意されたい。**

前回計測（99.25%・残存44）からの改善は、主に以下による：
- **`AdjustOutput.c`の打ち切り位置境界（3→0）**: `AdjustOutPut`を直接呼び出す既存ハーネスでは`X_LocationStopDecoding==1 && Y_LocationStopDecoding==0`の分岐が踏めていなかった原因を実際にソースへデバッグ出力を仕込んで特定——`stoppedstage==1`のとき、この特殊分岐に実際にたどり着けるのは`Y_LocationStopDecoding==0`のときだけだと構造的に証明できた（`m,n`が0または1しか取らない「親」象限では、`Y_Loc>=1`だと先行する`if`が必ず先に消費してしまうため）。同様に`BitPlaneStopDecoding==0`のチェックも、`char`型で`-1`（センチネル、既に上位ガードで除外済み）以外は`BitPlane-1`（>=0）か`0`リテラルしか代入されないという値域契約で解消。
- **AC/DC側`uncoded_flag`（5→0）**: 既存の`gen_residual_gaps_coverage.c`にビット列オール1（`0xFF`）のRice-decode直接呼び出しスイープを追加し、`min_k`が`ID_Length`桁全て1になるケース（エンコーダの「uncoded」モード相当）を発生させた。
- **`BPEBlockCoding.c`のビットプレーン有意性ヒストリ（7→0）**: `BlockScanEncode`用の新規直接呼び出しジェネレータ（`gen_blockscan_coverage.c`）を追加。振幅としきい値の組み合わせ2通り、`TranB`を同一`BitPlaneBits`に対して2回連続呼び出す（前のビットプレーンの呼び出しで書き込んだ`SymbolsBlock`の値が次の呼び出し時に持ち越される、という実際のエンコーダの挙動を再現）ことで解消。残り2本（`TypeCi`ループの`for`と`if(p==0)`）は、`TypeCi.TypeC << (1<<(3-i))`が整数の左シフトである以上`==1`には絶対にならないという既存の「タイプミス」パターンの直接的帰結として構造証明。
- **`StagesCodingGaggles.c`のビットプレーン有意性ヒストリ（5→0）**: `TranD&0x2==0`・`TranGi&0x2==0`の残り2分岐は、そこに到達するための前提条件（`counter>=2`、かつ直前の`if`で上位ビットが既に立っている）を組み合わせると値がちょうど1通りに強制されることを示す推移律の証明で解消。残り3本（`sym_len==3`/`==4`の分岐）は`StagesEnCodingGaggles1/2/3`用の新規直接呼び出しジェネレータ（`gen_stages_encode_coverage.c`）で、`TYPE_P`シンボルの長さ上限が3までしかない（`case 1:2:3`のみで`case 4`が無い）ことに気づかず一度`ErrorMsg`で落ちたのを修正した上で解消。
- **`PatternCoding.c`の型分岐・レート打ち切りチェーン（16→5）**: 新規ジェネレータ`gen_codingoptions_coverage.c`で`CodingOptions`/`PatternMapping`/`DeMappingPattern`を直接呼び出し、`sym_len`と`sym_val`（`PatternMapping`のテーブル変換を経て`sym_mapped_pattern`が狙った値になるよう逆算）を網羅的に振った。`RefBitsDe`のレート打ち切りチェーン8分岐は、当初「複数呼び出しにまたがる`LocationFind`の持ち越し」を狙って追加テストを書いたが効果がなく、上記7.の構造証明で「そもそもその方向は到達不能」と判明し解決した。

**この過程で得た教訓（今後の同種調査のために記録）**: (a) `gcov`の分岐ラベル`branch N taken X% (fallthrough)`は、複数の`if`/`else if`が連なる行では「その行の条件が真になって直後のコードへ進む」ことを指す（＝`else`側ではなく`if`側が`fallthrough`）——これを取り違えて一時的に誤った方向へ追加テストを書いてしまう手戻りがあった。実行回数（`gcov -c`の生カウント、または対応する本体行の実行回数との一致）で必ず裏付けを取ることを推奨する。(b) 同一の`.o`ファイルを異なる内容で繰り返しコンパイル・リンクし直すと、`.gcda`のチェックサム不一致で対象ファイルの累積カバレッジデータが破損しうる（本ラウンドで実際に発生し、全ハーネスをオブジェクト生成から1回だけ通しでビルド・実行し直して復旧した）——複数ラウンドに分けて計測を継続する場合は、途中の部分リビルドを避け、最終確認は必ずクリーンな1回通しの計測で行うこと。

## 7. 今後の課題

1. ~~`AdjustOutPut` のカバレッジ~~ → 対応済み。関数直接呼び出しハーネス（§3.5）により行65.02%→96.90%に改善、DWTType×stoppedstage×DC分岐×X/Y座標の全1024通りで不一致なしを確認。さらに残った95行を分析したところ符号バリエーション不足と判明し、テスト値に`variant`（符号反転・位相シフト）次元を追加して6144通りに拡張（§3.6）、行99.55%まで改善。最後の16行も制御フロー解析で確定デッドコード（冗長ガードの`else`節）と証明し、実効カバレッジ100%に到達した。
2. ~~§3.3 の1ULP残存差の頻度は3画像・28レート値で定量化済み（5/28件、noise_256が最多）。今後は原因不明の他の float DWT パス（特に**エンコード側**、まだ調べていない）でも起こり得るか確認する。~~ → 対応済み。根本原因（`inverse_lifting97f`がCの演算子単位の型変換規則を再現できていなかったこと）を特定し修正（§3.3）。修正後は5件全て含め不一致0件。エンコード側（`forwardf97f`）も確認したが、C参照実装の`LowPassFilter`/`HighPassFilter`が最初から`const float`宣言であるため同種のバグが生じる余地自体がないと確認した。
3. ~~AC側の他の関数（`ACGaggleEncoding`/`ACGaggleDecoding` 等、DPCM以外）~~ → 対応済み。AC係数振幅（`BitDepthAC_5Bits`）を1・2・17に制御した画像3種（ロスレス+セグメントサイズ16のレート掃引）を追加し、`AC_BitPlaneCoding.c`を行79.92%→86.07%・分岐71.88%→80.73%に改善（副産物として`DC_EnDeCoding.c`も83.04%→88.60%、`BPEBlockCoding.c`も93.96%→94.86%まで伸びた）。残存ギャップは`OptDCSelect`/`OptACSelect`常時TRUEによる到達不能なヒューリスティック分岐、`N>5`の数学的に到達不能な防御的`ErrorMsg`、`AdjustOutPut`のX/Y座標と同種のタイミング依存早期return数行——いずれも§4で確認済み。関数直接呼び出しハーネスまでは、残存規模に対して費用対効果が低いと判断し見送った。
4. ~~`bpe_decoder.c`・`header.c` の分岐カバレッジ~~ → 対応済み。`bpe_decoder.c` は画素フォーマット×DWTType×バイト順の組み合わせ拡充で行66.18%→87.87%・分岐50.00%→79.17%まで改善（この過程でRustのデコード側`-f`無視バグを発見・修正、§3.7）。残存ギャップは`TransposeImg`分岐・無効化済み関数・`UseFill`ゲートのループ・エラーパスといった到達不能/対象外コードと確認済み（§4）。`header.c` の残存ギャップも同様にCLIから到達不能なデッドコードと確認したため対応不要。
5. ~~`StagesCodingGaggles.c` のカバレッジ~~ → 対応済み。セグメントサイズ多様化掃引（行85.93%→88.85%）に続き、`StagesDeCodingGaggles1/2/3`の直接呼び出し＋ビット予算全数掃引ハーネスを追加し行88.85%→92.80%・分岐83.69%→87.06%に改善。残った31行も制御フロー解析で「より狭いチェックポイントが必ず先に横取りする」「座標選択の`else`分岐がその入室条件と矛盾する」の2パターンにより確定デッドコードと証明し、実効カバレッジ100%に到達した。
6. ~~`bpe_encoder.c`（75.98%/70.63%）の残存49行~~ → 調査完了、全行確定デッドコード/対象外エラーパスと確認。旧式のファイルサイズ推測フォールバック（42行、`ImageSize`内）は `main.c` が `ImageRows`/`ImageWidth` を常に既定値256で初期化する（`-w`/`-h`省略時も0にはならない）ため、CLIからは構造的に到達不能。残りはUseFillゲートのフィルループ（dead、既知原因）と `fopen`/戻り値チェックの防御的コード。これ以上の追加テストは不要と判断。
7. ~~DC側のN境界・負のMax_DC~~ → 対応済み。AC側と同様の手法（実測でのBitDepthDC制御画像探索）でDC_EnDeCoding.cを行83.04%→93.27%・分岐70.73%→86.18%に改善。当初未解明だった残り3行（`QuantizationFactorQ_prime`の特定条件×2、`return k;`×1）も、`dc_qprime_lo_64`画像と`DCEntropyDecoder`の返り値解析でそれぞれ解決し、実効カバレッジ100%に到達した。
8. ~~到達可能性の内訳（実効カバレッジ）の算出~~ → 対応済み。§4で初めて定量化し、確定デッドコード317行を除いた実効カバレッジ99.00%を算出した。これにより、生カバレッジ94.72%のうち「これ以上テストを工夫しても埋まらない領域」と「まだ伸びしろがある領域」を分離できた。当時の伸びしろ（70行、全体の約1.0%）は、`AdjustOutPut`と同じ直接呼び出しハーネス手法を`StagesCodingGaggles.c`・AC/DC側・`PatternCoding.c`にも適用したことで、ほぼ全てを解消（実際にカバー）するか確定デッドコードと証明でき、コア15ファイル全体の実効カバレッジは最終的に**100.00%**まで到達した（項目11参照）。
9. ~~`lifting_97M.c`・`waveletbpe.c`~~ → 調査完了、両ファイルとも残存全行が確定デッドコード（実効カバレッジ100%）と確認。`lifting_97M.c`の`half<=1`分岐は`IMAGE_WIDTH_MIN`/`IMAGE_ROWS_MIN`（17）とレベル数固定（3）の組み合わせで数学的に到達不能、`waveletbpe.c`の`DWT_Reverse`内`FLOAT_WAVELET`分岐は唯一の呼び出し元（`bpe_decoder.c`の`DecodingOutputInteger`）が`DWTType==INTEGER_WAVELET`を既に保証しているため到達不能——CLIの固定値によるデッドコードとは異なる「呼び出し元の事前条件による」新しいデッドコードパターン。これ以上のテスト追加は不要。
10. ~~`BPEBlockCoding.c`のCustomWt帯域依存分岐~~ → 対応済み。16行中13行は`TypeC << (1<<(3-i)) != 1`という比較式が`TypeC`（4bitフィールド）に対して数学的に常に真になる（`&`と書くべきところの誤記と見られる）ため構造的到達不能と判明。当初「未解明」としていた149・238行目も、2条件ANDの`continue`の後段で片方の条件だけを再チェックする冗長構造だと判明し確定デッドコードと証明。残る1行（181行目、`si++;`）も`si`の消費順序を精査した結果、直前の行（99-100行目）が全く同じ条件を既にチェック済みであることが判明し、確定デッドコードと証明（実効カバレッジ100%）。
11. ~~残存カバレッジギャップ（`StagesCodingGaggles.c`・AC/DC側・`PatternCoding.c`）の解消~~ → 対応済み。`AdjustOutPut`で確立した「直接呼び出し＋全数掃引」手法をこれらにも適用し、コア15ファイル全体の実効カバレッジを99.00%→100.00%まで押し上げた。`verify/c_unit_tests/gen_stages_gaggles_coverage.c`（`StagesDeCodingGaggles1/2/3`をビット予算0〜150で全数掃引）と`gen_residual_gaps_coverage.c`（`RefBitsDe`/`ACGaggleDecoding`向け）を新設し、raw行カバレッジは94.72%→95.14%まで伸びた。加えて、残ったギャップを制御フロー解析した結果、「より狭いチェックポイントが必ず先に横取りする」「座標選択の`else`分岐が入室条件と数学的に矛盾する」「`RateReached`と`BitPlaneStopDecoding`は常に同時にしか変化しない」「呼び出し先が出力・状態変更手段を一切持たない」「直前のチェックが同一条件を既に消費している」という5つの確定デッドコードパターンを発見し、コア15ファイル全体（`AdjustOutput.c`・`StagesCodingGaggles.c`・`DC_EnDeCoding.c`・`PatternCoding.c`・`AC_BitPlaneCoding.c`・`BPEBlockCoding.c`を含む全15ファイル）が実効カバレッジ100%に到達した。
12. ~~`PatternCoding.c`の`CodingOptions`に残っていた分岐カバレッジの残存5分岐~~ → 対応済み。1分岐（4-bit Rice分割オプション選択チェーンの328行目偽方向）は反例となる具体的シンボル値の組み合わせを新たに発見しテストへ追加、残り4分岐（330・334・335・336行目）は推移律と「4実数のうち必ずどれかは他の3つ以下」という恒真命題から構造的到達不能と証明した。これによりコア15ファイル全て（`PatternCoding.c`含む）が実効カバレッジ100.00%（行・分岐とも）に到達し、本レポートで「未証明」のまま残っていた項目はなくなった。
13. **カバレッジ100%は「形式的な完全互換の証明」ではない、という指摘を踏まえた今後の拡張方針。** コードカバレッジ（行・分岐）は「そのコードパスを少なくとも1つの値で実行し、Rustと一致した」ことしか保証せず、「同じパスを通る他の全ての値」まで保証しない。実際、本プロジェクトで見つかった実バグ（DPCM N=16、float DWT丸め、デコード側`-f`無視）はいずれも「未踏コードパスに潜んでいたバグ」であり、カバレッジ改善はこの種の穴を塞ぐ作業として有効だったが、「踏んだパスの中の別の値」由来のバグを排除する力はない。全入力値についての形式的な証明に近づけるには、優先度順に以下が考えられる：
    - ~~**ランダム化・fuzzing試験の追加**~~ → 対応済み。`verify/fuzz_compat.py`を新設し、画像サイズ・ビット深度・符号・バイト順・レート・セグメントサイズ・画素内容をランダムに組み合わせて観点1と同じバイト一致チェックを繰り返す（COMPATIBILITY_REPORT.ja.md §2.3）。整数・float両DWTが既定対象（§3.3の根本原因修正後はfloat DWTも除外する理由がなくなったため）。手元で両DWT込み500ケース・3000ケースを実行し不一致0件。CIに2段階で統合: push/PRごとに固定シード50ケースの軽量スモークテスト、週次/手動で乱数シード5000ケースの深掘り（`deep-fuzz`ジョブ）。
    - ~~**既存の「直接呼び出し＋全数（または準全数）列挙」手法（Rice/DPCM/PatternMapping/`AdjustOutPut`で実施済み、COMPATIBILITY_REPORT.ja.md §2.2）を、他のアキュムレータ絡み関数にも拡張する**~~ → 対応済み。`CodingOptions`（単一/ペア/三つ組の網羅的スイープ、4230通り）に続き、`ACDepthEncoder`/`ACDepthDecoder`（N全域×セグメントサイズ5通り×分布3パターン、120通り）・`DCEntropyEncoder`/`DCEntropyDecoder`（`QuantizationFactorQ_prime`の到達可能な全分岐×Nブラケット4種×セグメントサイズ5通り×分布3パターン、75通り）も直接呼び出しで追加、いずれも不一致なし（COMPATIBILITY_REPORT.ja.md §2.2）。`ACGaggleEncoding`/`ACGaggleDecoding`・`DCEncoder`/`DCGaggleDecoding`自体（gaggle単位の内部関数、bpe-rs側も非公開）は直接テストせず、公開されている一段上の`ACDepthEncoder`等を経由して間接的に駆動している——これらの関数自体を直接呼び出すには、bpe-rs側の可視性変更が必要になるため見送った。なおこの過程で、DCテストベクタ生成時に`CustomWtLL3_2bits`（LL3サブバンド既定ウェイト、`HeaderInilization`が常に`3`にハードコード）をジェネレータ側が誤って0初期化していたことが発覚したが、bpe-rs本体の実装は最初から正しく実装済みで、テストハーネス側の初期化漏れだったと確認できた（本文参照）。
    - ~~**ミューテーションテスト**~~ → 対応済み（§3.9）。`cargo-mutants`で8ファイル・1131変異を自動実行（383検出・655未検出・73無効・20タイムアウト）した上で、「未検出」とされた変異から5件を手動で実ソースに適用し`--include-ignored`込みで再検証したところ、4/5は共有ベクタ試験で確実に検出され（自動実行の655件という数字は`../verify/vectors/*.txt`への相対パスが解決できないという計測ツール側の制約による過小評価と判明）、残る1件（`rice/encode.rs`の`input_val - 4`→`+4`、bit_length=4）は出力バイト列を一切変えない**数学的に等価な変異**（`bits_write`が下位ビットしか出力しないため4〜7の範囲で`-4`と`+4`が2進数下位2bitで一致）と確認できた——試験群の弱さではなく、原理的に検出不可能な変異だった。
    - ~~**float DWTの1ULP残存差（§3.3）の解決**~~ → 対応済み。当初は「コンパイラの丸め実装差」と誤診断していたが、`-ffp-contract=off`・`-O0`で解消しないことを実測で確認した上で、`compare_traces.py`にレベル単位のトレース地点（`post_idwt_level2`/`post_idwt_level1`）を新設して乖離箇所を最初のレベルまで絞り込み、最終的に真の原因を特定した：Cの「usual arithmetic conversions」が式全体ではなく演算子ごとに適用されるため、`inversef97f`のペア加算（例: `x[r_idx+1] + x[r_idx-1]`）は実際にはf32同士の加算として行われ、その結果が乗算時に初めてf64へ昇格する。bpe-rs側は加算前に各被加数を個別にf64化していたため、この昇格タイミングの違いがそのまま1 ULP差になっていた。`inverse_lifting97f`に「f32のまま加算してからf64化する」ヘルパーを追加して修正し、178/178ケース・fuzz3500ケースで不一致0件を確認した（§3.3）。
    - （費用対効果が低いと見込むため優先度は最も低い）**形式手法・シンボリック実行による等価性の数学的証明**。CCSDSの仕様自体は単純なビット操作の集合であり理論上不可能ではないが、投じる労力に見合わない可能性が高い。
