# Comparison result history

[日本語版](history.ja.md)

`COMPATIBILITY_REPORT.md` is a snapshot of the verification results at a point in time (overwritten each round), so it can't track past trends. This file gets one line appended every time a full verification round (`verify/run_compat.sh --include-slow` + `verify/run_unit_vectors.py` + coverage measurement) is run, to preserve that trend.

Rows come from two sources:

- **Manual full-verification rounds**: appended by a human following the procedure below. These are complete rows, including line and branch coverage.
- **Automatic follow-up (`.github/workflows/bpe-rs-poll.yml`)**: polls bpe-rs's `main` daily; whenever there's a new commit for which `verify/run_compat.sh --include-slow` and `verify/run_unit_vectors.py` both PASS, it automatically opens a submodule-bump PR, and this file gets one line appended as part of that PR (the coverage columns are `—` since coverage isn't re-measured). On FAIL, no PR is opened and the workflow simply fails, so that round isn't recorded here — check the poller's run history on the Actions side instead.

## How to append a row (manual round)

After running a full verification, append one line (add the new row at the end; don't edit existing rows).

```sh
git log -1 --format='%H'                 # this repo's commit
git -C bpe-rs log -1 --format='%H'       # bpe-rs's commit
```

- **Byte compatibility**: the result of `verify/run_compat.sh --include-slow` (e.g. `166/166 PASS`)
- **Function-level**: the result of `verify/run_unit_vectors.py` (e.g. `6/6 PASS`)
- **Line/branch coverage**: the raw value from `COMPATIBILITY_REPORT.md` §4, and the effective-coverage value with confirmed dead code excluded
- **Known differences**: whatever known differences remain at that point (see `COMPATIBILITY_REPORT.md` §0). Changes here are visible over time
- **Notes**: mismatches newly found in that round, fixes, coverage improvements, etc. — hints for the next person tracing the diff

**Note (2026-07-29)**: on 2026-07-29, bpe-rs's commit history was rebased and force-pushed (details are in the session transcript; there's no dedicated record in this file). As a result, the bpe-rs commit hashes cited by earlier rows (`4ca3eb4`, `e91f677`, `fb494d3`) can no longer be resolved (they don't exist on GitHub). Their value as a record of what actually existed and was correct at the time is unchanged, so the past rows themselves haven't been rewritten — be aware of this if you try to open those hashes directly. The bpe-rs commits referenced by each row from `e0a492f` onward may also have been replaced by different hashes due to the rewrite (this file records the hash as of the time it was appended, and hasn't been retroactively traced/corrected).

## History

| Date | This repo's commit | bpe-rs commit | Byte compatibility | Function-level | Line coverage (raw/effective) | Branch coverage (raw/effective) | Known differences | Notes |
|---|---|---|---|---|---|---|---|---|
| 2026-07-28 | `f0b15ab` | `4ca3eb4` | 166/166 PASS | 6/6 PASS | 95.14% / 100.00% | 86.77% / 99.91% | float DWT's 1-ULP residual (5/28 cases), 5 unproven branches in `PatternCoding.c` | First entry. Records the content of the current `COMPATIBILITY_REPORT.md` as-is. Earlier rounds (the progression of line coverage 97.93%→99.25%→99.91%, etc.) remain in the git history (`git log --oneline`) but aren't retroactively listed in this file |
| 2026-07-28 | `6c08a53` (the parent commit at the time this round's changes were made, pre-commit) | `4ca3eb4` (unchanged) | 166/166 PASS (unchanged) | 6/6 PASS (unchanged) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (previously 86.77% / 99.91%) | float DWT's 1-ULP residual (5/28 cases) only | Resolved the 5 remaining branches in `PatternCoding.c`'s `CodingOptions` 4-bit selection chain (`COMPATIBILITY_REPORT.md` §4 item 8). One branch (line 328) was covered by adding a new counterexample (TYPE_CI pattern values 0×4, 6×3) to `verify/c_unit_tests/gen_codingoptions_coverage.c`; the remaining 4 branches (lines 330, 334, 335, 336) were proven structurally unreachable via the tautology "of 4 real numbers, one is always ≤ the other 3" plus transitivity. All 15 core files reached 100.00% effective coverage (both line and branch), leaving float DWT's 1-ULP residual as the only known difference. Pipeline and function-level tests were unchanged so weren't re-run (byte-compatibility/function-level columns carried over from last time) |
| 2026-07-28 | `bb5c517` (the parent commit at the time this round's changes were made, pre-commit) | `e91f677` (bpe-rs, changed from `4ca3eb4`) | 166/166 PASS (unchanged) | **7/7 PASS** (`CodingOptions` added) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | float DWT's 1-ULP residual (5/28 cases) only | In response to the point that "100% coverage isn't a proof for all input values," started on §7 items 1 and 2. (1) Added the randomized fuzz test `verify/fuzz_compat.py` (integer DWT only, `BPE_RATE_ERROR` auto-retries), ran 2000 cases locally (`--seed 7`) with 0 mismatches, integrated into CI as a lightweight smoke test (50 cases per push/PR) plus a weekly deep run (5000 cases). (2) Added exhaustive (near-exhaustive) direct-call verification of `CodingOptions` on the bpe-rs side (new `verify/c_unit_tests/gen_codingoptions_vectors.c`, 4230 combinations across single/pair/triple symbols, added the `shared_vectors_match_c_reference` test at bpe-rs commit `e91f677`), 0 mismatches. The same kind of extension to `ACGaggleEncoding`/`ACGaggleDecoding` and `DCGaggleEncoding`/`DCGaggleDecoding` is left unstarted (see §7 item 13) |
| 2026-07-28 | `e0a492f` (the parent commit at the time this round's changes were made, pre-commit) | `fb494d3` (bpe-rs, changed from `e91f677`) | 166/166 PASS (unchanged) | **9/9 PASS** (`ACDepthEncoder`/`Decoder`, `DCEntropyEncoder`/`Decoder` added) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | float DWT's 1-ULP residual (5/28 cases) only | Addressed the rest of §7 item 13(2) (`ACGaggleEncoding`/`DCGaggleEncoding`, etc.). bpe-rs's private gaggle-level functions aren't tested directly; instead the publicly exposed layer above them, `ACDepthEncoder`/`DCEntropyEncoder`, is driven via direct calls (new `gen_ac_depth_vectors.c` with 120 combinations, `gen_dc_entropy_vectors.c` with 75 combinations), no mismatches. In the process it turned out the DC-side test-vector generator was zero-initializing `CustomWtLL3_2bits` (which `HeaderInilization` always hardcodes to 3, never 0), but bpe-rs's actual implementation (`dc_entropy_encoder`) had it correctly folded in from the start — a test-harness initialization gap, not an implementation bug. |
| 2026-07-29 | `e0b8ba7` (the parent commit at the time this round's changes were made, pre-commit) | `fb494d3` (unchanged, no code changes on the bpe-rs side) | 166/166 PASS (unchanged) | 9/9 PASS (unchanged) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | float DWT's 1-ULP residual (5/28 cases) only, unresolved | Started on the remaining 2 items of §7 item 13. (1) Empirically tried to resolve the float DWT 1-ULP residual (§3.3): neither `-ffp-contract=off` (confirmed irrelevant since this build's gcc default target has FMA disabled) nor `-O0` (tested the compile-time constant-folding hypothesis, which instead made things worse and was rejected) resolved it — remains a known limitation (§3.3 addendum). (2) Ran mutation testing (§3.9) with `cargo-mutants` — during the automated run, a background-task-tracking glitch let the process keep running undetected and kept mutating `bpe-rs`'s source, an incident recovered from without harm via `kill` and a `git status` check. The automated run (1131 mutants, without `--include-ignored`) reported 655 undetected, but manually applying 5 of those to the real source and re-verifying with `--include-ignored` showed 4/5 were in fact caught by the shared-vector tests, and the remaining 1 was a mathematically equivalent mutation that doesn't change the output byte stream — confirming the test suite's actual detection power is higher than the automated run's raw number suggests. No code changes on the bpe-rs side (verification only, everything restored). |
| 2026-07-29 | `9fd9a87` (the parent commit at the time this round's changes were made, pre-commit) | `758c895` (bpe-rs) | 172/172 PASS (without `--include-slow`; the existing 166 plus some re-counted cases. Unchanged) | 9/9 PASS (unchanged) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | float DWT's 1-ULP residual (5/28 cases) only, unresolved | Further investigation into the float DWT 1-ULP residual (§3.3). Added level-granularity trace points `post_idwt_level2`/`post_idwt_level1` to `verify/compare_traces.py` (added a `#ifdef BPE_TRACE` hook to `original/source/lifting_97f.c`, added the corresponding dump to bpe-rs's `lifting97f.rs`). Running against the known-reproducing case showed the divergence isn't accumulated across all 3 levels but **already present at the coarsest, first level** — narrowed the cause from "somewhere in the whole inverse DWT" down to "somewhere within the first level." Didn't pin down the single row/column-pass call, and didn't resolve it (remains a known limitation). |
| 2026-07-29 | `cbe013c` | `6b0e68e` (bpe-rs) | **178/178 PASS** (with `--include-slow`; all rate values, including the 5 that used to be the known difference, are back) | 9/9 PASS (unchanged) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | **None** (float DWT's 1-ULP residual fully resolved) | Re-investigated in response to the user's request to "seriously resolve" the 1-ULP residual, and identified and fixed §3.3's residual's **true root cause**. Using the previous round's finding that "the divergence is already present at the first level" as a lead, built a standalone counterexample reproducing it from a minimal 64-element input; disassembly comparison via `gdb` on the C side and source inspection on the Rust side found the cause: C's "usual arithmetic conversions" apply **per operator, not to the whole expression** — `inversef97f`'s pairwise additions (e.g. `x[r_idx+1] + x[r_idx-1]`) are, on the C side, both f32 operands, so the addition itself happens in f32, only promoting to f64 once it's multiplied by the (unsuffixed, f64) coefficient; bpe-rs's `inverse_lifting97f`, however, converted each operand to f64 individually **before** the addition. This wasn't a "compiler rounding difference" but a genuine implementation bug rooted in addition order (f32-first vs. f64-first). Fixed by adding a `fp(a: f32, b: f32) -> f64` helper (add in f32, then widen to f64) (bpe-rs commit `8ca894e`; removal of the temporary diagnostic trace hooks is `6b0e68e`). The minimal counterexample went from 13/64 elements mismatching to 0; the known-reproducing case (`baseline_256`, rate=0.1, `-t 0`, segment=64) was confirmed to match exactly at `post_idwt` and byte-for-byte in the final output. Removed all the exclusions in `run_compat.sh --include-slow`, now 178/178 PASS; `fuzz_compat.py --dwt-type both` shows 0 mismatches across 500 and 3000 cases. Also checked the encode side (`forwardf97f`) and confirmed no room for the same kind of bug there, since C's `LowPassFilter`/`HighPassFilter` are declared `const float` from the start — known differences reached zero this round. |
| 2026-07-29 | `0d27b04` | `bd861cc` (bpe-rs, unchanged) | **178/178 PASS** (also confirmed 178/178 PASS on an Apple M5, ARM64/macOS, Apple clang -- 2 architectures now confirmed alongside x86-64/Linux) | 9/9 PASS (unchanged) | 95.14% / 100.00% (unchanged) | 86.79% / 100.00% (unchanged) | **None** (fixed a newly discovered FMA-contraction bug) | A "give me harsh feedback" review flagged a limitation (§5): every measurement had only ever come from one build environment. The user actually tested this on an Apple M5 Mac. Building on macOS/Apple clang/ARM64 and running `run_compat.sh --include-slow` FAILed 56 of 178 cases -- every single float-DWT (`-t 0`) case without exception, and for cases like `baseline_256_t0_r0` even the encode-side `.bpe` output itself diverged from the very first bytes -- systematic and large-scale, not the old rare 1-ULP kind. Root-caused to `original/source/makefile`'s `BPEFLAGS` never pinning `-ffp-contract`, leaving it to the compiler's FP_CONTRACT default. x86-64 baseline targets have no FMA instruction at all (exactly why §3.3's earlier `-ffp-contract=off` experiment found no effect -- there was nothing to fuse into regardless), but ARM64 has FMA as part of its baseline ISA, so the same default setting actually fuses there. Since bpe-rs's `f32`/`f64` operators always compute separately and never auto-fuse, even `forwardf97f` (pure float arithmetic, unrelated to §3.3's type-promotion issue) diverged wherever C-side fusion occurred. Fixed by adding `-ffp-contract=off` to both `BPEFLAGS` and `BPEFLAGS_TRACE` (only `original/source/makefile` changed, no bpe-rs changes). Confirmed 178/178 PASS on the reporting Mac after the fix, and rebuilt + reran the full suite on Linux too, confirming 178/178 PASS with no regression. Documented as §3.10 in COMPATIBILITY_REPORT (both languages), with §3.8's resolution table, §5's environment-limitation bullet, and §0's summary all updated to match. |
